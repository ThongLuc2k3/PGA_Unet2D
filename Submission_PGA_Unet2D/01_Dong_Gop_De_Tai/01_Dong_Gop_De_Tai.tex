
\section{Đóng Góp Của Đề Tài}

\textbf{Đề tài:} Phát triển hệ thống phân đoạn ảnh X-quang xương dựa vào câu nhắc trực quan  
\textbf{GVHD:} [Tên thầy/cô] | \textbf{Năm:} 2026

\vspace{0.5em}\hrule\vspace{0.5em}


\subsection{Đóng Góp 1 — Nghiên Cứu: Kiến Trúc PGA-UNet}

Đề xuất mô hình \textbf{PGA-UNet (Prompt-Guided Attention U-Net)} — kiến trúc phân đoạn ảnh y khoa có hướng dẫn từ bác sĩ qua hộp giới hạn (bounding box). Hai thành phần mới được tích hợp vào nền tảng U-Net, không kế thừa từ bài báo nào:


\subsubsection{1.1 Cổng không gian tại bộ mã hóa (Prompt Spatial Gate — PSG)}

Bản đồ nhiệt câu nhắc được nhân có chọn lọc vào từng lớp đặc trưng của bộ mã hóa:

\[
\tilde{\mathbf{x}}^l = \mathbf{x}^l \odot \bigl(1 + \alpha \cdot \sigma(\mathbf{W}_{gate} \ast \mathbf{H}^l)\bigr)
\]

Thiết kế \textbf{tăng cường} (không ức chế): vùng trong câu nhắc được khuếch đại, vùng ngoài giữ nguyên. Nhờ đó, bộ mã hóa tập trung học đặc trưng tổn thương ngay từ đầu mà không mất thông tin toàn cục.


\subsubsection{1.2 Cơ chế chú ý có điều kiện tại bộ giải mã (Conditioned Attention Decoder — CAD)}

Tín hiệu gating được điều kiện hóa bởi câu nhắc với \textbf{trọng số tin cậy giảm dần} theo độ sâu tầng giải mã:

\[
\mathbf{g}' = \mathbf{g} + c \cdot \alpha \cdot w \cdot \mathbf{p}_{\text{enc}}, \quad w \in \{1.0,\, 0.7,\, 0.4,\, 0.2\}
\]

Câu nhắc dẫn hướng từ ngữ nghĩa (tầng sâu) đến chi tiết đường biên (tầng nông), phù hợp với vai trò hướng dẫn giảm dần.


\subsubsection{1.3 Mã hóa câu nhắc Plateau Heatmap}

Hộp giới hạn được chuyển thành bản đồ nhiệt 2D: gán giá trị 1.0 đồng đều bên trong, sau đó làm mờ viền bằng bộ lọc Gaussian (kernel 31$\times$31):

\[
\mathbf{H}_{prompt} = \text{GaussianBlur}(\mathbf{B}_{mask},\; k=31)
\]

\textbf{Tại sao không dùng mặt nạ nhị phân:} đường biên sắc nét tạo gradient giả tạo khiến mạng học cạnh hộp thay vì đặc trưng tổn thương.  
\textbf{Tại sao không dùng Gaussian thuần:} mô hình SAM dùng positional embedding rời rạc (2 vector 256 chiều) phù hợp Transformer; PGA dùng heatmap 2D phù hợp trực tiếp với feature map U-Net qua phép nhân theo phần tử.

\vspace{0.5em}\hrule\vspace{0.5em}


\subsection{Đóng Góp 2 — Sản Phẩm: Pipeline Lâm Sàng End-to-End}

Tích hợp 3 thành phần thành hệ thống phần mềm triển khai được:


\subsubsection{2.1 Bộ Lọc Sàng Lọc (EfficientNet\_B3)}

Phân loại tự động ảnh X-quang "có bệnh lý / bình thường" trước khi đưa vào PGA-UNet.

\begin{itemize}
  \item \textbf{Kiến trúc:} EfficientNet\_B3, \textasciitilde{}10.7M tham số, ảnh 300$\times$300, ImageNet pretrained
  \item Fine-tune 2 giai đoạn trên BTXRD: giai đoạn 1 (25 epoch) đóng băng backbone, giai đoạn 2 (75 epoch) mở khóa toàn bộ trọng số trừ BatchNorm
  \item Kết quả standalone (tập test 375 ảnh): Accuracy=85.60\%, Recall=78.61\%, Specificity=92.55\%, \textbf{AUC-ROC=0.9258}
\end{itemize}


\subsubsection{2.2 Đánh Giá Pipeline End-to-End (dataset\_online)}

Pipeline chạy trên \textbf{dataset\_online} (375 ảnh hỗn hợp: 187 có bệnh + 188 bình thường) — EfficientNet\_B3 phân lớp trước, PGA-UNet phân đoạn sau.

\textbf{Kết quả phân loại:} TP=181, FP=38, FN=6, TN=150 — Sensitivity=96.79\%, Specificity=79.79\%, AUC-ROC=0.9688.

\textbf{Kết quả phân đoạn pipeline (per-polygon):}
\[
\text{Pipeline Dice} = \frac{\displaystyle\sum_{i \in \text{polygon(TP)}} \text{Dice}(i)}{N_{\text{polygon(TP)}} + N_{\text{image(FP)}}} = \frac{\sum 226 \text{ polygon Dice}}{226 + 38} = \mathbf{0.7296}
\]

Pipeline Dice (0.7296) thấp hơn PGA standalone (0.8524) do 38 ảnh FP kéo điểm xuống. Bottleneck là Specificity (79.79\%) — cải thiện Specificity sẽ trực tiếp nâng Pipeline Dice.


\subsubsection{2.3 Ứng dụng tích hợp (app.py)}

Giao diện Gradio đóng gói toàn bộ pipeline — bác sĩ tải ảnh X-quang, EfficientNet\_B3 phân lớp tự động, vẽ hộp giới hạn, PGA-UNet phân đoạn và hiển thị kết quả.

\vspace{0.5em}\hrule\vspace{0.5em}


\subsection{Tóm Tắt Điểm Khác Biệt So Với SAM-Med2D}

\begin{table}[htbp]
\centering\renewcommand{\arraystretch}{1.2}
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|l|l|l|l|}
\hline
\textbf{Tiêu chí} & \textbf{PGA-UNet} & \textbf{SAM FT} & \textbf{SAM ZS} & \textbf{U-Net / AttUNet} \\
\hline
Tham số & \textasciitilde{}4M & \textasciitilde{}100M & \textasciitilde{}100M & \textasciitilde{}7M / \textasciitilde{}8M \\
\hline
Câu nhắc & Plateau Heatmap & Pos.\ embedding & Pos.\ embedding & Không có \\
\hline
Độ phân giải & 512$\times$512 & 256$\times$256 & 256$\times$256 & 512$\times$512 \\
\hline
Dice Zoom-out & \textbf{0.8524} & 0.7350 & 0.5337 & 0.4534 / 0.4159 \\
\hline
Dice Shift & \textbf{0.8382} & 0.7097 & 0.5184 & N/A \\
\hline
Dice Mixed 70/30 & \textbf{0.8496} & 0.7283 & 0.5286 & N/A \\
\hline
HD95 (norm) Zoom-out & \textbf{0.027} & 0.207 & 0.374 & — \\
\hline
VRAM suy luận & \textasciitilde{}1 GB & \textasciitilde{}2 GB & \textasciitilde{}2 GB & \textasciitilde{}0.5 GB \\
\hline
Huấn luyện & Từ đầu BTXRD & Fine-tune ViT-B & Không fine-tune & Từ đầu BTXRD \\
\hline
\end{tabular}%
}
\end{table}
