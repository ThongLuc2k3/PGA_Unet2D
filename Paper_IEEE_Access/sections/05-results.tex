\section{Results}
\noindent\textbf{Status note.} The numerical tables currently shown in this section are legacy reference values from the previous protocol and are retained only to preserve the comparison structure while retraining is still pending. They have not yet been refreshed under the current standardized pipeline, which uses `center_mixed` training with 80\% `center_shift` and 20\% `center_zoom`, 150 training epochs, image-level merged validation selection under `center_shift`, and the current Precision definition. Until the retrained checkpoints and updated CSVs are in place, the text in this section should be read as a description of the intended comparison logic and of the claim scope, not as finalized evidence for the current manuscript version.

\subsection{Main Comparison Across Datasets and Resolutions}
This first group of experiments addresses the most basic question: does PGA-UNet remain effective across both datasets, across both tested input resolutions, and across both prompt scenarios? In the refreshed manuscript, this subsection should validate the main system-level contribution, namely that prompt-conditioned processing through both encoder and decoder remains useful after the search space has already been tightened by a coarse box prompt. Because the current tables are legacy placeholders, this subsection should not yet claim reproduced performance for the standardized protocol. After retraining, the interpretation should remain limited to independent within-dataset evaluation on BTXRD and FracAtlas, not to cross-dataset generalization.


\subsection{Effect of Input Resolution}
This experiment isolates the contribution of image resolution within the same architecture and prompt protocol. It is meant to support the claim that the proposed method is especially useful when small-lesion evidence would otherwise be weakened after the search space has been reduced but the lesion signal itself is still fragile. PGA-UNet is trained independently at $256\times256$ and $512\times512$ on each dataset. Until retraining is complete, the specific numbers in Table~\ref{tab:resolution} should not be cited as current-protocol evidence; only the intended comparison design should be carried forward.

\begin{table}
\caption{PGA-UNet Dice under covering and off-center prompts at two resolutions.}
\label{tab:resolution}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcc}
\toprule
Dataset & Resolution & Covering & Off-center \\
\midrule
BTXRD & $256\times256$ & 0.8449 & 0.8111 \\
BTXRD & $512\times512$ & \textbf{0.8788} & \textbf{0.8496} \\
\midrule
FracAtlas & $256\times256$ & 0.7974 & 0.7619 \\
FracAtlas & $512\times512$ & \textbf{0.8286} & \textbf{0.7925} \\
\bottomrule
\end{tabular}
}
\end{table}

The resolution comparison should not be overinterpreted as a pure image-size effect because the prompt heatmap is built once at the original image resolution with a fixed Gaussian kernel and only then downsampled together with the image, so the same absolute blur is compressed more aggressively into the $256\times256$ frame than into the $512\times512$ one. Even with that caveat, the main trend remains useful: prompt-guided segmentation of small radiographic lesions benefits from preserving more spatial detail, especially when the lesion occupies only a tiny fraction of the image.


\subsection{Monte Carlo Cross-Validation}
This experiment examines whether the main findings remain stable when the image-level split is redrawn repeatedly under limited data. It supports the robustness claim at the dataset-partition level rather than at the prompt-perturbation level. Supplementary experiments therefore used Monte Carlo cross-validation, that is, four repeated random image-level splits of the $512\times512$ PGA-UNet configuration, evaluated separately under covering and off-center prompts. These results are not intended to replace the fixed-split main tables, but they provide additional evidence that the prompt-guided gains are not tied to a single favorable split.

\begin{table}
\caption{Monte Carlo cross-validation for PGA-UNet at $512\times512$ (4 repeated random splits, mean $\pm$ standard deviation).}
\label{tab:mccv}
\centering
\scriptsize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llccc}
\toprule
Dataset & Prompt & Dice & CBL & HD95 \\
\midrule
\multirow{2}{*}{BTXRD}
& Covering & $0.8715\pm0.0083$ & $0.9600\pm0.0028$ & $8.09\pm0.67$ \\
& Off-center & $0.8357\pm0.0120$ & $0.9390\pm0.0062$ & $10.45\pm0.65$ \\
\midrule
\multirow{2}{*}{FracAtlas}
& Covering & $0.8281\pm0.0052$ & $0.9597\pm0.0039$ & $6.93\pm0.08$ \\
& Off-center & $0.7862\pm0.0114$ & $0.9297\pm0.0054$ & $8.75\pm0.48$ \\
\bottomrule
\end{tabular}
}
\end{table}

In the final refreshed version, this subsection should be used only to support split-level stability of PGA-UNet under the evaluated protocol. It should not be used by itself to claim superiority over any baseline. If paired statistical comparisons against baselines are later added, they should be presented separately rather than folded into this Monte Carlo section.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/results/prompt_robustness_examples.png}
\caption{Qualitative behavior under prompt perturbation. PGA-UNet remains more stable when the prompt is shifted away from the ideal covering box, although thin fracture patterns are still harder than larger compact lesions.}
\label{fig:robust}
\end{figure}


\subsection{Comparison with the Automatic Baseline}
Attention U-Net receives no clinician-provided box, so this comparison is not a like-for-like test of two prompt-guided methods. Its purpose is instead to measure how difficult this segmentation task is, over the full test set, when the network must localize the lesion entirely on its own, which is exactly the step that a coarse clinician prompt is meant to remove. Table~\ref{tab:baseline} therefore serves as an automatic-reference comparison, not as a fully matched architecture comparison. In the refreshed manuscript, this subsection should support the narrower claim that automatic localization is a major challenge in this task setting, while avoiding any wording that would treat the Attention U-Net row as proof that PGA-UNet is architecturally superior in a like-for-like sense.

\begin{table}
\caption{Comparison with the automatic baseline at $512\times512$ under covering prompts.}
\label{tab:baseline}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llccc}
\toprule
Dataset & Model & Dice & CBL & HD95 \\
\midrule
\multirow{2}{*}{BTXRD}
& Attention U-Net & 0.5160 & 0.5995 & 129.67 \\
& PGA-UNet & \textbf{0.8788} & \textbf{0.9619} & \textbf{9.08} \\
\midrule
\multirow{2}{*}{FracAtlas}
& Attention U-Net & 0.3075 & 0.4658 & 110.83 \\
& PGA-UNet & \textbf{0.8286} & \textbf{0.9615} & \textbf{6.21} \\
\bottomrule
\end{tabular}
}
\end{table}


\subsection{Attention U-Net Top- and Bottom-Dice Subsets}
This subgroup analysis is an extension of the automatic-baseline comparison. After comparing PGA-UNet with Attention U-Net at the dataset level, we ask a more specific question: how does PGA-UNet behave on images where the automatic baseline is relatively strong versus relatively weak? The purpose is to clarify the practical meaning of the Attention U-Net comparison rather than to define objective data difficulty. We therefore compare Attention U-Net top-Dice and bottom-Dice image groups on both BTXRD and FracAtlas. Specifically, for each dataset we select the 50 test images with the highest Attention U-Net Dice scores and the 50 test images with the lowest Attention U-Net Dice scores, then re-evaluate PGA-UNet on the same two groups. This analysis does not define intrinsic data difficulty; it only reports performance on subsets induced by the Attention U-Net ranking.

\begin{table}
\caption{Top-Dice and bottom-Dice comparison under covering prompts at $512\times512$ (50 images per group).}
\label{tab:extreme}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lllccc}
\toprule
Dataset & Group & Model & Dice & CBL & HD95 \\
\midrule
\multirow{4}{*}{BTXRD}
& \multirow{2}{*}{Top-Dice} & Attention U-Net & 0.8629 & 0.9436 & 39.32 \\
& & PGA-UNet & \textbf{0.9036} & \textbf{0.9709} & \textbf{9.23} \\
& \multirow{2}{*}{Bottom-Dice} & Attention U-Net & 0.0335 & 0.0928 & 270.72 \\
& & PGA-UNet & \textbf{0.8521} & \textbf{0.9459} & \textbf{5.19} \\
\midrule
\multirow{4}{*}{FracAtlas}
& \multirow{2}{*}{Top-Dice} & Attention U-Net & 0.4428 & 0.6277 & 82.08 \\
& & PGA-UNet & \textbf{0.8318} & \textbf{0.9666} & \textbf{6.37} \\
& \multirow{2}{*}{Bottom-Dice} & Attention U-Net & 0.1529 & 0.3345 & 135.76 \\
& & PGA-UNet & \textbf{0.8151} & \textbf{0.9582} & \textbf{6.25} \\
\bottomrule
\end{tabular}
}
\end{table}

Because the four groups are defined directly from Attention U-Net scores, these values should not be interpreted as global averages or as intrinsic clinical difficulty strata. In the refreshed manuscript, this subsection should keep that narrower interpretation explicit: the top-Dice and bottom-Dice groups only show how the prompt-guided models behave on image stems where the automatic baseline is relatively stronger or weaker.

The qualitative examples help clarify this behavior. In the Attention U-Net top-Dice groups, the automatic baseline can often place the predicted mask in roughly the correct anatomical region, but it may still produce spurious responses far from the lesion. Even when the lesion itself is covered reasonably well, those distant false positives reduce overlap-based metrics such as Dice and IoU, which is consistent with the still-elevated HD95 values (39.32 on BTXRD, 82.08 on FracAtlas) despite a moderate-to-low Dice. PGA-UNet is less prone to this pattern because the bounding box prompt constrains the model to a more relevant spatial region. In the bottom-Dice groups, the failure is more severe: the automatic baseline may localize the wrong region or miss the lesion almost entirely. A likely reason is that the attention gate itself has no guarantee of learning a clinically correct gating policy: it filters whatever features it has learned to emphasize, whether those features correspond to lesions or to spurious high-energy structures. When the learned response is dominated by background clutter or artifact-like signals with strength comparable to, or larger than, the true lesion evidence, the gate can no longer rescue localization. In practice, it may suppress only mild background activity while remaining ineffective against stronger false responses. In contrast, PGA-UNet receives a coarse spatial prior and can therefore identify the lesion region more reliably before refining the mask.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/results/unet_extreme_groups_btxrd.png}
\caption{Qualitative comparison between Attention U-Net and PGA-UNet on BTXRD for one case from the Attention U-Net top-Dice group (IMG000184) and one case from the Attention U-Net bottom-Dice group (IMG000791). Each row shows four panels; for PGA-UNet, the input-image and box-prompt panels are merged into a single "Input image (with prompt)" panel.}
\label{fig:extreme-btxrd}
\end{figure}


\subsection{Matched Comparison with SAM-Med2D}
This comparison tests the architectural contribution of the proposed prompt handling against a prompt-based foundation baseline under matched input conditions. Both models receive the same prompt type and the same resolution, so the difference is more directly tied to how prompt information is represented and propagated after localization has already been externally provided. Table~\ref{tab:sam} reports the intended matched comparison at $256\times256$ under both covering and off-center prompts. Because the present table still contains legacy values, this subsection should currently be interpreted as the comparison structure and claim scope only; the final wording about relative performance against SAM-Med2D must be decided after retraining.

\begin{table}
\caption{Dice comparison with fine-tuned SAM-Med2D at $256\times256$.}
\label{tab:sam}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcc}
\toprule
Dataset & Prompt & PGA-UNet & SAM-Med2D-FT \\
\midrule
\multirow{2}{*}{BTXRD}
& Covering & \textbf{0.8449} & 0.7624 \\
& Off-center & \textbf{0.8111} & 0.7542 \\
\midrule
\multirow{2}{*}{FracAtlas}
& Covering & \textbf{0.7974} & 0.7439 \\
& Off-center & \textbf{0.7619} & 0.7275 \\
\bottomrule
\end{tabular}
}
\end{table}

When the tables are refreshed, the relative degradation from covering to off-center prompts should be reported separately from absolute accuracy. The two prompt conditions measure sensitivity to the controlled displacement defined in this study; they do not establish robustness to arbitrary clinician prompts. Any comparison with SAM-Med2D should therefore report both absolute Dice and the change between the two conditions without assuming that the model with the higher absolute Dice also has the smaller relative degradation.


\subsection{Performance on Small Lesions}
This experiment is an extension of the SAM-Med2D comparison to the setting in which the contribution of prompt-aware feature preservation should be most visible, namely very small lesions. To keep subgroup analysis objective, we retain only the small-lesion subset because it is defined directly by lesion size rather than by a model-dependent ranking rule. For each dataset, the subset is formed by selecting the 50 test images with the smallest total lesion area, evaluated under covering prompts. This subset is then used for two related comparisons: PGA-UNet versus fine-tuned SAM-Med2D at the matched resolution of $256\times256$, and PGA-UNet$_{256}$ versus PGA-UNet$_{512}$ to examine the effect of resolution. All metrics are computed at a shared $512\times512$ reference frame, with the $256\times256$ predictions of SAM-Med2D and PGA-UNet upsampled before scoring, so that the three models are compared on identical ground-truth masks.

\begin{table}
\caption{Dice, CBL, and HD95 on the 50 test images with the smallest total lesion area per dataset. IoU is omitted here because it is a noisy, boundary-sensitive statistic at this lesion size; Dice, CBL, and HD95 provide complementary overlap, centroid, and boundary measures.}
\label{tab:small}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llccc}
\toprule
Dataset & Model & Dice & CBL & HD95 \\
\midrule
\multirow{3}{*}{BTXRD}
& SAM-Med2D-FT $256\times256$ & 0.1903 & 0.4489 & 240.72 \\
& PGA-UNet $256\times256$ & 0.7650 & 0.9261 & 3.11 \\
& PGA-UNet $512\times512$ & \textbf{0.8483} & \textbf{0.9451} & \textbf{2.32} \\
\midrule
\multirow{3}{*}{FracAtlas}
& SAM-Med2D-FT $256\times256$ & 0.3736 & 0.7116 & 105.19 \\
& PGA-UNet $256\times256$ & 0.7973 & 0.9569 & 5.62 \\
& PGA-UNet $512\times512$ & \textbf{0.8348} & \textbf{0.9609} & \textbf{4.83} \\
\bottomrule
\end{tabular}
}
\end{table}

In the refreshed manuscript, this subsection should remain focused on the narrow small-lesion question and should avoid overextending from subgroup results to broad clinical robustness. The safest interpretation is that very small lesions are an especially relevant stress case for prompt-conditioned segmentation, and that both explicit prompt conditioning and higher spatial resolution may matter more strongly there than on the full test set.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/results/small_lesion_sam_pga256_pga512.png}
\caption{Qualitative comparison on small lesions in BTXRD (IMG000868, two small polygons). The examples contrast fine-tuned SAM-Med2D at $256\times256$, PGA-UNet at $256\times256$, and PGA-UNet at $512\times512$; the input-image and box-prompt panels are merged into a single "Input image (with prompt)" panel. SAM-Med2D produces no visible prediction from the same prompt (Dice 0.000), while both PGA-UNet variants recover both lesions.}
\label{fig:small-btxrd}
\end{figure}


\subsection{Efficiency Analysis}
This experiment evaluates whether the proposed gains are achieved with a practical computational profile. It supports the claim that the method is suitable for repeated low-latency interactive use rather than only for offline batch inference. Table~\ref{tab:efficiency} reports the efficiency comparison, measured on the same NVIDIA Tesla T4 GPU used for training (mean latency over repeated forward passes, batch size 1). PGA-UNet contains only 2.95M parameters. In the tested $256\times256$ setting, this corresponds to an approximately 92-fold reduction in parameter count and an 11.9-fold reduction in FLOPs relative to SAM-Med2D, together with 6.0$\times$ faster GPU inference and 11.6$\times$ faster CPU inference.

\begin{table}
\caption{Efficiency comparison in the tested environment.}
\label{tab:efficiency}
\centering
\scriptsize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Model & Params & GFLOPs & Size & GPU ms & CPU ms \\
\midrule
PGA-UNet 256 & 2.95M & 7.74 & 11.35 MB & 8.17 & 109.17 \\
PGA-UNet 512 & 2.95M & 30.97 & 11.35 MB & 18.28 & 336.74 \\
SAM-Med2D 256 & 271.24M & 92.02 & 2443.16 MB & 48.75 & 1263.77 \\
\bottomrule
\end{tabular}
}
\end{table}


\subsection{Ablation Study}
The ablation study directly tests which proposed components are responsible for the observed gains. In particular, it examines whether performance comes from the prompt representation itself, from encoder-side prompt gating (PSG), from decoder-side prompt-conditioned attention (CAD), or from their combination. We isolate three factors: whether PSG is present in the encoder, what form of decoder skip-attention is used (none, the original unconditioned Attention U-Net gate, or CAD), and whether the prompt is represented as a binary box or a Gaussian-smoothed plateau. Combining these factors gives eight trained configurations per dataset, all at $512\times512$: the seven partial variants that isolate individual factors and their pairwise interactions, plus the full proposed PGA-UNet, which uses PSG, CAD, and the Gaussian prompt together. Tables~\ref{tab:ablation-btxrd} and~\ref{tab:ablation-fracatlas} report Dice under covering and off-center prompts for BTXRD and FracAtlas, respectively.

\begin{table}
\caption{Ablation results (Dice) on BTXRD at $512\times512$.}
\label{tab:ablation-btxrd}
\centering
\scriptsize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{ccccc}
\toprule
PSG & Decoder attention & Prompt & Covering & Off-center \\
\midrule
No & None & Gaussian & 0.8596 & 0.8230 \\
No & None & Binary & 0.8597 & 0.8253 \\
No & Vanilla gate & Gaussian & 0.8671 & 0.8408 \\
No & CAD & Gaussian & 0.8685 & 0.8358 \\
Yes & None & Gaussian & 0.8567 & 0.8297 \\
Yes & Vanilla gate & Gaussian & 0.8611 & 0.8314 \\
Yes & CAD & Binary & 0.8611 & 0.8317 \\
Yes & CAD & Gaussian (PGA-UNet) & \textbf{0.8788} & \textbf{0.8496} \\
\bottomrule
\end{tabular}
}
\end{table}

\begin{table}
\caption{Ablation results (Dice) on FracAtlas at $512\times512$.}
\label{tab:ablation-fracatlas}
\centering
\scriptsize
\resizebox{\columnwidth}{!}{%
\begin{tabular}{ccccc}
\toprule
PSG & Decoder attention & Prompt & Covering & Off-center \\
\midrule
No & None & Gaussian & 0.7727 & 0.7356 \\
No & None & Binary & 0.7670 & 0.7199 \\
No & Vanilla gate & Gaussian & 0.7560 & 0.7311 \\
No & CAD & Gaussian & 0.8158 & 0.7777 \\
Yes & None & Gaussian & 0.7115 & 0.6990 \\
Yes & Vanilla gate & Gaussian & 0.7372 & 0.7170 \\
Yes & CAD & Binary & 0.8132 & 0.7896 \\
Yes & CAD & Gaussian (PGA-UNet) & \textbf{0.8286} & \textbf{0.7925} \\
\bottomrule
\end{tabular}
}
\end{table}

The full PGA-UNet configuration (PSG, CAD, and the Gaussian prompt combined) is the intended reference row in this ablation design. Once retrained under the current protocol, the safest wording is that the resulting pattern may be consistent with complementary contributions from PSG, CAD, and Gaussian prompt encoding within the evaluated configurations. Without repeated multi-seed ablation runs or factorial statistical analysis, the manuscript should avoid stronger language such as ``proves synergy'' or any claim of definitive mechanistic decomposition.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/ablation/ablation_gaussian_vs_binary_case.png}
\caption{Qualitative comparison between the Gaussian prompt representation and the binary prompt representation. The Gaussian version produces cleaner boundaries and better overlap on this representative BTXRD example.}
\label{fig:ablation-gaussian-binary}
\end{figure}

\subsection{Failure Modes}
The method still has identifiable failure cases. In the qualitative review collected during the thesis experiments, lower Dice scores were more likely for lesions with highly elongated or branching shapes, lesions located in anatomically cluttered regions such as the shoulder or clavicle, and lesions with weak contrast relative to surrounding structures. These cases are relevant because they show that prompt guidance does not eliminate ambiguity once the local image evidence itself is poor.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/failure/failure_case_overlap.png}
\caption{Representative failure case in an anatomically overlapping region. Even with a covering prompt, the lesion boundary remains difficult because the local X-ray structures are visually confounded.}
\label{fig:failure}
\end{figure}
