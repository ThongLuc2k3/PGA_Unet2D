% ==========================================
% CHƯƠNG 4: THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ
% ==========================================
% Biến số liệu, cập nhật sau khi có kết quả mới
\newcommand{\Ntest}{232}           % N test samples (per-polygon)
\newcommand{\Ntestimg}{187}        % N test images  (merged mask, UNet/AttUNet)
\newcommand{\Ntrain}{1\,493}       % N train samples
\newcommand{\Nval}{187}            % N val samples

% ==========================================
\chapter{THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ}
\label{Chapter4}

Chương này tổ chức kết quả thực nghiệm thành hai phần. \textbf{Phần I} tập trung đánh giá kiến trúc PGA-UNet trên tập kiểm thử \Ntest mẫu độc lập: so sánh với các mô hình cơ sở và SAM-Med2D, ablation study (PSG/CAD), đánh giá tính bền bỉ (Robustness) và kiểm định xác thực chéo. \textbf{Phần II} trình bày đánh giá module Gatekeeper hỗ trợ (EfficientNet\_B3) và pipeline end-to-end trên dữ liệu hỗn hợp thực tế.

% ============================================================
% PHẦN I, ĐÓNG GÓP 1: Nghiên cứu kiến trúc PGA-UNet
% ============================================================

% ------------------------------------------------------------
\section{Thiết lập môi trường thực nghiệm}
\label{sec:thiet_lap}
% ------------------------------------------------------------

Để đảm bảo tính khách quan và khả năng tái lập (reproducibility) của các kết quả nghiên cứu, mục này trình bày chi tiết về quy mô dữ liệu, phương pháp phân chia các tập đánh giá, cấu hình phần cứng cũng như các siêu tham số (hyperparameters) được thiết lập trong suốt quá trình huấn luyện mô hình.

\subsection{Tập dữ liệu ảnh X-quang (Dataset)}
\label{subsec:dataset}

Dữ liệu thực nghiệm được sử dụng trong khóa luận là bộ \textbf{BTXRD} (\textit{Bone Tumor X-Ray Dataset})~\cite{btxrd2024}, một tập ảnh X-quang xương công khai có đầy đủ ba cấp nhãn: phân lớp, định vị và phân đoạn pixel-level. Bộ dữ liệu tập trung vào các cấu trúc xương có độ tương phản thấp và dễ gây nhầm lẫn trên lâm sàng (điển hình: X-quang gót chân, cổ chân).

\textbf{Lý do chọn BTXRD:} Không giống các bộ dữ liệu X-quang công khai chỉ cung cấp nhãn phân lớp mà thiếu mặt nạ phân đoạn, BTXRD cung cấp đồng thời cả ba cấp nhãn (phân lớp, định vị, phân đoạn pixel-level), đáp ứng đầy đủ yêu cầu huấn luyện có giám sát. Ảnh X-quang thô chứa nhiều nhiễu kỹ thuật (ký tự R/L, metadata) và đã trải qua quy trình tiền xử lý nghiêm ngặt bằng YOLOv11s như mô tả ở Mục~\ref{sec:tien_xu_ly}.

\textbf{Hai phân vùng dữ liệu song song:} Kiến trúc hệ thống sử dụng hai tập dữ liệu với mục đích khác nhau:
\begin{itemize}
    \item \textbf{Dữ liệu phân đoạn} (cho PGA-UNet và baseline): Chỉ chứa các ảnh \textit{có bệnh lý} kèm mặt nạ phân đoạn pixel-level. Đây là giới hạn tự nhiên của học có giám sát, chỉ ảnh có nhãn mới dùng được.
    \item \textbf{Dữ liệu phân lớp} (cho Gatekeeper): Chứa cả ảnh bệnh lý lẫn ảnh bình thường, phân chia theo tỷ lệ 80/10/10 (train/val/test), batch size = 16.
\end{itemize}

\textbf{Thống kê dữ liệu:} Bảng~\ref{tab:dataset_stats} tóm tắt phân bố dữ liệu sau khi phân chia.

\begin{table}[htbp]
    \centering
    \caption{Thống kê phân bố bộ dữ liệu thực nghiệm}
    \label{tab:dataset_stats}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|c|p{5.5cm}|}
        \hline
        \textbf{Tập dữ liệu} & \textbf{Số ảnh} & \textbf{Số mẫu (per-polygon)} & \textbf{Ghi chú} \\
        \hline
        Huấn luyện (Train) & \Ntrain & --- & Tất cả ca bệnh lý \\
        \hline
        Xác thực (Validation) & \Nval & \Nval & \\
        \hline
        Kiểm thử (Test) & \Ntestimg & \Ntest & Nhiều ảnh có $\geq$2 polygon $\rightarrow$ \Ntest~mẫu per-polygon \\
        \hline
    \end{tabular}
\end{table}

\textbf{Thống kê bộ dữ liệu phân lớp (Gatekeeper):} Bộ BTXRD gồm 3.746 ảnh X-quang (cả lành lặn lẫn bệnh lý) được dùng toàn bộ để huấn luyện mô hình gác cổng, phân chia train/val/test theo tỷ lệ 80/10/10 ($\approx$2.996/375/375~ảnh). Tập phân lớp và tập phân đoạn dùng chung ảnh bệnh lý nhưng độc lập về nhãn: nhãn nhị phân (lành/bệnh) cho Gatekeeper, nhãn mặt nạ pixel-level cho PGA-UNet.

\textbf{Phân tích kết quả:}
\begin{itemize}
    \item \textbf{Sự giới hạn của AI tự động:} U-Net gốc đạt Dice = 0.4534, phản ánh sự chật vật khi phải tự tìm vùng tổn thương trên ảnh X-quang thô. Đáng chú ý, Attention U-Net còn sụt giảm xuống Dice = 0.4159, thấp hơn cả U-Net gốc. Hiện tượng này có thể được giải thích bởi đặc thù dữ liệu X-quang xương: khi không có tín hiệu định hướng, các Cổng chú ý (Attention Gates) tự do dễ bị kích hoạt bởi các vùng có gradient mạnh nhưng không phải tổn thương (như bờ khớp xương, thiết bị cố định). Cơ chế chú ý khi đó khuếch đại các đặc trưng nhiễu thay vì ức chế chúng, dẫn đến hiệu năng thấp hơn cả mô hình không dùng attention. Nhóm thừa nhận rằng kết quả này có thể chịu ảnh hưởng bởi việc thiết lập siêu tham số chưa tối ưu cho Attention U-Net trên bộ dữ liệu này, và cần thêm thí nghiệm bổ sung để khẳng định chắc chắn hơn.
    \item \textbf{Giá trị của hướng tiếp cận tương tác:} PGA-UNet (kịch bản Zoom-out lý tưởng) đạt Dice = 0.8524, vượt trội hoàn toàn so với hai baseline tự động. Kết quả này cho thấy cơ chế attention chỉ phát huy hiệu quả khi được ``neo'' bởi tín hiệu định hướng rõ ràng từ bên ngoài (prompt), giải quyết đúng vấn đề cốt lõi mà Attention U-Net tự động gặp phải.
\end{itemize}

% ------------------------------------------------------------
\subsection{Cấu hình huấn luyện (Hyperparameters, Loss functions)}
\label{subsec:hyperparameters}
% ------------------------------------------------------------

Toàn bộ quá trình huấn luyện và suy luận thực nghiệm được triển khai trên môi trường Google Colab với GPU nhằm tăng tốc độ tính toán. Các mô hình được cài đặt dựa trên thư viện PyTorch.

\textbf{Hàm mất mát (Loss Function):}
Để xử lý mất cân bằng giữa foreground (vùng tổn thương) và background, hệ thống sử dụng hàm mất mát kết hợp:
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{BCE} + \mathcal{L}_{Dice}
\end{equation}
Thành phần $\mathcal{L}_{BCE}$ tối ưu phân loại từng pixel; $\mathcal{L}_{Dice}$ tối ưu độ chồng lấp diện tích tổng thể.

\textbf{Siêu tham số huấn luyện:}
Tất cả mô hình (U-Net, Attention U-Net, PGA-UNet) được huấn luyện dưới cùng thiết lập:
\begin{itemize}
    \item \textbf{Batch size:} 4 (phù hợp giới hạn VRAM khi dùng ảnh 512$\times$512).
    \item \textbf{Epochs tối đa:} 100, kết hợp Early Stopping (patience=15).
    \item \textbf{Optimizer:} AdamW với weight decay $10^{-4}$.
    \item \textbf{Learning Rate:} Khởi điểm $10^{-4}$, bộ lập lịch \texttt{ReduceLROnPlateau} (giảm 0.5× sau 5 epoch không cải thiện Dice xác thực).
\end{itemize}

% ============================================================
\section{Đánh giá hiệu năng kiến trúc PGA-UNet}
\label{sec:danh_gia_hieu_nang}
% ============================================================

Để đo lường khách quan năng lực của kiến trúc PGA-UNet, quá trình thực nghiệm được tiến hành qua ba bước: (1)~so sánh với mô hình cơ sở không có prompt (U-Net, Attention U-Net); (2)~đánh giá tính bền bỉ trước sai số câu nhắc; (3)~so sánh với SAM-Med2D (SOTA prompt-based). Toàn bộ số liệu được thống kê trên tập kiểm thử \Ntest~mẫu per-polygon độc lập.

% ------------------------------------------------------------
\subsection{Đánh giá cơ sở (Baseline Comparison): U-Net vs.\ Attention U-Net vs.\ PGA-UNet}
% ------------------------------------------------------------

Trong kịch bản đánh giá đầu tiên, PGA-UNet (kịch bản câu nhắc Zoom-out lý tưởng) được so sánh với U-Net truyền thống và Attention U-Net hoạt động hoàn toàn tự động.

\textbf{Điều kiện so sánh:}
U-Net và Attention U-Net hoạt động theo cơ chế \textit{tự động hoàn toàn} (chỉ nhận ảnh thô), trong khi PGA-UNet hoạt động \textit{tương tác} (nhận thêm bounding box prompt). Sự chênh lệch hiệu năng phản ánh giá trị của việc tích hợp tri thức chuyên gia vào quá trình phân đoạn. Đây là so sánh có chủ đích: minh chứng lợi ích của hướng tiếp cận prompt-guided so với segmentation tự động trên ảnh X-quang xương.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng phân đoạn: U-Net, Attention U-Net (tự động, N=\Ntestimg~ảnh) so với PGA-UNet kịch bản Zoom-out lý tưởng (prompt-guided, N=\Ntest~mẫu per-polygon)}
    \label{tab:baseline_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        U-Net & 0.4534 & 0.3671 & 0.6320 & 0.4539 & 128.61 & 0.5968 \\
        \hline
        Attention U-Net & 0.4159 & 0.3306 & 0.5951 & 0.4204 & 132.86 & 0.5569 \\
        \hline
        \textbf{PGA-UNet (Zoom-out)} & \textbf{0.8524} & \textbf{0.7527} & \textbf{0.8505} & \textbf{0.8739} & \textbf{13.96} & \textbf{0.9451} \\
        \hline
    \end{tabular}%
    }
\end{table}

\subsection{Đánh giá tính bền bỉ (Robustness): Kịch bản Zoom-out, Shift và Mixed}

Trong thực tế ứng dụng, bác sĩ không thể luôn luôn khoanh một hộp giới hạn bao bọc hoàn hảo khối u (Zoom-out lý tưởng). Sẽ có những lúc mỏi mắt hoặc do góc nhìn chủ quan khiến khung khoanh bị lệch đi một phần so với vị trí trung tâm thực sự. Để kiểm chứng xem PGA-UNet có bị phụ thuộc quá mức vào vị trí tâm câu nhắc hay không, hệ thống được thử nghiệm trên 3 kịch bản:
\begin{enumerate}
    \item \textbf{100\% Zoom-out:} Câu nhắc lý tưởng, bao trọn vẹn tổn thương, cận trên hiệu năng của mô hình.
    \item \textbf{100\% Shift (Lệch tâm):} Câu nhắc bị cố tình dịch chuyển lệch khỏi tâm tổn thương một lượng ngẫu nhiên, điều kiện xấu nhất thực tế, mô phỏng bác sĩ mất tập trung.
    \item \textbf{Mixed (70\% Zoom-out - 30\% Shift):} Pha trộn theo tỷ lệ ước lượng trong lâm sàng thực tế, phần lớn câu nhắc đúng, một phần nhỏ bị lệch.
\end{enumerate}

\textbf{Lý do chọn 3 kịch bản này và không dùng nhiễu mạnh hơn:} Ba mức độ bao phủ toàn bộ phổ sai số câu nhắc hiện thực trong lâm sàng từ hoàn hảo đến lệch tâm. Các hình thức nhiễu mạnh hơn (hộp giới hạn đặt hoàn toàn ngẫu nhiên trên ảnh, không liên quan đến vùng tổn thương) không được đưa vào vì chúng không phản ánh quy trình thực tế, bác sĩ luôn có thông tin sơ bộ về vùng nghi ngờ trước khi vẽ hộp; sai số trong lâm sàng thường là lệch vị trí, không phải sai hoàn toàn. Kịch bản Shift 100\% đã đại diện cho mức nhiễu cực đoan nhất khả thi trong thực tế và cho thấy PGA-UNet vẫn duy trì Dice = 0.8382, đủ để kết luận về tính bền bỉ.


\begin{table}[htbp]
    \centering
    \caption{Đánh giá tính bền bỉ của PGA-UNet trên 3 kịch bản prompt (N=\Ntest~mẫu per-polygon)}
    \label{tab:robustness_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản prompt} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        Zoom-out (100\%, lý tưởng) & 0.8524 & 0.7527 & 0.8505 & 0.8739 & 13.96 & 0.9451 \\
        \hline
        Shift (100\%, lệch tâm) & 0.8382 & 0.7336 & 0.8434 & 0.8556 & 13.57 & 0.9379 \\
        \hline
        Mixed (70\% Zoom-out + 30\% Shift) & 0.8496 & 0.7486 & 0.8511 & 0.8689 & 14.38 & 0.9436 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích tính bền bỉ:}
Số liệu thực nghiệm (Bảng~\ref{tab:robustness_comparison}) khẳng định đặc tính "hướng dẫn mềm" (soft guidance) của kiến trúc PGA-UNet. Khi đối mặt với kịch bản câu nhắc lệch tâm hoàn toàn (100\% Shift), hiệu năng phân đoạn chỉ giảm từ Dice = 0.8524 (Zoom-out) xuống Dice = 0.8382 (Shift), chênh lệch 0.0142, không đáng kể. Kịch bản Mixed (70\% Zoom-out + 30\% Shift) đạt Dice = 0.8496, xác nhận mô hình duy trì hiệu năng ổn định trong môi trường thực tế.

Điều này chứng minh bằng thực nghiệm toán học rằng: mô hình \textbf{không} ghi nhớ một cách mù quáng và cắt hình theo tâm của bản đồ nhiệt câu nhắc. Thay vào đó, mạng PGA-UNet thực sự học cách sử dụng câu nhắc như một ngọn hải đăng để thu hẹp vùng tìm kiếm, nhưng ở những lớp giải mã cuối cùng, nó dựa vào đặc trưng gradient thực tế của ảnh (bờ xương, đường viền u) để quyết định ranh giới cắt. Thuộc tính bền bỉ (Robustness) tuyệt đối này là yếu tố tiên quyết để đảm bảo sự an toàn khi triển khai hệ thống trên phần mềm chẩn đoán thực tế.

\subsection{So sánh với SAM-Med2D (SOTA Prompt-based)}
\label{subsec:sam_comparison}

Để đánh giá đầy đủ hơn, mô hình PGA-UNet được so sánh trực tiếp với SAM-Med2D~\cite{2023-SAMMed2D-Cheng}, mô hình SOTA prompt-based dựa trên kiến trúc ViT-B ($\sim$91M tham số~\cite{2023-SAMMed2D-Cheng}, fine-tuned từ 4.6M ảnh y tế 2D). Cả hai mô hình được đánh giá trên cùng \Ntest~mẫu kiểm thử per-polygon, cùng 3 kịch bản prompt. SAM-Med2D sử dụng độ phân giải ảnh $256\times256$ (bắt buộc do position embedding cố định), trong khi PGA-UNet dùng $512\times512$.

\begin{table}[htbp]
    \centering
    \caption{So sánh toàn diện PGA-UNet với SAM-Med2D fine-tuned và SAM-Med2D zero-shot trên 3 kịch bản prompt (N=\Ntest~mẫu per-polygon). HD95* chuẩn hóa theo kích thước ảnh: PGA÷512, SAM÷256.}
    \label{tab:sam_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Pre↑} & \textbf{Rec↑} & \textbf{HD95*(norm)↓} & \textbf{CBL↑} \\
        \hline
        PGA-UNet & Zoom-out & \textbf{0.8524} & \textbf{0.7527} & \textbf{0.8505} & \textbf{0.8739} & \textbf{0.027} & \textbf{0.9451} \\
        PGA-UNet & Shift & \textbf{0.8382} & \textbf{0.7336} & \textbf{0.8434} & \textbf{0.8556} & \textbf{0.027} & \textbf{0.9379} \\
        PGA-UNet & Mixed & \textbf{0.8496} & \textbf{0.7486} & \textbf{0.8511} & \textbf{0.8689} & \textbf{0.028} & \textbf{0.9436} \\
        \hline
        SAM-Med2D (FT) & Zoom-out & 0.7350 & 0.6130 & 0.7513 & 0.7478 & 0.207 & 0.8893 \\
        SAM-Med2D (FT) & Shift & 0.7097 & 0.5818 & 0.7385 & 0.7117 & 0.213 & 0.8767 \\
        SAM-Med2D (FT) & Mixed & 0.7283 & 0.6044 & 0.7491 & 0.7379 & 0.208 & 0.8863 \\
        \hline
        SAM-Med2D (ZS) & Zoom-out & 0.5337 & 0.3924 & 0.4743 & 0.6776 & 0.374 & 0.8194 \\
        SAM-Med2D (ZS) & Shift & 0.5184 & 0.3775 & 0.4570 & 0.6559 & 0.403 & 0.7945 \\
        SAM-Med2D (ZS) & Mixed & 0.5286 & 0.3882 & 0.4700 & 0.6670 & 0.384 & 0.8093 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích kết quả so sánh PGA vs SAM-Med2D:}
\begin{itemize}
    \item \textbf{PGA vượt trội nhất quán:} Trên kịch bản Zoom-out lý tưởng, PGA-UNet đạt Dice = 0.8524, vượt SAM-Med2D fine-tuned (Dice = 0.7350) và SAM zero-shot (Dice = 0.5337). Sự chênh lệch lớn giữa SAM fine-tuned và zero-shot cho thấy fine-tuning trên dữ liệu miền đặc thù là cần thiết, và PGA vượt qua cả SAM đã được fine-tuned dù chỉ với $\sim$3M tham số. Điều này đặc biệt có ý nghĩa vì PGA-UNet được huấn luyện \textbf{hoàn toàn từ đầu} trên BTXRD (toàn bộ $\sim$3M tham số tham gia backpropagation, không đóng băng bất kỳ thành phần nào), trong khi SAM-Med2D kế thừa trọng số ViT-B đã học từ 4.6M ảnh y tế 2D, xem Bảng~\ref{tab:training_strategy_compare} tại Chương~\ref{Chapter3} để biết chi tiết.
    \item \textbf{Lợi thế HD95 tuyệt đối:} Sau chuẩn hóa theo kích thước ảnh, PGA có HD95/imgsize thấp hơn SAM đáng kể, PGA bám biên chính xác hơn nhiều lần.
    \item \textbf{Tham số hiệu quả:} PGA ($\sim$3M, toàn bộ trainable) vượt SAM-Med2D ($\sim$91M tổng, chỉ $\sim$15M trainable gồm Adapter + Decoder), chứng minh kiến trúc nhẹ được thiết kế đặc thù cho bài toán phân đoạn có prompt trên X-quang xương hiệu quả hơn foundation model đa mục đích.
    \item \textbf{SAM hội tụ rất nhanh:} SAM-Med2D hội tụ sớm nhờ lợi thế pretrained từ 4.6M ảnh y tế 2D, nhưng kiến trúc ViT-B với resolution 256 là giới hạn vốn có trên ảnh X-quang chi tiết.
\end{itemize}

% ------------------------------------------------------------
\subsection{So sánh hiệu quả tính toán}
\label{subsec:efficiency}
% ------------------------------------------------------------

Ngoài hiệu năng phân đoạn, tính khả thi triển khai trong môi trường lâm sàng đòi hỏi mô hình phải đủ nhẹ để chạy trên phần cứng y tế tuyến cơ sở. Bảng~\ref{tab:efficiency} so sánh toàn diện giữa PGA-UNet và SAM-Med2D về các chỉ số tính toán.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu quả tính toán: PGA-UNet vs SAM-Med2D}
    \label{tab:efficiency}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Chỉ số} & \textbf{PGA-UNet (đề xuất)} & \textbf{SAM-Med2D} \\
        \hline
        Tổng tham số & $\sim$3M & $\sim$91M (ViT-B) + Adapter \\
        \hline
        Tham số huấn luyện được & $\sim$3M (100\%) & $\sim$15M (Adapter + Decoder) \\
        \hline
        Độ phân giải ảnh & 512$\times$512 & 256$\times$256 (cố định) \\
        \hline
        VRAM huấn luyện (batch=4) & $\sim$3 GB & $\sim$6 GB \\
        \hline
        VRAM suy luận (batch=1) & $\sim$1 GB & $\sim$2 GB \\
        \hline
        Có thể chạy trên GPU 4GB & \checkmark & Hạn chế \\
        \hline
    \end{tabular}%
    }
\end{table}

% ============================================================
\section{Phân tích kiến trúc và khả năng tổng quát hóa}
\label{sec:ablation_study}
% ============================================================

Mục này trình bày hai nhóm thực nghiệm bổ sung: (1)~ablation study so sánh các cấu hình kiến trúc PSG/CAD; (2)~kiểm định độ ổn định bằng đánh giá chéo 4-fold.

% ------------------------------------------------------------
\subsection{Ablation kiến trúc: Đóng góp của PSG và CAD}
\label{subsec:ablation_arch}
% ------------------------------------------------------------

Năm biến thể kiến trúc được huấn luyện lại hoàn toàn từ đầu trên cùng tập BTXRD, chỉ thay đổi cấu hình PSG/CAD và loại câu nhắc:
\begin{itemize}
    \item \textbf{V1:} No PSG, No CAD (chỉ concatenate heatmap).
    \item \textbf{V2:} PSG only (không có CAD).
    \item \textbf{V3:} CAD only (không có PSG).
    \item \textbf{V4:} PSG + CAD với Binary bbox làm câu nhắc.
    \item \textbf{V5 (đề xuất):} PSG + CAD với Gaussian Heatmap làm câu nhắc.
\end{itemize}
Thiết kế năm biến thể này cho phép cô lập đóng góp riêng lẻ của PSG (V2 so V1), CAD (V3 so V1), tổ hợp cả hai (V4 so V2/V3) và ảnh hưởng của loại câu nhắc Gaussian vs Binary (V5 so V4). Kết quả được trình bày trong Bảng~\ref{tab:ablation_arch}.

\begin{table}[htbp]
    \centering
    \caption{Ablation study: Dice Score trên 3 kịch bản prompt của 5 biến thể kiến trúc PGA-UNet (N=\Ntest~mẫu per-polygon). V5 là mô hình đề xuất.}
    \label{tab:ablation_arch}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Biến thể} & \textbf{PSG} & \textbf{CAD} & \textbf{Prompt} & \textbf{Zoom-out↑} & \textbf{Shift↑} & \textbf{Mixed↑} \\
        \hline
        V1: Baseline (concat heatmap) & \texttimes & \texttimes & Gaussian & 0.8718 & 0.7201 & 0.8158 \\
        \hline
        V2: PSG only & \checkmark & \texttimes & Gaussian & 0.8643 & 0.7291 & 0.8146 \\
        \hline
        V3: CAD only & \texttimes & \checkmark & Gaussian & 0.8827 & 0.7335 & 0.8256 \\
        \hline
        V4: Full PGA + Binary prompt & \checkmark & \checkmark & Binary & 0.8800 & 0.7378 & 0.8276 \\
        \hline
        \textbf{V5: Full PGA + Gaussian (đề xuất)} & \checkmark & \checkmark & Gaussian & 0.8524 & \textbf{0.8382} & \textbf{0.8496} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích ablation:} Hai quan sát chính từ Bảng~\ref{tab:ablation_arch}:

\textbf{(1) Đóng góp của PSG và CAD:} V3 (CAD only, Zoom=0.8827) cao hơn V2 (PSG only, Zoom=0.8643) cho thấy CAD đóng góp nhiều hơn vào hiệu năng Zoom-out; song cả hai đều thua V5 trong kịch bản Shift (V2: 0.7291, V3: 0.7335 so với V5: 0.8382), xác nhận kiến trúc đầy đủ PSG+CAD là cần thiết.

\textbf{(2) Trade-off Gaussian vs Binary prompt (V5 vs V4):} Đây là phát hiện quan trọng nhất. Khi chuyển từ Binary bbox sang Gaussian heatmap, Dice Shift tăng mạnh +0.1004 (0.7378 → 0.8382), Dice Mixed tăng +0.0220 (0.8276 → 0.8496), trong khi Dice Zoom-out giảm nhẹ -0.0276 (0.8800 → 0.8524). \textbf{Trade-off này là có chủ đích:} Gaussian heatmap "làm mờ" đường biên tín hiệu câu nhắc, buộc mô hình không thể ghi nhớ cứng nhắc vị trí tâm bounding box. Hệ quả là robustness cải thiện vượt trội khi câu nhắc bị lệch, trong khi hiệu năng với câu nhắc lý tưởng chỉ suy giảm không đáng kể. Trong môi trường lâm sàng thực tế, khả năng chịu đựng sai số câu nhắc quan trọng hơn điểm Dice tuyệt đối với câu nhắc hoàn hảo, V5 tối ưu cho đúng mục tiêu này.

% ------------------------------------------------------------
\subsection{Kiểm định độ ổn định bằng đánh giá chéo (Cross-validation)}
\label{subsec:cross_validation}
% ------------------------------------------------------------

Để xác minh tính ổn định và khả năng tổng quát hóa của PGA-UNet không phụ thuộc vào một lần phân chia dữ liệu duy nhất, mô hình được đánh giá chéo trên 4 phân vùng dữ liệu khác nhau. Kết quả trình bày tại Bảng~\ref{tab:cross_validation}.

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá chéo 4-fold PGA-UNet trên 3 kịch bản prompt (trung bình 4 fold)}
    \label{tab:cross_validation}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        Zoom-out & 0.8784 & 0.7897 & 0.8583 & 0.9107 & 10.29 & 0.9601 \\
        \hline
        Shift & 0.8522 & 0.7522 & 0.8440 & 0.8769 & 12.60 & 0.9420 \\
        \hline
        Mixed (70\% Zoom-out + 30\% Shift) & 0.8723 & 0.7807 & 0.8571 & 0.9006 & 10.91 & 0.9552 \\
        \hline
    \end{tabular}%
    }
\end{table}

\begin{table}[htbp]
    \centering
    \caption{Dice Score từng fold trong đánh giá chéo 4-fold PGA-UNet (kịch bản Zoom-out / Shift / Mixed)}
    \label{tab:cross_validation_per_fold}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|c|c|c|c|}
        \hline
        \textbf{Fold} & \textbf{Zoom-out↑} & \textbf{Shift↑} & \textbf{Mixed↑} \\
        \hline
        Fold 1 & 0.8769 & 0.8422 & 0.8686 \\
        \hline
        Fold 2 & 0.8805 & 0.8628 & 0.8753 \\
        \hline
        Fold 3 & 0.8768 & 0.8456 & 0.8700 \\
        \hline
        Fold 4 & 0.8795 & 0.8583 & 0.8752 \\
        \hline
        \textbf{Trung bình ± Std} & $\mathbf{0.8784 \pm 0.0019}$ & $\mathbf{0.8522 \pm 0.0099}$ & $\mathbf{0.8723 \pm 0.0035}$ \\
        \hline
    \end{tabular}
\end{table}

Bảng~\ref{tab:cross_validation_per_fold} cho thấy Dice Score dao động rất nhỏ giữa các fold: độ lệch chuẩn thấp nhất ở kịch bản Zoom-out ($\pm0.0019$) và lớn nhất ở Shift ($\pm0.0099$), biên độ hoàn toàn chấp nhận được trong nghiên cứu y tế. Kết quả này xác nhận mô hình hội tụ ổn định trên các phân vùng dữ liệu khác nhau, không bị phụ thuộc vào một cách chia cụ thể. Các chỉ số cross-validation nhỉnh hơn tập test chính một chút (Dice Zoom-out: 0.8784 vs 0.8524) phản ánh sự khác biệt tự nhiên giữa dữ liệu huấn luyện và tập kiểm thử độc lập được giữ lại hoàn toàn từ đầu.

% ------------------------------------------------------------
\subsection{Đánh giá theo đặc tính tổn thương (Sub-Category)}
\label{subsec:subcategory}
% ------------------------------------------------------------

Để đánh giá sâu hơn khả năng tổng quát hóa của PGA-UNet, hai phân tích sub-category được thực hiện: \textbf{(1)}~so sánh PGA-UNet với U-Net/Att-UNet trên nhóm \textit{Dễ} (top-50 U-Net Dice) và \textit{Khó} (bottom-50 U-Net Dice), trên $N=\Ntestimg$~ảnh (50 ảnh mỗi nhóm); \textbf{(2)}~so sánh PGA-UNet với SAM-Med2D trên ba nhóm đặc tính lâm sàng (Tổn thương nhỏ, Biên giới mờ, Tổn thương rõ nét), mỗi nhóm 50 mẫu per-polygon.

\subsubsection*{PGA-UNet so với U-Net/Att-UNet theo độ khó tác vụ}

Ở nhóm \textit{Dễ} (tương phản cao, hình dạng rõ), U-Net vẫn hoạt động được; PGA kỳ vọng cải thiện thêm đáng kể. Ở nhóm \textit{Khó} (tương phản thấp, xương chồng lấp, hình dạng bất thường), U-Net và Att-UNet thiếu thông tin định hướng nên dự đoán suy giảm mạnh, trong khi PGA-UNet nhờ cơ chế prompt guidance kỳ vọng duy trì Dice ổn định hơn, đây là lý do cốt lõi cho sự tồn tại của hướng tiếp cận tương tác.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng PGA-UNet với U-Net và Att-UNet theo độ khó tác vụ ($N=\Ntestimg$~ảnh, phân nhóm 50 dễ nhất / 50 khó nhất theo Dice của U-Net; PGA tính trên polygon)}
    \label{tab:subcat_baseline}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{3}{*}{Dễ (top-50)}
            & U-Net       & 0.8691 & 0.7713 & 0.8711 & 0.8771 & 21.05  & 0.9510 \\
            & Att-UNet    & 0.7609 & 0.6539 & 0.8055 & 0.7797 & 34.48  & 0.8662 \\
            & PGA-UNet    & \textbf{0.8938} & \textbf{0.8122} & \textbf{0.8704} & \textbf{0.9249} & \textbf{13.16} & \textbf{0.9588} \\
        \hline
        \multirow{3}{*}{Khó (bottom-50)}
            & U-Net       & 0.0000 & 0.0000 & 0.2200 & 0.0000 & 265.69 & 0.0243 \\
            & Att-UNet    & 0.0929 & 0.0639 & 0.3397 & 0.1034 & 236.46 & 0.1551 \\
            & PGA-UNet    & \textbf{0.8181} & \textbf{0.7114} & \textbf{0.8446} & \textbf{0.8274} & \textbf{18.06} & \textbf{0.9247} \\
        \hline
    \end{tabular}%
    }
\end{table}

Kết quả Bảng~\ref{tab:subcat_baseline} xác nhận rõ ràng giả thuyết lý thuyết. Ở nhóm \textit{Dễ} (top-50), U-Net đạt Dice~=~0.8691 trong khi PGA-UNet đạt 0.8938 ($\Delta=+0.0247$), cải thiện có ý nghĩa nhưng không đột biến vì tổn thương đã rõ. Ở nhóm \textit{Khó} (bottom-50), U-Net sụp đổ hoàn toàn xuống Dice~=~0.0000 (HD95~=~265.69~px), Att-UNet cũng chỉ đạt 0.0929; trong khi đó PGA-UNet \textbf{duy trì ổn định ở 0.8181} ($\Delta=+0.8181$ so với U-Net). Điều này chứng minh rằng cơ chế prompt spatial gate là điều kiện cần thiết cho khả năng hoạt động nhất quán trên các ca bệnh phức tạp.


\subsubsection*{PGA-UNet so với SAM-Med2D theo đặc tính lâm sàng}

Ba quan sát chính dự kiến: (1)~\textbf{Tổn thương nhỏ}: SAM-Med2D hoạt động ở độ phân giải 256$\times$256 khiến các tổn thương nhỏ chỉ còn vài pixel, SAM bỏ sót phần lớn diện tích, trong khi PGA ở 512$\times$512 với heatmap dẫn đường đạt kết quả tốt hơn đáng kể. (2)~\textbf{Biên giới mờ}: ranh giới tổn thương không rõ ràng trên X-quang xương đặc thù BTXRD, SAM pretrain đa dạng nhưng không chuyên biệt hóa đặc trưng gradient X-quang xương; PGA train from scratch trên BTXRD học được các đặc trưng cụ thể này. (3)~\textbf{Tổn thương rõ nét}: khi tổn thương lớn và ranh giới rõ, cả hai mô hình đều hoạt động tốt; PGA nhỉnh hơn SAM với $\sim$30$\times$ ít tham số hơn.

\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet với SAM-Med2D trên ba nhóm đặc tính lâm sàng (kịch bản Zoom-out, 50 mẫu per-polygon mỗi nhóm)}
    \label{tab:subcat_sam}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{2}{*}{Tổn thương nhỏ}
            & SAM-Med2D  & 0.3887 & 0.2835 & 0.7223 & 0.3335 & 88.73 & 0.6855 \\
            & PGA-UNet   & \textbf{0.7970} & \textbf{0.6818} & \textbf{0.7906} & \textbf{0.8453} & \textbf{13.64} & \textbf{0.9202} \\
        \hline
        \multirow{2}{*}{Biên giới mờ}
            & SAM-Med2D  & 0.5407 & 0.3886 & 0.7673 & 0.4610 & 27.44 & 0.8401 \\
            & PGA-UNet   & \textbf{0.8288} & \textbf{0.7152} & \textbf{0.8382} & \textbf{0.8413} & \textbf{12.00} & \textbf{0.9384} \\
        \hline
        \multirow{2}{*}{Tổn thương rõ nét}
            & SAM-Med2D  & 0.8465 & 0.7392 & 0.8955 & 0.8129 & 27.04 & 0.9483 \\
            & PGA-UNet   & \textbf{0.8741} & \textbf{0.7850} & \textbf{0.8638} & \textbf{0.8969} & \textbf{24.71} & \textbf{0.9548} \\
        \hline
    \end{tabular}%
    }
\end{table}

Kết quả Bảng~\ref{tab:subcat_sam} xác nhận cả ba nhận định. \textbf{Tổn thương nhỏ}: PGA đạt Dice~=~0.7970 trong khi SAM chỉ đạt 0.3887 ($\Delta=+0.4083$), khoảng cách lớn nhất trong ba nhóm, phản ánh tác động trực tiếp của độ phân giải 512$\times$512 so với 256$\times$256 đối với vùng nhỏ. \textbf{Biên giới mờ}: PGA~=~0.8288 vs SAM~=~0.5407 ($\Delta=+0.2882$), cho thấy đặc trưng X-quang xương chuyên biệt mà PGA học được trên BTXRD. \textbf{Tổn thương rõ nét}: khoảng cách thu hẹp đáng kể (PGA~=~0.8741, SAM~=~0.8465, $\Delta=+0.0276$), cả hai mô hình hoạt động tốt khi điều kiện thuận lợi; PGA vẫn nhỉnh hơn với kiến trúc nhỏ gọn hơn $\sim$30$\times$.


% ------------------------------------------------------------
\section{Trực quan hóa kết quả (Qualitative Results)}
\label{sec:truc_quan_hoa}
% ------------------------------------------------------------

Bên cạnh các đánh giá định lượng bằng số liệu thống kê, việc quan sát trực tiếp kết quả phân đoạn mang ý nghĩa đặc biệt quan trọng trong lĩnh vực y khoa lâm sàng. Mục này trình bày các hình ảnh trực quan hóa nhằm minh họa rõ nét cách mô hình PGA-UNet xử lý các trường hợp phức tạp trong thực tế.

\FloatBarrier
\subsection{Trực quan hóa các kịch bản phân đoạn tiêu biểu}

Hình~\ref{fig:vis_pga_sample} minh họa một ca kiểm thử tiêu biểu của PGA-UNet (kịch bản Zoom-out): mặt nạ phân đoạn bám sát đường biên tổn thương nhờ cơ chế Prompt Spatial Gate và Conditioned Attention dẫn đường từ heatmap phân lớp.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_01.png}
    \caption{Trực quan hóa kết quả phân đoạn của PGA-UNet kịch bản Zoom-out: ảnh gốc (trái), mặt nạ Ground Truth (giữa), mặt nạ dự đoán (phải). Đường biên bám sát GT nhờ cơ chế Prompt Spatial Gate và Conditioned Attention.}
    \label{fig:vis_pga_sample}
\end{figure}


% ------------------------------------------------------------
\section{Giới hạn kiến trúc và phân tích trường hợp thất bại}
\label{sec:failure_cases}
% ------------------------------------------------------------

Bên cạnh các kết quả tích cực đã trình bày, việc phân tích khách quan các trường hợp kiến trúc PGA-UNet hoạt động kém là cần thiết để đánh giá đúng giới hạn và định hướng cải tiến.

\FloatBarrier
\subsection{Trường hợp PGA-UNet dự đoán kém}

Qua quá trình kiểm tra trên tập test, nhóm nhận diện được ba nhóm trường hợp mà PGA-UNet cho kết quả phân đoạn dưới mức trung bình (Dice $< 0.70$):

\begin{itemize}
    \item \textbf{Tổn thương có hình dáng bất thường:} Các khối u dạng kéo dài, phân nhánh hoặc có đường biên không liên tục thường vượt quá khả năng biểu diễn của bản đồ nhiệt Gaussian (vốn có dạng hình elip đối xứng). Hậu quả là mô hình có xu hướng ``cắt tròn'' đường biên thay vì bám sát hình dạng thực tế.
    \item \textbf{Vùng chồng lấp đa lớp xương dày đặc:} Tại các vị trí như khớp cổ chân nơi nhiều cấu trúc xương chồng lên nhau, gradient cạnh bị nhiễu nghiêm trọng khiến bộ giải mã gặp khó khăn trong việc xác định ranh giới chính xác, ngay cả khi có prompt hướng dẫn.
    \item \textbf{Ảnh chất lượng thấp:} Các ảnh X-quang bị mờ, thiếu sáng hoặc có nhiễu kỹ thuật còn sót lại sau tiền xử lý đều làm suy giảm chất lượng đặc trưng mà bộ mã hóa trích xuất được.
\end{itemize}

% ============================================================
% PHẦN II, Module Gatekeeper và Pipeline End-to-End
% ============================================================

\section{Đánh giá pipeline end-to-end với module Gatekeeper hỗ trợ}
\label{sec:product_overview}

Phần này trình bày đánh giá module Gatekeeper (EfficientNet\_B3), thành phần hỗ trợ thực nghiệm được tích hợp để lấp đầy khoảng trống giữa điều kiện huấn luyện và triển khai thực tế, và đánh giá pipeline end-to-end tự động trên dữ liệu hỗn hợp thực tế. Phân tích sai số tích lũy của pipeline được trình bày tại Mục~\ref{subsec:cascading_error}.

% ------------------------------------------------------------
\section{Đánh giá mô hình phân lớp gác cổng}
\label{sec:danh_gia_phan_lop}
% ------------------------------------------------------------
% ------------------------------------------------------------

Mô hình phân lớp đóng vai trò là "người gác cổng" (gatekeeper) trong luồng xử lý đầu cuối của phần mềm. Nhiệm vụ của mô hình là sàng lọc tự động ảnh X-quang đầu vào thành hai nhóm: Bình thường (Âm tính) và Có bệnh lý bất thường (Dương tính). Việc đánh giá chính xác hiệu năng của module này mang ý nghĩa quyết định, bởi một dự đoán sai sót ở bước này có thể khiến bệnh nhân bị lọt lưới sàng lọc mà không bao giờ đi tới được bước phân đoạn chi tiết phía sau.

\textbf{Kiến trúc: EfficientNet\_B3 tinh chỉnh hai giai đoạn.}
Mô hình \textbf{EfficientNet\_B3}~\cite{tan2019efficientnet} được tiền huấn luyện trên ImageNet-1K sử dụng chiến lược \textit{compound scaling}, đồng thời mở rộng chiều rộng, chiều sâu và độ phân giải theo một hệ số hợp lý. Backbone gồm các khối MBConv kết hợp với cơ chế Squeeze-and-Excitation (SE). Với $\sim$12M tham số và đầu vào $300\times300$, EfficientNet\_B3 đạt cân bằng tốt giữa hiệu năng phân lớp và tốc độ suy luận. Tầng phân lớp gốc được thay thế bởi $\text{Linear}(1536, 1)$ + Sigmoid, xuất xác suất $p \in (0,1)$; ngưỡng $p \geq 0.5$ → dương tính.

\textit{Giai đoạn 1 (25 epoch):} Đóng băng backbone, chỉ cập nhật head với $\text{LR}=10^{-4}$, giúp head thích nghi không gian đặc trưng X-quang mà không phá hủy biểu diễn ImageNet.

\textit{Giai đoạn 2 (75 epoch):} Mở khóa toàn bộ, $\text{LR}=10^{-5}$, CosineAnnealingLR. Hàm mất mát: BCEWithLogitsLoss; tối ưu: AdamW; dừng sớm: patience = 15; Gradient Clipping: max\_norm = 1.0.

\textbf{Các độ đo đánh giá (Evaluation Metrics):}
Để có cái nhìn toàn diện, mô hình được đánh giá qua các độ đo tiêu chuẩn của bài toán phân loại nhị phân: Độ chính xác tổng thể (Accuracy), Độ chính xác (Precision), Độ bao phủ/Độ nhạy (Recall/Sensitivity), Độ đặc hiệu (Specificity) và Điểm F1 (F1-Score). 

Đặc biệt trong bối cảnh y tế dự phòng và sàng lọc, \textbf{Độ nhạy (Recall)} là chỉ số được ưu tiên hàng đầu. Một mô hình sàng lọc tốt thà báo động nhầm (False Positive) để bác sĩ tốn thêm chút thời gian kiểm tra lại ở bước phân đoạn, còn hơn là bỏ sót bệnh nhân thực sự có khối u (False Negative).

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá hiệu năng của mô hình phân lớp sàng lọc trên tập kiểm thử}
    \label{tab:classification_results}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Độ đo đánh giá (Metrics)} & \textbf{Kết quả} \\
        \hline
        Độ chính xác tổng thể (Accuracy) & \textbf{85.60\%} \\
        \hline
        Độ chính xác (Precision / PPV) & \textbf{91.30\%} \\
        \hline
        Độ nhạy (Recall / Sensitivity) & \textbf{78.61\%} \\
        \hline
        Độ đặc hiệu (Specificity) & \textbf{92.55\%} \\
        \hline
        Giá trị dự báo Âm tính (NPV) & \textbf{81.31\%} \\
        \hline
        Điểm F1 (F1-Score) & \textbf{84.52\%} \\
        \hline
        Diện tích dưới đường cong (AUC-ROC) & \textbf{0.9258} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Phân tích kết quả:}
Bảng~\ref{tab:classification_results} tổng hợp hiệu năng của mô hình phân lớp gác cổng sau quá trình tinh chỉnh hai giai đoạn trên BTXRD. Các chỉ số chứng minh module Gatekeeper hoạt động ổn định và đáng tin cậy trong bối cảnh y tế:
\begin{itemize}
    \item \textbf{AUC-ROC 0.9258:} Diện tích dưới đường cong ROC đạt 0.926 phản ánh khả năng phân biệt bệnh/không bệnh tốt, bất kể ngưỡng phân loại.
    \item \textbf{Precision 91.30\%:} Khi mô hình báo dương tính, xác suất đúng là 91.3\%, hạn chế tối đa việc chuyển ảnh lành sang module phân đoạn.
    \item \textbf{Recall 78.61\%:} Mô hình bắt được $\approx$79\% ca bệnh thực sự. Đây là điểm cần cải thiện trong tương lai.
    \item \textbf{Specificity 92.55\%:} Loại đúng $\approx$93\% ca bình thường, tiết kiệm đáng kể tài nguyên tính toán và thời gian bác sĩ.
    \item \textbf{NPV 81.31\%:} Khi hệ thống kết luận "bình thường", xác suất không có bệnh là 81.3\%, đủ tin cậy làm bộ lọc sơ cấp tự động.
\end{itemize}

\textit{Lưu ý về tập dữ liệu:} Bảng~\ref{tab:classification_results} được đánh giá trên tập kiểm thử phân hoạch ngẫu nhiên từ BTXRD (tỉ lệ 80/10/10), khác với tập \textbf{dataset\_online} dùng trong đánh giá pipeline (Mục~\ref{sec:pipeline_eval}). Dataset\_online được lắp ghép từ tập kiểm thử PGA-UNet (187 ảnh bệnh lý có chú thích đa giác) và tập kiểm thử EfficientNet (188 ảnh bình thường) nhằm mô phỏng kịch bản lâm sàng end-to-end. Sự khác biệt về thành phần hai tập này giải thích tại sao Sensitivity trên dataset\_online (96.79\%) cao hơn Recall trên tập BTXRD độc lập (78.61\%).

\subsection{Phân tích sai số tích lũy của pipeline (Cascading Error)}
\label{subsec:cascading_error}

Khi đánh giá \textbf{hệ thống end-to-end} trên tập \textbf{dataset\_online} (375 ảnh hỗn hợp, bao gồm 187 ảnh bệnh lý và 188 ảnh bình thường), sai số từ bước phân lớp lan truyền sang bước phân đoạn qua hai kênh:

\begin{enumerate}
    \item \textbf{Sai số Recall (FN):} Các ca bệnh bị gác cổng bỏ sót → không chuyển xuống PGA-UNet (6 ảnh FN trên dataset\_online).
    \item \textbf{Sai số Specificity (FP):} Các ảnh bình thường bị phân loại nhầm thành dương tính → PGA-UNet chạy nhưng không có GT mask (38 ảnh FP trên dataset\_online), đóng góp Dice = 0 vào mẫu số.
\end{enumerate}

Cận trên lý thuyết của Pipeline Dice (chỉ tính sai số từ Recall):
\begin{equation}
    \begin{aligned}
    \text{Pipeline Dice}_{\text{ước tính}} &\approx
        \text{Sensitivity}_{\text{online}} \times \text{Dice}_{\text{PGA}} \\
    &= 96.79\% \times 85.24\% \approx \mathbf{82.5\%}
    \end{aligned}
\end{equation}

Tuy nhiên, kết quả thực nghiệm đo được Pipeline Dice = \textbf{0.7296}, thấp hơn cận trên 82.5\%. Khoảng cách này giải thích bởi \textbf{38 ảnh FP}: gác cổng phân loại sai 38 ảnh bình thường thành dương tính (Specificity~=~79.79\%), gây ra 38 đơn vị Dice~=~0 trong mẫu số 264 đơn vị, kéo Pipeline Dice từ cận trên xuống 0.7296. Bottleneck thực tế \textbf{không phải Recall} (đã đạt 96.79\%, tốt) mà là \textbf{Specificity}: cứ mỗi ảnh FP giảm bớt, Pipeline Dice tăng lên trực tiếp. Chi tiết xem Mục~\ref{sec:pipeline_eval} và định hướng cải thiện tại Chương~\ref{Chapter5}.


% ------------------------------------------------------------
\section{Đánh giá pipeline end-to-end trên dữ liệu hỗn hợp}
\label{sec:pipeline_eval}
% ------------------------------------------------------------

Để đánh giá pipeline trong điều kiện thực tế, nơi không phải ảnh nào cũng có bệnh lý, hệ thống được chạy trên tập \textbf{dataset\_online} gồm cả ảnh bình thường lẫn ảnh có bệnh.

\textbf{Thiết kế đánh giá:} Mỗi ảnh đi qua toàn bộ pipeline (EfficientNet\_B3 → PGA-UNet). Kết quả chia thành 4 trường hợp: TP (có JSON, dự đoán dương, PGA chạy thực tế), FP (không có JSON, dự đoán dương, Dice = 0 vì không có GT mask), FN (có JSON, dự đoán âm, không qua PGA, không tính), TN (không có JSON, dự đoán âm).

Dice pipeline được tính trên \textit{toàn bộ đơn vị classifier gửi cho PGA} (mẫu số = polygon(TP) + ảnh(FP)), phản ánh hiệu năng tổng thể khi tích hợp cả trường hợp phân loại sai. Mỗi ảnh TP được tách thành các polygon riêng lẻ (khớp với đơn vị đánh giá PGA-UNet độc lập), mỗi ảnh FP đóng góp 1 đơn vị với Dice~=~0:
\begin{equation}
    \text{Pipeline Dice} = \frac{\displaystyle\sum_{i \in \text{polygon(TP)}} \text{Dice}_i}{N_{\text{polygon(TP)}} + N_{\text{image(FP)}}}
\end{equation}

\begin{table}[htbp]
    \centering
    \caption{Kết quả pipeline end-to-end trên tập dataset\_online (hỗn hợp bình thường + có bệnh)}
    \label{tab:pipeline_eval}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|p{7.5cm}|}
        \hline
        \textbf{Thống kê} & \textbf{Giá trị} \\
        \hline
        Tổng ảnh đầu vào & 375 (187 có bệnh, 188 bình thường) \\
        \hline
        TP / FP / FN / TN (ảnh) & 181 / 38 / 6 / 150 \\
        \hline
        \multicolumn{2}{|c|}{\textit{Phân loại (EfficientNet\_B3 trên dataset\_online)}} \\
        \hline
        Sensitivity (Recall) & \textbf{96.79\%} \\
        \hline
        Specificity & \textbf{79.79\%} \\
        \hline
        Accuracy & \textbf{88.27\%} \\
        \hline
        AUC-ROC & \textbf{0.9688} \\
        \hline
        \multicolumn{2}{|c|}{\textit{Phân đoạn pipeline (PGA-UNet, đánh giá per-polygon)}} \\
        \hline
        Ảnh TP đưa vào PGA & 181 ảnh $\rightarrow$ 226 polygon (tb. 1.25 polygon/ảnh) \\
        \hline
        Ảnh FP (Dice = 0) & 38 ảnh \\
        \hline
        Mẫu số (polygon TP + ảnh FP) & 226 + 38 = \textbf{264 đơn vị} \\
        \hline
        Pipeline Dice & \textbf{0.7296} \\
        \hline
        Pipeline IoU & \textbf{0.6430} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Nhận xét:} Pipeline Dice~=~0.7296 (mẫu số 264 đơn vị: 226 polygon từ 181 ảnh TP và 38 ảnh FP bị gán 0) phản ánh hiệu năng tổng thể khi pipeline xử lý luồng ảnh hỗn hợp theo đúng thiết kế per-polygon. Con số này thấp hơn PGA-UNet độc lập (Dice~=~0.8524) do hai nguồn suy giảm: (1) 38 ảnh bình thường bị phân loại sai (FP) kéo điểm xuống; (2) 6 ảnh bệnh bị bỏ sót (FN) không được phân đoạn. Đây là \textbf{kết quả thực nghiệm đầy đủ} trên dữ liệu hỗn hợp thực tế, minh chứng cho sai số tích lũy tất yếu của kiến trúc hai tầng.

Mô hình phân lớp gác cổng đạt AUC-ROC~=~0.9258 (standalone BTXRD) / 0.9688 (dataset\_online) là bản lề để toàn bộ hệ thống vận hành ổn định. Cải thiện Specificity (hiện 79.79\%) nhằm giảm FP là hướng ưu tiên phát triển tiếp theo (xem Chương~\ref{Chapter5}).

% ------------------------------------------------------------
\section*{Ứng dụng tích hợp (app.py)}
% ------------------------------------------------------------

Toàn bộ pipeline (Gatekeeper → PGA-UNet) được đóng gói vào ứng dụng demo \texttt{app.py} sử dụng Gradio, cho phép minh họa trực quan luồng chẩn đoán end-to-end tự động trong buổi thuyết trình. Tối ưu hóa hiệu năng thời gian phản hồi cho triển khai thực tế là định hướng phát triển tiếp theo (xem Chương~\ref{Chapter5}).