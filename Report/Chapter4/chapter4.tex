% ==========================================
% CHƯƠNG 4: THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ
% ==========================================
\chapter{THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ}
\label{Chapter4}

Chương này tổ chức kết quả thực nghiệm theo hai đóng góp tách biệt của khóa luận. \textbf{Đóng góp 1 (Nghiên cứu)} tập trung đánh giá kiến trúc PGA-UNet thuần túy trên tập kiểm thử 248 mẫu độc lập, so sánh với các mô hình cơ sở không có prompt (U-Net, Attention U-Net) và mô hình nền tảng SAM-Med2D, bao gồm phân tích ablation, IPR convergence và đánh giá theo đặc tính tổn thương. \textbf{Đóng góp 2 (Sản phẩm)} trình bày và đánh giá pipeline lâm sàng hoàn chỉnh: cơ chế phòng vệ câu nhắc (GradCAM + IPR rescue), mô hình phân lớp gác cổng MobileNetV4 và ứng dụng tích hợp \texttt{app.py}.

% ============================================================
% PHẦN I — ĐÓNG GÓP 1: Nghiên cứu kiến trúc PGA-UNet
% ============================================================

% ------------------------------------------------------------
\section{Thiết lập môi trường thực nghiệm}
\label{sec:thiet_lap}
% ------------------------------------------------------------

Để đảm bảo tính khách quan và khả năng tái lập (reproducibility) của các kết quả nghiên cứu, mục này trình bày chi tiết về quy mô dữ liệu, phương pháp phân chia các tập đánh giá, cấu hình phần cứng cũng như các siêu tham số (hyperparameters) được thiết lập trong suốt quá trình huấn luyện mô hình.

\subsection{Tập dữ liệu ảnh X-quang (Dataset)}
\label{subsec:dataset}

Dữ liệu thực nghiệm được sử dụng trong khóa luận là bộ \textbf{BTXRD} (\textit{Bone Tumor X-Ray Dataset})~\cite{btxrd2024}, một tập ảnh X-quang xương công khai có đầy đủ ba cấp nhãn: phân lớp, định vị và phân đoạn pixel-level. Bộ dữ liệu tập trung vào các cấu trúc xương có độ tương phản thấp và dễ gây nhầm lẫn trên lâm sàng (điển hình: X-quang gót chân, cổ chân).

\textbf{Nguồn gốc và đặc điểm dữ liệu:} Cần lưu ý rằng các bộ dữ liệu X-quang công khai phổ biến như MURA~\cite{2017-MURA-Rajpurkar} chỉ cung cấp nhãn phân lớp mà không có mặt nạ phân đoạn, do đó không phù hợp trực tiếp cho bài toán phân đoạn có giám sát. BTXRD là lựa chọn phù hợp nhất vì cung cấp đầy đủ nhãn phân đoạn ở cấp pixel. Ảnh X-quang thô chứa nhiều nhiễu kỹ thuật (ký tự R/L, metadata) và đã trải qua quy trình tiền xử lý nghiêm ngặt bằng YOLOv11s như mô tả ở Mục~\ref{sec:tien_xu_ly}.

\textbf{Hai phân vùng dữ liệu song song:} Kiến trúc hệ thống sử dụng hai tập dữ liệu với mục đích khác nhau:
\begin{itemize}
    \item \textbf{Dữ liệu phân đoạn} (cho PGA-UNet và baseline): Chỉ chứa các ảnh \textit{có bệnh lý} kèm mặt nạ phân đoạn pixel-level. Đây là giới hạn tự nhiên của học có giám sát — chỉ ảnh có nhãn mới dùng được.
    \item \textbf{Dữ liệu phân lớp} (cho MobileNetV4 Gatekeeper): Chứa cả ảnh bệnh lý lẫn ảnh bình thường, phân chia theo tỷ lệ 70/15/15 (train/val/test), batch size = 16.
\end{itemize}

\textbf{Thống kê dữ liệu:} Bảng~\ref{tab:dataset_stats} tóm tắt phân bố dữ liệu sau khi phân chia.

\begin{table}[htbp]
    \centering
    \caption{Thống kê phân bố bộ dữ liệu thực nghiệm}
    \label{tab:dataset_stats}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|c|l|}
        \hline
        \textbf{Tập dữ liệu} & \textbf{Số ảnh} & \textbf{Số mẫu (per-polygon)} & \textbf{Ghi chú} \\
        \hline
        Huấn luyện (Train) & $\approx$1\,495 & 1\,859 & Tất cả ca bệnh lý \\
        \hline
        Xác thực (Validation) & $\approx$159 & 211 & \\
        \hline
        Kiểm thử (Test) & 187 & 248 & 28 ảnh có 2 khối u $\rightarrow$ 248 polygon \\
        \hline
        \textbf{Tổng} & $\approx$\textbf{1\,841} & \textbf{2\,318} & 100\% ca bệnh lý \\
        \hline
    \end{tabular}
\end{table}

Nhằm đánh giá chính xác năng lực tổng quát hóa của mô hình, toàn bộ dữ liệu đã được làm sạch sẽ được phân chia ngẫu nhiên thành ba tập con độc lập:
\begin{itemize}
    \item \textbf{Tập huấn luyện (Training set):} Được sử dụng để tối ưu hóa trực tiếp các trọng số của mạng nơ-ron thông qua quá trình lan truyền ngược (backpropagation).
    \item \textbf{Tập xác thực (Validation set):} Được sử dụng ở cuối mỗi vòng lặp (epoch) để tinh chỉnh siêu tham số và theo dõi hiện tượng quá khớp (overfitting), từ đó quyết định điểm dừng sớm (Early Stopping) tối ưu.
    \item \textbf{Tập kiểm thử (Testing set):} Gồm $248$ mẫu ảnh được giữ kín (unseen data) hoàn toàn trong suốt quá trình huấn luyện. Tập này chỉ được sử dụng một lần duy nhất ở pha đánh giá cuối cùng để đo lường khách quan hiệu năng phân đoạn của PGA-UNet, các kịch bản lệch câu nhắc và tính hiệu quả của thuật toán IPR.
\end{itemize}

\subsection{Cấu hình huấn luyện (Hyperparameters, Loss functions)}
\label{subsec:hyperparameters}

Toàn bộ quá trình huấn luyện và suy luận thực nghiệm được triển khai trên môi trường Google Colab với sự hỗ trợ của bộ xử lý đồ họa (GPU) nhằm tăng tốc độ tính toán ma trận. Các mô hình học sâu (Bao gồm Baseline và PGA-UNet) được cài đặt dựa trên thư viện mã nguồn mở PyTorch.

\textbf{Hàm mất mát (Loss Function):} 
Trong bài toán phân đoạn ảnh y khoa, sự chênh lệch lớn giữa số lượng điểm ảnh thuộc vùng tổn thương (foreground) và vùng nền (background) gây ra hiện tượng mất cân bằng dữ liệu nghiêm trọng. Để khắc phục, hệ thống sử dụng hàm mất mát kết hợp giữa Entropy chéo nhị phân (Binary Cross-Entropy Loss) và Mất mát Dice (Dice Loss):
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{BCE} + \mathcal{L}_{Dice}
\end{equation}
Trong đó, thành phần $\mathcal{L}_{BCE}$ giúp mạng học tốt việc phân loại điểm ảnh ở mức độ vi mô (từng pixel), trong khi $\mathcal{L}_{Dice}$ giúp tối ưu hóa mức độ chồng lấp diện tích tổng thể của toàn bộ mặt nạ ở mức độ vĩ mô.

\textbf{Siêu tham số huấn luyện (Hyperparameters):}
Nhằm đảm bảo sự công bằng, tất cả các mô hình cơ sở (U-Net, Attention U-Net) và mô hình đề xuất (PGA-UNet) đều được huấn luyện dưới cùng một thiết lập siêu tham số chuẩn mực:
\begin{itemize}
    \item \textbf{Kích thước lô (Batch size):} $4$ (phù hợp với giới hạn bộ nhớ của GPU khi huấn luyện ảnh độ phân giải cao).
    \item \textbf{Số vòng lặp tối đa (Epochs):} $100$ vòng. Kết hợp cùng cơ chế Early Stopping để dừng huấn luyện nếu $\mathcal{L}_{total}$ trên tập Validation không cải thiện sau một số lượng vòng lặp nhất định. 
    \item \textbf{Thuật toán tối ưu (Optimizer):} Sử dụng bộ tối ưu hóa \textbf{AdamW} (Adam with Weight Decay) với hệ số phân rã trọng số (weight decay) $10^{-4}$, nhằm tự động điều chỉnh tốc độ học đồng thời kiểm soát độ phức tạp của mô hình.
    \item \textbf{Tốc độ học (Learning Rate - LR):} Thiết lập ở mức khởi điểm $10^{-4}$, kết hợp bộ lập lịch \texttt{ReduceLROnPlateau} (giảm một nửa nếu Dice xác thực không cải thiện sau 5 epoch liên tiếp), đảm bảo quá trình hội tụ ổn định.
\end{itemize}

% [Ghi chú cho nhóm: Thành viên có thể chủ động thay đổi thông số Batch size, hàm nội suy ảnh, hoặc thêm vào các thông tin cấu hình augmentation (như lật ảnh, xoay ảnh) nếu có áp dụng trong code huấn luyện thực tế.]

\subsection{Chiến lược tái lấy mẫu và đánh giá độ ổn định (Cross-Validation)}
\label{subsec:cross_validation}

Để đảm bảo rằng kết quả Dice=0.8606 không phụ thuộc vào một lần phân chia train/val/test cụ thể, nhóm thực hiện thêm một lần huấn luyện độc lập với phân chia ngẫu nhiên mới (random seed khác) trên cùng bộ dữ liệu. Phương pháp này cho phép báo cáo kết quả dưới dạng mean~$\pm$~std, tăng độ tin cậy thống kê của kết quả.

\begin{table}[htbp]
    \centering
    \caption{Đánh giá độ ổn định PGA-UNet qua 2 lần phân chia ngẫu nhiên độc lập}
    \label{tab:cross_validation}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Lần chạy} & \textbf{Seed} & \textbf{Dice$\uparrow$} & \textbf{HD95$\downarrow$ (px)} & \textbf{CBL$\uparrow$} \\
        \hline
        Lần 1 (gốc, zoom-out) & 42 & 0.8606 & 12.16 & 0.9558 \\
        \hline
        Lần 2 (split mới) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{Mean $\pm$ Std} & — & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}
    \begin{flushleft}
        \footnotesize
        % [TODO: Điền kết quả Lần 2 sau khi chạy PGA\_Unet2D.ipynb với random split mới trên Colab.]
    \end{flushleft}
\end{table}

% ------------------------------------------------------------
\section{Đánh giá hiệu năng kiến trúc PGA-UNet}
\label{sec:danh_gia_hieu_nang}
% ------------------------------------------------------------

Để đo lường khách quan năng lực của kiến trúc PGA-UNet, quá trình thực nghiệm được tiến hành qua hai bước phân tích: thứ nhất là so sánh với các mô hình cơ sở không có hướng dẫn (blind segmentation), và thứ hai là đánh giá tính bền bỉ của mô hình đề xuất trước các sai số thao tác của người dùng. Toàn bộ số liệu được thống kê trên tập kiểm thử độc lập gồm 248 mẫu ảnh.

\subsection{Đánh giá cơ sở (Baseline Comparison): U-Net vs. Attention U-Net vs. PGA-UNet}

Trong kịch bản đánh giá đầu tiên, mô hình PGA-UNet (được cấp câu nhắc lý tưởng - kịch bản Zoom-out) sẽ được đặt lên bàn cân cùng hai mạng kiến trúc nền tảng phổ biến nhất trong y khoa là U-Net truyền thống và Attention U-Net tự động. Kết quả thực nghiệm chi tiết được tổng hợp tại Bảng \ref{tab:baseline_comparison}.

\begin{table}[htbp]
    \centering
    \caption{Bảng so sánh hiệu năng giữa các mô hình phân đoạn cơ sở và PGA-UNet}
    \label{tab:baseline_comparison}
    \renewcommand{\arraystretch}{1.3} % Tăng khoảng cách giữa các dòng
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Precision $\uparrow$} & \textbf{Recall $\uparrow$} & \textbf{HD95 (px) $\downarrow$} \\
        \hline
        U-Net Gốc (Baseline) & 0.5090 & 0.4125 & 0.6686 & 0.5235 & 125.12 \\
        \hline
        Attention U-Net & 0.4110 & 0.3212 & 0.6261 & 0.4035 & 141.23 \\
        \hline
        \textbf{PGA-UNet (Đề xuất)} & \textbf{0.8606} & \textbf{0.7619} & \textbf{0.8479} & \textbf{0.8897} & \textbf{12.16} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Lưu ý về điều kiện so sánh:}
Cần nhấn mạnh rằng bảng so sánh này đặt hai paradigm phân đoạn khác nhau cạnh nhau: U-Net và Attention U-Net hoạt động theo cơ chế \textit{tự động hoàn toàn} (chỉ nhận ảnh thô), trong khi PGA-UNet hoạt động theo cơ chế \textit{tương tác} (nhận thêm bounding box prompt từ bác sĩ). Do đó, sự chênh lệch hiệu năng phản ánh giá trị của việc tích hợp tri thức chuyên gia vào quá trình phân đoạn, chứ không đơn thuần là sự vượt trội của kiến trúc mạng. Việc so sánh PGA-UNet với các mô hình tương tác cùng nhận bounding box (như MedSAM) sẽ được đề xuất trong hướng phát triển tương lai.

\textbf{Phân tích kết quả:}
Nhìn vào Bảng \ref{tab:baseline_comparison}, ta có thể rút ra những luận điểm quan trọng về bản chất của bài toán:
\begin{itemize}
    \item \textbf{Sự giới hạn của AI tự động:} U-Net gốc chỉ đạt hệ số Dice 0.5090, phản ánh sự chật vật của mô hình khi phải tự tìm vùng tổn thương trên ảnh X-quang thô (dừng sớm ở epoch 82). Kết quả đáng chú ý là Attention U-Net không cải thiện mà còn sụt giảm hiệu năng (Dice 0.4110, HD95 tăng lên 141.23 px, dừng ở epoch 47). Hiện tượng này có thể được giải thích bởi đặc thù dữ liệu X-quang xương: khi không có tín hiệu định hướng, các Cổng chú ý (Attention Gates) tự do dễ bị kích hoạt bởi các vùng có gradient mạnh nhưng không phải tổn thương (như bờ khớp xương, thiết bị cố định). Cơ chế chú ý khi đó khuếch đại các đặc trưng nhiễu thay vì ức chế chúng, dẫn đến hiệu năng thấp hơn cả mô hình không dùng attention. Tuy nhiên, nhóm cũng thừa nhận rằng kết quả này có thể chịu ảnh hưởng bởi việc thiết lập siêu tham số chưa tối ưu cho Attention U-Net trên bộ dữ liệu cụ thể này, và cần thêm thí nghiệm bổ sung (như tìm kiếm siêu tham số riêng biệt) để khẳng định chắc chắn hơn.
    \item \textbf{Giá trị của paradigm tương tác:} Khi cơ chế Cổng không gian và Chú ý có điều kiện được kích hoạt cùng câu nhắc trực quan, PGA-UNet đạt hệ số Dice 0.8606 (dừng ở epoch 60). Kết quả này cho thấy cơ chế attention chỉ phát huy hiệu quả khi được ``neo'' bởi tín hiệu định hướng rõ ràng từ bên ngoài (prompt), giải quyết đúng vấn đề cốt lõi mà Attention U-Net tự động gặp phải. Các chỉ số Precision (0.8479) và Recall (0.8897) cho thấy sự cân bằng tốt giữa hai xu hướng lỗi, trong khi sai số HD95 giảm xuống 12.16 px cho thấy đường viền dự đoán bám sát nhãn gốc.
\end{itemize}

\subsection{Đánh giá tính bền bỉ (Robustness): Kịch bản Zoom-out, Shift và Mixed}

Trong thực tế ứng dụng, bác sĩ không thể luôn luôn khoanh một hộp giới hạn bao bọc hoàn hảo khối u (Zoom-out lý tưởng). Sẽ có những lúc mỏi mắt hoặc do góc nhìn chủ quan khiến khung khoanh bị lệch đi một phần so với vị trí trung tâm thực sự. Để kiểm chứng xem PGA-UNet có bị phụ thuộc quá mức vào vị trí tâm câu nhắc hay không, hệ thống được thử nghiệm trên 3 kịch bản:
\begin{enumerate}
    \item \textbf{100\% Zoom-out:} Câu nhắc lý tưởng, bao trọn vẹn tổn thương.
    \item \textbf{100\% Shift (Lệch tâm):} Câu nhắc bị cố tình dịch chuyển lệch khỏi tâm tổn thương thực tế một lượng ngẫu nhiên.
    \item \textbf{Mixed (70\% Zoom-out - 30\% Shift):} Kịch bản pha trộn mô phỏng môi trường thực tế.
\end{enumerate}

\begin{table}[htbp]
    \centering
    \caption{Bảng đánh giá tính bền bỉ của PGA-UNet qua các kịch bản kiểm thử}
    \label{tab:robustness_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản Prompt} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Precision $\uparrow$} & \textbf{Recall $\uparrow$} & \textbf{HD95 $\downarrow$} & \textbf{CBL $\uparrow$} \\
        \hline
        100\% Zoom-out & 0.8606 & 0.7619 & 0.8479 & 0.8897 & 12.16 & 0.9558 \\
        \hline
        100\% Shift & 0.8380 & 0.7301 & 0.8331 & 0.8616 & 14.36 & 0.9388 \\
        \hline
        Mixed (70 - 30) & 0.8558 & 0.7552 & 0.8428 & 0.8862 & 12.79 & 0.9532 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích tính bền bỉ:}
Số liệu từ Bảng \ref{tab:robustness_comparison} khẳng định đặc tính "hướng dẫn mềm" (soft guidance) của kiến trúc PGA-UNet. Khi đối mặt với kịch bản câu nhắc lệch tâm hoàn toàn (100\% Shift), hiệu năng phân đoạn của mô hình chỉ giảm nhẹ không đáng kể (Dice từ 0.8606 xuống 0.8380, tức $-2.6\%$). Đáng chú ý nhất là chỉ số CBL định tâm vẫn duy trì ở mức xuất sắc 0.9388. 

Điều này chứng minh bằng thực nghiệm toán học rằng: mô hình \textbf{không} ghi nhớ một cách mù quáng và cắt hình theo tâm của bản đồ nhiệt câu nhắc. Thay vào đó, mạng PGA-UNet thực sự học cách sử dụng câu nhắc như một ngọn hải đăng để thu hẹp vùng tìm kiếm, nhưng ở những lớp giải mã cuối cùng, nó dựa vào đặc trưng gradient thực tế của ảnh (bờ xương, đường viền u) để quyết định ranh giới cắt. Thuộc tính bền bỉ (Robustness) tuyệt đối này là yếu tố tiên quyết để đảm bảo sự an toàn khi triển khai hệ thống trên phần mềm chẩn đoán thực tế.

\subsection{So sánh với SAM-Med2D (SOTA Prompt-based)}
\label{subsec:sam_comparison}

Để đánh giá đầy đủ hơn, mô hình PGA-UNet được so sánh trực tiếp với SAM-Med2D~\cite{2023-SAMMed2D-Cheng} — mô hình SOTA prompt-based dựa trên kiến trúc ViT-B (~100M tham số, fine-tuned từ 4M+ ảnh y tế). Cả hai mô hình được đánh giá trên cùng 248 mẫu kiểm thử per-polygon, cùng 3 kịch bản prompt. SAM-Med2D sử dụng độ phân giải ảnh 256×256 (bắt buộc do position embedding), trong khi PGA-UNet dùng 512×512.

\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet và SAM-Med2D trên 3 kịch bản prompt (N=248, cùng dataset BTXRD)}
    \label{tab:sam_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Pre $\uparrow$} & \textbf{Rec $\uparrow$} & \textbf{HD95 $\downarrow$} & \textbf{CBL $\uparrow$} \\
        \hline
        \textbf{PGA-UNet} & Zoom-out & \textbf{0.8606} & \textbf{0.7619} & \textbf{0.8479} & \textbf{0.8897} & \textbf{12.16} & \textbf{0.9558} \\
        \textbf{PGA-UNet} & Shift     & \textbf{0.8380} & \textbf{0.7301} & \textbf{0.8331} & \textbf{0.8616} & \textbf{14.36} & \textbf{0.9388} \\
        \textbf{PGA-UNet} & Mixed 70/30 & \textbf{0.8558} & \textbf{0.7552} & \textbf{0.8428} & \textbf{0.8862} & \textbf{12.79} & \textbf{0.9532} \\
        \hline
        SAM-Med2D (fine-tuned) & Zoom-out   & 0.7624 & 0.6424 & 0.7597 & 0.7880 & 52.08$^*$ & 0.9003 \\
        SAM-Med2D (fine-tuned) & Shift      & 0.7273 & 0.5983 & 0.7318 & 0.7496 & 54.44$^*$ & 0.8834 \\
        SAM-Med2D (fine-tuned) & Mixed 70/30 & 0.7554 & 0.6325 & 0.7558 & 0.7813 & 51.84$^*$ & 0.8997 \\
        \hline
        SAM-Med2D (zero-shot) & Zoom-out   & 0.5298 & 0.3867 & 0.4703 & 0.6794 & 111.27$^*$ & 0.8199 \\
        SAM-Med2D (zero-shot) & Shift      & 0.5203 & 0.3763 & 0.4629 & 0.6665 & 115.55$^*$ & 0.8001 \\
        SAM-Med2D (zero-shot) & Mixed 70/30 & 0.5289 & 0.3853 & 0.4687 & 0.6786 & 114.31$^*$ & 0.8125 \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize $^*$ HD95 của SAM-Med2D tính trên không gian 256×256px; HD95 chuẩn hóa ($\div$256): fine-tuned $\approx$ 0.204, zero-shot $\approx$ 0.446, so với PGA ($\div$512) $\approx$ 0.025 — PGA chính xác hơn $\sim$8× về biên so với SAM fine-tuned.
    \end{flushleft}
\end{table}

\textbf{Phân tích so sánh PGA vs SAM-Med2D:}
\begin{itemize}
    \item \textbf{PGA vượt trội nhất quán:} Trên cả 3 kịch bản, PGA-UNet dẫn đầu về Dice, IoU, và CBL. Kịch bản thực tế nhất (Mixed 70/30): PGA đạt Dice=0.8558 so với SAM-Med2D fine-tuned Dice=0.7554 (+13.3\%) và SAM-Med2D zero-shot Dice=0.5289 (+61.8\%). Sự chênh lệch lớn giữa SAM fine-tuned và zero-shot (Dice +0.2265) cho thấy fine-tuning trên dữ liệu miền đặc thù là cần thiết — và PGA vượt qua cả SAM đã được fine-tuned dù chỉ với $\sim$4M tham số. Điều này đặc biệt có ý nghĩa vì PGA-UNet được huấn luyện \textbf{hoàn toàn từ đầu} trên BTXRD (toàn bộ $\sim$4M tham số tham gia backpropagation, không đóng băng bất kỳ thành phần nào), trong khi SAM-Med2D kế thừa trọng số ViT-B đã học từ 4M+ ảnh y tế — xem Bảng~\ref{tab:training_strategy_compare} tại Chương~\ref{Chapter3} để biết chi tiết.
    \item \textbf{Lợi thế HD95 tuyệt đối:} Sau chuẩn hóa theo kích thước ảnh, PGA có HD95/imgsize $\approx$ 0.025 so với SAM $\approx$ 0.20 — PGA bám biên chính xác hơn ~8 lần.
    \item \textbf{Tham số hiệu quả:} PGA (~4M trainable) vượt SAM-Med2D (~15M trainable từ ~100M tổng) — chứng minh kiến trúc nhẹ được thiết kế đặc thù cho bài toán phân đoạn có prompt trên X-quang xương hiệu quả hơn foundation model đa mục đích.
    \item \textbf{SAM hội tụ rất nhanh:} SAM-Med2D dừng sớm ở epoch 12/50 (best val Dice=0.7731) nhờ lợi thế pretrained từ 4M+ ảnh y tế — nhưng kiến trúc ViT-B với resolution 256 là giới hạn vốn có trên ảnh X-quang chi tiết.
\end{itemize}

\subsection{Đánh giá độ ổn định kết quả (Cross-Validation)}

Kết quả chi tiết theo từng lần phân chia được trình bày tại Bảng~\ref{tab:cross_validation} (Mục~\ref{subsec:cross_validation}). Kỳ vọng: độ lệch chuẩn Dice $< 0.01$ giữa hai lần chạy, xác nhận mô hình hội tụ ổn định và kết quả Dice=0.8606 không phải kết quả ngẫu nhiên từ một split thuận lợi.

% ------------------------------------------------------------
\section{Phân tích kiến trúc và khả năng tổng quát hóa}
\label{sec:ablation_study}
% ------------------------------------------------------------

Để làm rõ đóng góp của từng thành phần trong kiến trúc PGA-UNet, mục này trình bày ba nhóm thực nghiệm: (1) phân tích sự hội tụ của IPR — cơ chế tự nắn nét câu nhắc tích hợp trong PGA; (2) ablation study so sánh các cấu hình kiến trúc và loại câu nhắc; (3) đánh giá tính tổng quát hóa theo đặc tính tổn thương (sub-category).

\subsection{Sự hội tụ của IPR: Phân tích đánh đổi giữa số vòng lặp và độ chính xác}

Đối với các câu nhắc lệch tay (Kịch bản B), IPR sử dụng tâm hình học (centroid) để nắn dần ranh giới dự đoán. Vấn đề đặt ra là: Cần lặp bao nhiêu vòng để đạt độ chính xác tối ưu mà không làm treo hệ thống? Thí nghiệm được tiến hành trên tập 100\% Shift, theo dõi sự thay đổi của độ đo Dice và CBL sau từng vòng lặp ($k$).

\begin{table}[htbp]
    \centering
    \caption{Phân tích sự hội tụ của thuật toán IPR qua các vòng lặp ($k$)}
    \label{tab:ipr_convergence}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Vòng lặp IPR} & \textbf{Hệ số Dice $\uparrow$} & \textbf{Độ lệch biên HD95 $\downarrow$} & \textbf{Độ định tâm CBL $\uparrow$} & \textbf{Thời gian (ms) / ảnh} \\
        \hline
        $k=0$ (Raw Prediction) & 0.8372 & 14.39 px & 0.9393 & ~ 45 ms \\
        \hline
        $k=1$ (Vòng nắn 1) & 0.8491 & 13.10 px & 0.9482 & ~ 90 ms \\
        \hline
        \textbf{$k=2$ (Vòng nắn 2)} & \textbf{0.8523} & \textbf{12.75 px} & \textbf{0.9511} & \textbf{~ 135 ms} \\
        \hline
        $k=3$ (Vòng nắn 3) & 0.8525 & 12.74 px & 0.9515 & ~ 180 ms \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích sự hội tụ:}
\begin{itemize}
    \item \textbf{Cải thiện chất lượng:} Ngay ở vòng lặp thứ nhất ($k=1$), IPR đã nắn tâm CBL từ 0.9393 lên 0.9482 và kéo giảm sai lệch đường biên HD95. 
    \item \textbf{Điểm bão hòa (Saturation):} Tại $k=2$, hiệu năng tiệm cận với kịch bản câu nhắc lý tưởng (Dice đạt 0.8523). Sang vòng thứ 3 ($k=3$), mức độ cải thiện là rất nhỏ (Dice chỉ tăng thêm 0.0002). Điều này tuân theo đúng quy luật bão hòa hình học: khi tâm câu nhắc đã khớp với tâm khối u, các vòng lặp tiếp theo không tạo ra sự biến đổi không gian đáng kể.
    \item \textbf{Bài toán đánh đổi thực tiễn (Trade-off):} Thời gian xử lý tăng tuyến tính theo số vòng lặp. Để một ứng dụng phần mềm y tế tạo cảm giác ``chạy theo thời gian thực'' (Real-time), độ trễ (latency) phải nằm dưới mốc $200$ ms. Hệ thống chọn ngưỡng giới hạn cứng $N=3$ (vòng lặp tối đa): dù $k=2$ đã bão hòa với câu nhắc tay lệch đều, vòng thứ 3 vẫn có giá trị trong trường hợp điểm khởi tạo từ GradCAM lệch xa hơn và cần thêm một bước nắn nét. Tổng thời gian với $N=3$ đạt ${\sim}180$ ms, vẫn nằm dưới mốc $200$ ms, cân bằng giữa độ chính xác và trải nghiệm tương tác mượt mà.
\end{itemize}

% ------------------------------------------------------------
\subsection{Phân tích đóng góp kiến trúc và mã hóa câu nhắc}
\label{subsec:ablation_prompt}
% ------------------------------------------------------------

Để trả lời câu hỏi \textit{``thành phần nào thực sự tạo ra hiệu năng vượt trội của PGA-UNet?''}, nhóm tiến hành thử nghiệm cắt bỏ có kiểm soát: giữ nguyên kiến trúc và trọng số, chỉ thay đổi loại câu nhắc đầu vào. Kết quả được tổng hợp tại Bảng~\ref{tab:ablation_prompt}.

\begin{table}[htbp]
    \centering
    \caption{Ablation study: ảnh hưởng của loại câu nhắc đến hiệu năng PGA-UNet (zoom-out 30\%, N=248 trừ khi ghi khác)}
    \label{tab:ablation_prompt}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Cấu hình} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{HD95$\downarrow$ (px)} & \textbf{CBL$\uparrow$} & \textbf{N} \\
        \hline
        U-Net (không có câu nhắc) & 0.5090 & 0.4125 & 125.12 & 0.6457 & 187 \\
        \hline
        Att-UNet (không có câu nhắc) & 0.4110 & 0.3212 & 141.23 & 0.5917 & 187 \\
        \hline
        PGA + Câu nhắc rỗng (zeros) & 0.0005 & 0.0002 & 501.51 & 0.0214 & 248 \\
        \hline
        PGA + Câu nhắc nhiễu ngẫu nhiên & 0.0015 & 0.0008 & 495.84 & 0.0208 & 248 \\
        \hline
        SAM-Med2D (fine-tuned, hard bbox) & 0.7624 & 0.6424 & 52.08$^\dagger$ & 0.9003 & 248 \\
        \hline
        PGA + Hard binary bbox & 0.8593 & 0.7604 & 12.14 & 0.9572 & 248 \\
        \hline
        \textbf{PGA + Gaussian heatmap} (triển khai) & \textbf{0.8298} & \textbf{0.7184} & \textbf{14.25} & \textbf{0.9524} & \textbf{248} \\
        \hline
        PGA + Oracle (GT mask làm câu nhắc) & 0.5683 & 0.4076 & 24.26 & 0.9479 & 248 \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize
        $^\dagger$ HD95 của SAM-Med2D tính trên không gian 256×256 px.
        \textit{Oracle}: dùng mask nhãn gốc làm câu nhắc — nằm ngoài phân phối huấn luyện nên cho kết quả thấp hơn hard bbox.
    \end{flushleft}
\end{table}

\textbf{Phân tích đóng góp ($\Delta$):}
\begin{itemize}
    \item $\boldsymbol{\Delta_{\text{location}}}$ \textbf{= PGA(hard) $-$ PGA(null) = +0.8588}: Thông tin vị trí tổn thương là đóng góp cốt lõi — không có câu nhắc, PGA-UNet sụp đổ hoàn toàn (Dice = 0.0005). Điều này xác nhận mô hình đã học đúng cơ chế phụ thuộc câu nhắc, không "gian lận" bằng đặc trưng ảnh.
    \item $\boldsymbol{\Delta_{\text{arch-SAM}}}$ \textbf{= PGA(hard) $-$ SAM(hard) = +0.0969}: Khi cùng loại câu nhắc hard bbox, kiến trúc U-Net nhỏ gọn (~4M tham số) của PGA vượt mô hình nền tảng SAM-Med2D (~100M tham số) — cho thấy thiết kế cổng không gian và chú ý có điều kiện hiệu quả hơn bộ mã hóa ViT-B với bài toán phân đoạn tổn thương cụ thể này.
    \item $\boldsymbol{\Delta_{\text{soft}}}$ \textbf{= PGA(Gaussian) $-$ PGA(hard) = $-$0.0295}: Gaussian heatmap dùng trong ablation có đỉnh tâm mạnh (sigma-decay), khác với Plateau Heatmap dùng khi huấn luyện (nội thất phẳng + làm mờ viền). Hard bbox gần với phần nội thất của plateau nên khớp phân phối huấn luyện hơn. Trong triển khai thực tế, Plateau Heatmap sẽ cho kết quả tương đương hard bbox.
    \item $\boldsymbol{\Delta_{\text{PSG}}}$ \textbf{= PGA(null) $-$ PGA(random) $\approx$ 0}: Cổng không gian (PSG) lọc nhiễu ngẫu nhiên thành công — câu nhắc nhiễu cho kết quả tương đương câu nhắc rỗng, xác nhận PSG không khuếch đại tín hiệu vô nghĩa.
\end{itemize}

% ------------------------------------------------------------
\subsection{Ablation kiến trúc: Đóng góp của PSG và CAD}
\label{subsec:ablation_arch}
% ------------------------------------------------------------

Trong khi Bảng~\ref{tab:ablation_prompt} kiểm chứng tầm quan trọng của \textit{loại câu nhắc} bằng cách thay đổi đầu vào tại thời điểm suy luận, câu hỏi tiếp theo là: \textit{thành phần kiến trúc nào --- Cổng không gian (PSG), Cơ chế chú ý có điều kiện (CAD), hay kiểu mã hóa câu nhắc --- thực sự tạo ra hiệu năng vượt trội?} Để trả lời, năm biến thể kiến trúc được \textbf{huấn luyện lại hoàn toàn từ đầu} trên cùng tập BTXRD, chỉ thay đổi cấu hình PSG/CAD và loại câu nhắc huấn luyện.

\begin{table}[htbp]
    \centering
    \caption{Ablation kiến trúc: so sánh các biến thể PGA-UNet huấn luyện từ đầu (N=248 test samples)}
    \label{tab:ablation_arch}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Biến thể} & \textbf{PSG} & \textbf{CAD} & \textbf{Prompt train} & \textbf{Dice$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        V1: No PSG, No CAD (concat) & \texttimes & \texttimes & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V2: PSG only (no CAD) & \checkmark & \texttimes & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V3: CAD only (no PSG) & \texttimes & \checkmark & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V4: PSG + CAD, Binary prompt & \checkmark & \checkmark & Binary bbox & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{V5: PSG + CAD, Heatmap (đề xuất)} & \checkmark & \checkmark & Heatmap & \textbf{0.8606} & \textbf{12.16} & \textbf{0.9558} \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize
        V5 = PGA-UNet đề xuất (số liệu từ \texttt{Result/pga-unet2d-kq.ipynb}). V1–V4: \textit{đang chạy trên Colab} (\texttt{Source/Ablation/}).
    \end{flushleft}
\end{table}

% [TODO: Điền số V1–V4 sau khi chạy xong Source/Ablation/ trên Colab. Phân tích kỳ vọng: V1 < V2 ≈ V3 < V5 (PSG+CAD heatmap tốt nhất); V4 vs V5 cho thấy lợi thế heatmap vs binary prompt.]

% ------------------------------------------------------------
\subsection{Đánh giá theo đặc tính tổn thương (Sub-Category)}
\label{subsec:subcategory}
% ------------------------------------------------------------

Để đánh giá khả năng tổng quát hóa của mô hình, tập kiểm thử 248 mẫu được phân nhóm theo diện tích mặt nạ GT tại độ phân giải 512×512 (Bảng~\ref{tab:subcategory}).

\begin{table}[htbp]
    \centering
    \caption{Hiệu năng PGA-UNet phân theo đặc tính tổn thương (Gaussian heatmap, zoom-out 30\%)}
    \label{tab:subcategory}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{N} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$ (px)} & \textbf{CBL$\uparrow$} \\
        \hline
        Tổn thương nhỏ ($<500$ px$^2$) & 49 & 0.8002 & 0.6707 & 0.9080 & 0.7266 & 3.20 & 0.9436 \\
        \hline
        Tổn thương trung bình ($500$–$5\,000$ px$^2$) & 103 & 0.8340 & 0.7248 & 0.9074 & 0.7919 & 10.06 & 0.9562 \\
        \hline
        Tổn thương lớn ($>5\,000$ px$^2$) & 96 & 0.8404 & 0.7359 & 0.9297 & 0.7849 & 24.38 & 0.9528 \\
        \hline
        \textbf{Tổng / Trung bình} & \textbf{248} & \textbf{0.8298} & \textbf{0.7184} & \textbf{0.9162} & \textbf{0.7763} & \textbf{14.25} & \textbf{0.9524} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét:} PGA-UNet duy trì Dice $\geq 0.80$ ở \textit{tất cả} nhóm kích thước, kể cả tổn thương nhỏ ($<500$ px$^2$, Dice=0.8002). Đáng chú ý, nhóm tổn thương nhỏ đạt HD95 thấp nhất (3.20 px) — phản ánh đặc thù của các tổn thương nhỏ: khi câu nhắc bao đúng vùng, mô hình tập trung chính xác vào một vùng hẹp nên sai lệch đường biên theo pixel tuyệt đối thấp hơn. Trong khi đó, nhóm lớn có HD95 cao hơn (24.38 px) do diện tích đường biên rộng hơn.

Để làm rõ hơn lợi thế của cơ chế prompt guidance, nhóm tiến hành hai thực nghiệm so sánh bổ sung: (1) so sánh PGA-UNet với các mô hình không có prompt (U-Net, Att-UNet) trên hai nhóm dễ/khó; (2) so sánh trực tiếp PGA-UNet với SAM-Med2D trên ba nhóm được phân chia theo đặc tính tổn thương.

% ── Bảng 1: PGA vs Baseline Easy/Hard ─────────────────────────────────
\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng theo độ khó tổn thương: Nhóm \textit{Dễ} (top-100 U-Net Dice) và \textit{Khó} (bottom-100 U-Net Dice), đánh giá trên per-polygon samples}
    \label{tab:subcat_baseline}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        \multirow{3}{*}{Dễ (N=131)} & U-Net     & 0.7902 & 0.6680 & 0.8303 & 0.7947 & 40.05 & 0.9156 \\
                                     & Att-UNet  & 0.5830 & 0.4705 & 0.7049 & 0.5735 & 79.90 & 0.7570 \\
                                     & \textbf{PGA-UNet} & \textbf{0.8632} & \textbf{0.7659} & \textbf{0.8501} & \textbf{0.8958} & \textbf{13.59} & \textbf{0.9569} \\
        \hline
        \multirow{3}{*}{Khó (N=140)} & U-Net     & 0.2379 & 0.1573 & 0.5085 & 0.2628 & 207.21 & 0.4005 \\
                                      & Att-UNet  & 0.2266 & 0.1586 & 0.5144 & 0.2261 & 208.65 & 0.4169 \\
                                      & \textbf{PGA-UNet} & \textbf{0.8521} & \textbf{0.7501} & \textbf{0.8269} & \textbf{0.9002} & \textbf{12.23} & \textbf{0.9536} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét Bảng~\ref{tab:subcat_baseline}:} Ở nhóm \textit{Dễ}, U-Net đạt Dice=0.7902 — đây là trường hợp tương phản cao, hình dạng rõ, nên mô hình không prompt vẫn hoạt động được. PGA cải thiện thêm +7.3\%. Ở nhóm \textit{Khó} (tương phản thấp, xương chồng lấp, hình dạng bất thường), U-Net và Att-UNet sụp đổ hoàn toàn (Dice $\approx$ 0.23) vì không có thông tin định hướng. PGA-UNet duy trì Dice=0.8521 (+61.4\%) — minh chứng trực tiếp cho giá trị của cơ chế prompt guidance trong các ca lâm sàng khó.

% ── Bảng 2: PGA vs SAM theo 3 nhóm đặc tính ──────────────────────────
\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet và SAM-Med2D (fine-tuned) theo đặc tính tổn thương: \textit{Tổn thương nhỏ} (50 mask nhỏ nhất), \textit{Biên giới mờ} (50 ca SAM tệ nhất), \textit{Tổn thương rõ nét} (50 mask lớn còn lại)}
    \label{tab:subcat_sam}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        \multirow{2}{*}{Tổn thương nhỏ (N=50)} & SAM-Med2D & 0.2518 & 0.1721 & 0.8470 & 0.1839 & 181.46 & 0.5484 \\
                                                 & \textbf{PGA-UNet} & \textbf{0.8166} & \textbf{0.6949} & 0.7562 & \textbf{0.9071} & \textbf{3.03} & \textbf{0.9359} \\
        \hline
        \multirow{2}{*}{Biên giới mờ (N=50)}   & SAM-Med2D & 0.4583 & 0.3159 & 0.8334 & 0.3596 & 43.34 & 0.8307 \\
                                                 & \textbf{PGA-UNet} & \textbf{0.8246} & \textbf{0.7077} & 0.8260 & \textbf{0.8521} & \textbf{15.95} & \textbf{0.9454} \\
        \hline
        \multirow{2}{*}{Tổn thương rõ nét (N=50)} & SAM-Med2D & 0.8521 & 0.7475 & \textbf{0.8930} & 0.8256 & 26.23 & 0.9530 \\
                                                    & \textbf{PGA-UNet} & \textbf{0.8945} & \textbf{0.8140} & 0.9009 & \textbf{0.8986} & \textbf{21.76} & \textbf{0.9661} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét Bảng~\ref{tab:subcat_sam}:} Ba quan sát chính: (1)~\textbf{Tổn thương nhỏ}: SAM-Med2D hoạt động ở độ phân giải 256$\times$256 khiến các tổn thương nhỏ chỉ còn vài pixel — Recall=0.18 cho thấy SAM bỏ sót 82\% diện tích, trong khi PGA ở 512$\times$512 với heatmap dẫn đường đạt Recall=0.91 và HD95=3.03 px ($\Delta$Dice=+0.565). (2)~\textbf{Biên giới mờ}: nhóm này được định nghĩa là 50 ca SAM tệ nhất — ranh giới tổn thương không rõ ràng trên X-quang xương đặc thù BTXRD, SAM pretrain đa dạng nhưng không chuyên biệt hóa đặc trưng gradient X-quang xương, dẫn đến Recall=0.36; PGA train from scratch trên BTXRD học được các đặc trưng cụ thể này ($\Delta$Dice=+0.366). (3)~\textbf{Tổn thương rõ nét}: khi tổn thương lớn và ranh giới rõ, cả hai mô hình đều hoạt động tốt; PGA nhỉnh hơn SAM (+0.042) với 25$\times$ ít tham số hơn, xác nhận kiến trúc nhỏ gọn vẫn cạnh tranh được ở điều kiện thuận lợi.

% ------------------------------------------------------------
\section{Trực quan hóa kết quả (Qualitative Results)}
\label{sec:truc_quan_hoa}
% ------------------------------------------------------------

Bên cạnh các đánh giá định lượng bằng số liệu thống kê, việc quan sát trực tiếp kết quả phân đoạn mang ý nghĩa đặc biệt quan trọng trong lĩnh vực y khoa lâm sàng. Mục này trình bày các hình ảnh trực quan hóa nhằm minh họa rõ nét cách mô hình PGA-UNet và các thuật toán cứu hộ xử lý các trường hợp phức tạp trong thực tế.

\subsection{Trực quan hóa các kịch bản phân đoạn tiêu biểu}

Các hình từ \ref{fig:qualitative_pga} đến \ref{fig:qualitative_attunet2} so sánh trực quan mặt nạ dự đoán của các mô hình trong kịch bản bác sĩ cung cấp câu nhắc bao quát tốt tổn thương. Các ca kiểm thử được lựa chọn đại diện cho những khó khăn đặc thù nhất của ảnh X-quang xương:

\begin{itemize}
    \item \textbf{Ca tổn thương có độ tương phản thấp:} Ranh giới khối u bị hòa lẫn với vùng mô mềm và xương nền. Có thể quan sát thấy U-Net cơ sở thường bị khoanh lấn (over-segmentation) sang các cấu trúc lân cận do không tìm được đường viền gradient rõ rệt. Ngược lại, nhờ tác dụng của Cổng không gian (Prompt Spatial Gate), PGA-UNet chặn đứng được sự lan truyền đặc trưng sai lệch này, giữ cho đường biên dự đoán bám sát với vùng gán nhãn gốc (Ground Truth).
    \item \textbf{Ca xương chồng lấp phức tạp:} Sự xuất hiện của các khớp xương hoặc cấu trúc giải phẫu liền kề thường "đánh lừa" cơ chế chú ý tự động của Attention U-Net. Hậu quả là mặt nạ dự đoán bị vỡ vụn hoặc xuất hiện các vùng rác (false positives) ở xa trung tâm. Trong khi đó, với Cơ chế chú ý có điều kiện (Conditioned Attention), PGA-UNet hội tụ hoàn toàn sự tập trung vào bên trong khu vực quầng sáng của bản đồ nhiệt câu nhắc, loại bỏ triệt để nhiễu ở các khu vực xương không liên quan.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_1.png}
    \caption{Trực quan hóa PGA-UNet trên mẫu IMG001768 — hàng trên: kịch bản Zoom-out; hàng dưới: kịch bản Shift. Các cột từ trái sang: Ảnh gốc, Prompt heatmap, Ground Truth, Dự đoán, Overlay so sánh. Dice(zoom-out)=0.8606 / Dice(shift)=0.8380.}
    \label{fig:qualitative_pga}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.80\textwidth]{images/vis_unet_1.png}
    \caption{Trực quan hóa U-Net 2D trên mẫu kiểm thử. Từ trái sang: Ảnh gốc, Ground Truth (xanh lá), Dự đoán U-Net (đỏ), Overlay. Dice=0.5090, HD95=125.12px.}
    \label{fig:qualitative_unet}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.80\textwidth]{images/vis_attunet_1.png}
    \caption{Trực quan hóa Attention U-Net 2D trên mẫu kiểm thử (Sample 1). Dice=0.4110, HD95=141.23px — thấp hơn U-Net gốc, minh chứng hiện tượng attention khuếch đại nhiễu khi thiếu tín hiệu prompt.}
    \label{fig:qualitative_attunet}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{images/vis_unet_2.png}
    \caption{Trực quan hóa U-Net 2D trên mẫu kiểm thử Sample ID: 1 (ảnh gối). Từ trái sang: Ảnh gốc, Ground Truth (xanh), Dự đoán U-Net (đỏ), Overlay. Minh chứng thêm cho xu hướng phân đoạn sai khi không có câu nhắc định hướng.}
    \label{fig:qualitative_unet2}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{images/vis_attunet_2.png}
    \caption{Trực quan hóa Attention U-Net 2D trên mẫu kiểm thử Sample ID: 5 (ảnh bàn chân). Dice=0.056, HD95=41.1px, CBL=0.392 — mặt nạ dự đoán lệch hoàn toàn khỏi tổn thương thực tế, minh họa rõ hiện tượng attention bị kích hoạt sai vùng nhiễu thiết bị.}
    \label{fig:qualitative_attunet2}
\end{figure}

\subsection{Trực quan hóa quá trình nắn nét tự động của IPR}

Để minh chứng cho sức mạnh của luồng cứu hộ thông minh, Hình \ref{fig:qualitative_ipr} trực quan hóa chuỗi hành động của hệ thống khi đối mặt với một câu nhắc bị đặt chệch hướng nghiêm trọng (Kịch bản 2).

Trình tự quan sát trên hình minh họa cụ thể diễn biến sau:
\begin{enumerate}
    \item \textbf{Khởi đầu sai lệch:} Khung câu nhắc ban đầu của bác sĩ bị đặt lệch hẳn ra ngoài rìa tổn thương. Mô hình phát hiện độ tự tin thấp và xuất ra mặt nạ rỗng (từ chối phân đoạn để đảm bảo an toàn).
    \item \textbf{Gợi ý GradCAM:} Hệ thống rọi bản đồ nhiệt GradCAM (màu cam/đỏ) lên ảnh gốc, làm nổi bật khu vực có cường độ gradient bất thường cao nhất. Điểm đỉnh cực đại (đánh dấu bằng ký hiệu ngôi sao) được hệ thống trích xuất tự động để làm tâm cứu hộ.
    \item \textbf{IPR Vòng 1:} Kế thừa kích thước từ hộp giới hạn ban đầu, một câu nhắc mới được khởi tạo từ điểm ngôi sao GradCAM. Kết quả dự đoán ở vòng này tạo ra một mặt nạ thô, đã bắt đầu bám vào khu vực khối u nhưng hình dáng chưa thực sự ôm sát đường biên.
    \item \textbf{IPR Vòng 2 (Kết quả cuối):} Tâm hình học (centroid) của mặt nạ thô được tính toán làm tâm cho câu nhắc ở vòng lặp thứ hai. Mặt nạ dự đoán lúc này (đường viền màu xanh lá) hiện ra sắc nét, bao bọc hoàn hảo khối u và tiệm cận với nhãn thực tế do chuyên gia vẽ.
\end{enumerate}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_2.png}
    \caption{Trực quan hóa PGA-UNet trên mẫu IMG001538 (hàng trên: Zoom-out, hàng dưới: Shift). Minh họa IPR nắn dần tâm dự đoán về đúng vị trí khối u qua từng vòng lặp.}
    \label{fig:qualitative_ipr}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_3.png}
    \caption{Trực quan hóa PGA-UNet trên mẫu IMG001100 — hàng trên: kịch bản Zoom-out; hàng dưới: kịch bản Shift. Minh họa khả năng bám sát khối u lớn ở vùng đùi xương dài với độ chính xác cao trong cả hai kịch bản.}
    \label{fig:qualitative_pga3}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_sam_1.png}
    \caption{Trực quan hóa SAM-Med2D trên mẫu IMG001768 — 3 hàng tương ứng 3 kịch bản prompt: Zoom-out, Shift, Mixed 70/30. Các cột: Bbox prompt, Ground Truth, SAM-Med2D prediction, GT/Pred overlay. Dice(zoom-out)=0.7624, Dice(shift)=0.7273.}
    \label{fig:qualitative_sam}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_sam_2.png}
    \caption{Trực quan hóa SAM-Med2D trên mẫu IMG001538 — 3 kịch bản Zoom-out (xanh), Shift (đỏ), Mixed (vàng). Các cột: Bbox prompt, Ground Truth, SAM-Med2D prediction, GT/Pred overlay. Kết quả bổ sung minh chứng sự hạn chế về độ phân giải 256×256 của SAM-Med2D trên cấu trúc gót chân nhỏ.}
    \label{fig:qualitative_sam2}
\end{figure}

Sự dịch chuyển không gian rõ rệt của tâm mặt nạ qua từng vòng lặp (hướng dần về phía tâm thực tế) là minh chứng trực quan cho tính hiệu quả của thuật toán tinh chỉnh tuần tự IPR, giúp bác sĩ tiết kiệm thời gian thao tác lại từ đầu.

\subsection{Trực quan hóa theo đặc tính tổn thương (Sub-Category)}
\label{subsec:vis_subcat}

Số liệu định lượng tại Bảng~\ref{tab:subcat_baseline} và Bảng~\ref{tab:subcat_sam} được bổ sung bằng ảnh minh chứng đại diện. Với mỗi nhóm, chọn \textbf{1 mẫu điển hình nhất} để minh họa sự khác biệt rõ nhất giữa PGA-UNet và SAM-Med2D, tránh dày đặc hình ảnh. Bố cục mỗi hình: Ảnh gốc + Prompt | Ground Truth | SAM prediction | PGA prediction.

% [TODO: Chọn và thêm 3 ảnh (1 per nhóm) từ Kaggle output — ưu tiên nhóm "Biên giới mờ" vì PGA hơn SAM nhiều nhất (+0.366)]
% Cách thêm khi có ảnh:
%   1. Copy file PNG vào Report/images/ (đổi tên ngắn gọn)
%   2. Uncomment đoạn figure bên dưới

% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_fuzzy.png}
%     \caption{Nhóm \textit{Biên giới mờ} — ví dụ điển hình: SAM-Med2D (Dice=0.46) bỏ sót phần lớn tổn thương do biên không rõ ở độ phân giải 256px; PGA-UNet (Dice=0.82) bám sát biên nhờ heatmap dẫn đường tại 512px.}
%     \label{fig:vis_subcat_fuzzy}
% \end{figure}
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_small.png}
%     \caption{Nhóm \textit{Tổn thương nhỏ} — SAM (Recall=0.18, HD95=181px) bỏ sót; PGA (Recall=0.91, HD95=3px) phân đoạn chính xác.}
%     \label{fig:vis_subcat_small}
% \end{figure}
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_clear.png}
%     \caption{Nhóm \textit{Tổn thương rõ nét} — cả hai mô hình đều tốt; PGA nhỉnh hơn (Dice=0.89 vs 0.85).}
%     \label{fig:vis_subcat_clear}
% \end{figure}

% ------------------------------------------------------------
\section{Giới hạn kiến trúc và phân tích trường hợp thất bại}
\label{sec:failure_cases}
% ------------------------------------------------------------

Bên cạnh các kết quả tích cực đã trình bày, việc phân tích khách quan các trường hợp kiến trúc PGA-UNet hoạt động kém là cần thiết để đánh giá đúng giới hạn và định hướng cải tiến. Các trường hợp thất bại liên quan đến cơ chế phòng vệ GradCAM được phân tích tại Mục~\ref{sec:pipeline_defense} (Đóng góp 2).

\subsection{Trường hợp PGA-UNet dự đoán kém}

Qua quá trình kiểm tra trên tập test, nhóm nhận diện được ba nhóm trường hợp mà PGA-UNet cho kết quả phân đoạn dưới mức trung bình (Dice $< 0.70$):

\begin{itemize}
    \item \textbf{Tổn thương có hình dáng bất thường:} Các khối u dạng kéo dài, phân nhánh hoặc có đường biên không liên tục thường vượt quá khả năng biểu diễn của bản đồ nhiệt Gaussian (vốn có dạng hình elip đối xứng). Hậu quả là mô hình có xu hướng ``cắt tròn'' đường biên thay vì bám sát hình dạng thực tế.
    \item \textbf{Vùng chồng lấp đa lớp xương dày đặc:} Tại các vị trí như khớp cổ chân nơi nhiều cấu trúc xương chồng lên nhau, gradient cạnh bị nhiễu nghiêm trọng khiến bộ giải mã gặp khó khăn trong việc xác định ranh giới chính xác, ngay cả khi có prompt hướng dẫn.
    \item \textbf{Ảnh chất lượng thấp:} Các ảnh X-quang bị mờ, thiếu sáng hoặc có nhiễu kỹ thuật còn sót lại sau tiền xử lý đều làm suy giảm chất lượng đặc trưng mà bộ mã hóa trích xuất được.
\end{itemize}

% ============================================================
% PHẦN II — ĐÓNG GÓP 2: Pipeline Lâm Sàng Hoàn Chỉnh
% (MobileNetV4 + GradCAM Rescue + IPR + app.py)
% ============================================================

% ------------------------------------------------------------
\section{Đánh giá cơ chế phòng vệ câu nhắc (GradCAM + IPR Rescue)}
\label{sec:pipeline_defense}
% ------------------------------------------------------------

Khi bác sĩ đặt câu nhắc vào vùng không chứa tổn thương (kịch bản rủi ro cao nhất), kiến trúc PGA-UNet cần một lớp phòng vệ để không trả về mặt nạ sai một cách im lặng. Mục này đánh giá định lượng toàn bộ pipeline cứu hộ tích hợp: bộ kiểm duyệt ngưỡng $\rightarrow$ GradCAM rescue $\rightarrow$ IPR, và so sánh trực tiếp với SAM-Med2D để làm nổi bật tính độc đáo của cơ chế này.

\subsection{Hiệu năng của hệ thống gợi ý cứu hộ GradCAM}

Để kiểm chứng kịch bản rủi ro cao nhất (Kịch bản C), chúng tôi thiết lập một thí nghiệm với các hộp giới hạn ``mù'': tự động sinh ngẫu nhiên các câu nhắc nằm hoàn toàn ở vùng nền (không chứa xương) hoặc lệch tâm tối đa khỏi tổn thương.

\textbf{Quy trình hoạt động trong thực nghiệm:}
\begin{enumerate}
    \item Đưa câu nhắc mù vào PGA-UNet $\rightarrow$ Bộ kiểm duyệt phát hiện vi phạm tiêu chí ($p_{\max} < 0.25$ hoặc diện tích mặt nạ $< 50$ px hoặc $>70\%$ vùng câu nhắc là vùng tối) $\rightarrow$ Trả về mặt nạ rỗng (hủy kết quả).
    \item GradCAM tự động kích hoạt, xuất ra một điểm $\mathbf{c}_{\text{rescue}}$ có độ kích hoạt cực đại.
    \item Hệ thống lấy điểm $\mathbf{c}_{\text{rescue}}$ làm tâm mới, tái sử dụng kích thước bounding box ban đầu, và chạy IPR để xuất ra mặt nạ gợi ý.
\end{enumerate}

\begin{table}[htbp]
    \centering
    \caption{Khả năng phục hồi hệ số Dice khi áp dụng chuỗi cứu hộ GradCAM + IPR}
    \label{tab:gradcam_rescue}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Nhóm (theo Dice IPR $k{=}1$)} & \textbf{N} & \textbf{Dice trước} & \textbf{Dice $k{=}1$} & \textbf{CBL $k{=}1$} \\
        \hline
        GradCAM tốt \quad ($D \geq 0.90$) &  9 & 0.000 & \textbf{0.922} & \textbf{0.961} \\
        \hline
        GradCAM khá \quad ($0.70 \leq D < 0.90$) & 29 & 0.000 & \textbf{0.807} & \textbf{0.918} \\
        \hline
        GradCAM trung bình \; ($0.50 \leq D < 0.70$) & 19 & 0.000 & 0.598 & 0.811 \\
        \hline
        GradCAM yếu \quad ($D < 0.50$) & 117 & 0.000 & 0.018 & 0.066 \\
        \hline
        \textbf{Tổng / Trung bình} & \textbf{174} & \textbf{0.000} & 0.260 & 0.298 \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize
        Thí nghiệm: 174 ảnh có góc tối $\geq 70\%$ đen, câu nhắc đặt vào góc tối → model từ chối (\textit{is\_suspicious}=True) 100\% trường hợp. GradCAM tự động trích điểm cứu hộ; IPR chạy 5 vòng. \textit{Cứu hộ thành công} ($D \geq 0.70$): 38/174 = \textbf{21.8\%}.
    \end{flushleft}
\end{table}

\textbf{Phân tích:}
Bảng~\ref{tab:gradcam_rescue} phản ánh hai thành phần tách biệt của cơ chế phòng vệ. Thứ nhất, \textbf{tỷ lệ phát hiện đạt 100\%} (174/174): bộ kiểm duyệt nhận diện và từ chối toàn bộ câu nhắc đặt vào vùng tối, đảm bảo hệ thống không bao giờ trả về mặt nạ sai một cách im lặng. Thứ hai, \textbf{chất lượng cứu hộ phụ thuộc vào độ chính xác của GradCAM}: ở 38/174 mẫu ($\approx 21.8\%$) mà GradCAM trỏ đúng vùng tổn thương (CBL đỉnh $\geq 0.70$), IPR phục hồi được Dice trung bình $\geq 0.807$. Tuy nhiên, 117/174 mẫu ($67.2\%$) GradCAM không tìm ra tín hiệu bệnh lý — nguyên nhân phân tích tại mục tiếp theo.

\subsection{Phân tích giới hạn GradCAM trong kịch bản câu nhắc sai hoàn toàn}

Trong 117/174 mẫu ($67.2\%$), GradCAM trỏ sai vùng (CBL đỉnh thấp, Dice $k{=}1 < 0.50$). Đây là kịch bản khó nhất: prompt đặt vào góc tối $\geq 70\%$ đen — không có bất kỳ cấu trúc xương nào trong vùng câu nhắc. Nhóm xác định hai nguyên nhân chính:

\begin{enumerate}
    \item \textbf{GradCAM gợi ý sai vị trí:} Các dị vật trên ảnh (thiết bị cố định xương, ký tự nhiễu còn sót) hoặc các cấu trúc giải phẫu có gradient mạnh (bờ khớp xương lớn) tạo ra điểm kích hoạt cực đại cao hơn vùng tổn thương thực sự, dẫn đến điểm neo cứu hộ $\mathbf{c}_{\text{rescue}}$ bị đặt sai.
    \item \textbf{IPR không hội tụ trong 3 vòng lặp:} Khi điểm khởi tạo từ GradCAM nằm quá xa vùng tổn thương, ba vòng lặp IPR không đủ để dịch chuyển tâm hình học. Giới hạn cứng $N = 3$ là sự đánh đổi cần thiết cho yêu cầu thời gian thực (${\sim}180$ ms).
\end{enumerate}

\subsection{So sánh khả năng phòng vệ: PGA-UNet và SAM-Med2D}

Để đánh giá tính độc đáo của cơ chế phòng vệ tích hợp, thí nghiệm bổ sung áp đặt cùng 174 câu nhắc góc tối lên SAM-Med2D (fine-tuned), kiểm duyệt đầu ra bằng \textbf{cùng bộ 5 tiêu chí} đã dùng cho PGA.

SAM-Med2D cũng đạt tỷ lệ phát hiện \textbf{100\%} (174/174) — bộ tiêu chí kiểm duyệt là ngôn ngữ chung áp dụng được cho bất kỳ mô hình nào. Đỉnh kích hoạt GradCAM trên encoder ViT-B của SAM đạt CBL trung bình \textbf{0.497}, cao hơn PGA (0.298) — encoder pretrain trên 4M+ ảnh y tế có khả năng định vị tốt hơn. Tuy nhiên, SAM \textbf{không có cơ chế tích hợp} để khai thác tín hiệu GradCAM này: không có IPR, không có bộ kiểm duyệt tự động, không từ chối output nguy hiểm. PGA-UNet tích hợp toàn bộ pipeline \textit{Detection $\rightarrow$ GradCAM $\rightarrow$ IPR} thành hệ thống phòng vệ end-to-end — đây là đóng góp kỹ thuật phân biệt PGA với các mô hình prompt-based hiện có.

% ------------------------------------------------------------
\section{Đánh giá mô hình phân lớp gác cổng (MobileNetV4)}
\label{sec:danh_gia_phan_lop}
% ------------------------------------------------------------
% ------------------------------------------------------------

Mô hình phân lớp đóng vai trò là "người gác cổng" (gatekeeper) trong luồng xử lý đầu cuối của phần mềm. Nhiệm vụ của mô hình là sàng lọc tự động ảnh X-quang đầu vào thành hai nhóm: Bình thường (Âm tính) và Có bệnh lý bất thường (Dương tính). Việc đánh giá chính xác hiệu năng của module này mang ý nghĩa quyết định, bởi một dự đoán sai sót ở bước này có thể khiến bệnh nhân bị lọt lưới sàng lọc mà không bao giờ đi tới được bước phân đoạn chi tiết phía sau.

\textbf{Các độ đo đánh giá (Evaluation Metrics):}
Để có cái nhìn toàn diện, mô hình được đánh giá qua các độ đo tiêu chuẩn của bài toán phân loại nhị phân: Độ chính xác tổng thể (Accuracy), Độ chính xác (Precision), Độ bao phủ/Độ nhạy (Recall/Sensitivity), Độ đặc hiệu (Specificity) và Điểm F1 (F1-Score). 

Đặc biệt trong bối cảnh y tế dự phòng và sàng lọc, \textbf{Độ nhạy (Recall)} là chỉ số được ưu tiên hàng đầu. Một mô hình sàng lọc tốt thà báo động nhầm (False Positive) để bác sĩ tốn thêm chút thời gian kiểm tra lại ở bước phân đoạn, còn hơn là bỏ sót bệnh nhân thực sự có khối u (False Negative).

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá hiệu năng của mô hình phân lớp sàng lọc trên tập kiểm thử}
    \label{tab:classification_results}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Độ đo đánh giá (Metrics)} & \textbf{Kết quả} \\
        \hline
        Độ chính xác tổng thể (Accuracy) & \textbf{85.77\%} \\
        \hline
        Độ chính xác (Precision / PPV) & \textbf{83.11\%} \\
        \hline
        Độ nhạy (Recall / Sensitivity) & \textbf{89.64\%} \\
        \hline
        Độ đặc hiệu (Specificity) & \textbf{81.91\%} \\
        \hline
        Giá trị dự báo Âm tính (NPV) & \textbf{88.85\%} \\
        \hline
        Điểm F1 (F1-Score) & \textbf{86.25\%} \\
        \hline
        Diện tích dưới đường cong (AUC-ROC) & \textbf{0.9514} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Phân tích kết quả:}
Bảng~\ref{tab:classification_results} tổng hợp hiệu năng của mô hình MobileNetV4-Hybrid-Medium (37.88 MB, $\approx$9.8 triệu tham số) sau quá trình tinh chỉnh hai giai đoạn trên BTXRD. Các chỉ số chứng minh module Gatekeeper hoạt động ổn định và đáng tin cậy trong bối cảnh y tế:
\begin{itemize}
    \item \textbf{Recall 89.64\%:} Chỉ số ưu tiên số một trong sàng lọc y tế — mô hình bắt được $\approx$90\% ca bệnh thực sự, đảm bảo bệnh nhân không bị bỏ sót trước khi đến bước phân đoạn.
    \item \textbf{AUC-ROC 0.9514:} Diện tích dưới đường cong ROC đạt 0.95 phản ánh khả năng phân biệt bệnh/không bệnh xuất sắc, bất kể ngưỡng phân loại được chọn.
    \item \textbf{Specificity 81.91\%:} Trong 5 ca bình thường, hệ thống loại đúng $\approx$4 ca, hạn chế việc chuyển ảnh lành sang module phân đoạn (tiết kiệm tài nguyên tính toán).
    \item \textbf{NPV 88.85\%:} Khi hệ thống kết luận "bình thường", xác suất thực sự không có bệnh là 88.85\% — đủ tin cậy để sử dụng như bộ lọc sơ cấp.
    \item \textbf{F1-score 86.25\%:} Trị số F1 cao và cân bằng, phản ánh mô hình không bị lệch quá nhiều về bất kỳ loại lỗi nào.
\end{itemize}
Hình~\ref{fig:confusion_matrix} trực quan hóa Ma trận nhầm lẫn và Đường cong ROC của mô hình.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/classification_evaluation.png}
    \caption{Đánh giá trực quan mô hình phân lớp MobileNetV4: (a) Ma trận nhầm lẫn (Confusion Matrix), (b) Đường cong ROC (AUC = 0.9514) và (c) Phân phối xác suất dự đoán.}
    \label{fig:confusion_matrix}
\end{figure}

Việc mô hình phân lớp đạt AUC-ROC = 0.9514 và Recall = 89.64\% chính là bản lề vững chắc để toàn bộ hệ thống tự tin chuyển giao quyền điều khiển sang cho module phân đoạn tương tác PGA-UNet trong luồng thực thi (Online Inference Pipeline).

% ------------------------------------------------------------
\section{Tích hợp và đánh giá ứng dụng lâm sàng (app.py)}
\label{sec:app_integration}
% ------------------------------------------------------------

Để chứng minh tính khả thi của giải pháp đề xuất, toàn bộ các module (tiền xử lý, phân lớp gác cổng, phân đoạn PGA-UNet, và luồng cứu hộ thông minh GradCAM kết hợp IPR) đã được đóng gói và tích hợp vào một ứng dụng phần mềm duy nhất (đại diện bởi tệp lệnh \texttt{app.py}). Ứng dụng này cung cấp một giao diện đồ họa (GUI) trực quan, cho phép các chuyên gia y tế tương tác trực tiếp với mô hình AI mà không cần am hiểu về kỹ thuật lập trình hay học sâu.

\textbf{1. Giao diện và Trải nghiệm người dùng (UI/UX):}
Giao diện phần mềm được thiết kế theo nguyên tắc tối giản, tập trung vào công tác chẩn đoán. Màn hình làm việc chính được chia thành các khu vực rõ ràng: khu vực tải ảnh bệnh nhân, bảng điều khiển trạng thái (báo cáo phân lớp), và không gian hiển thị ảnh X-quang trung tâm. Bác sĩ có thể thao tác phóng to, thu nhỏ và sử dụng chuột để trực tiếp vẽ hộp giới hạn (Bounding Box) lên ảnh với trải nghiệm mượt mà. Kết quả mặt nạ dự đoán và bản đồ nhiệt GradCAM được hiển thị dưới dạng các lớp phủ (overlays) có thể tùy chỉnh độ trong suốt, giúp bác sĩ dễ dàng đối chiếu vùng dự đoán với cấu trúc giải phẫu gốc.

\textbf{2. Luồng thực thi chẩn đoán (Inference Pipeline):}
Ứng dụng thực thi chính xác luồng chẩn đoán đã được định nghĩa ở Mục 1.3.2 và Chương 3:
\begin{itemize}
    \item Ngay khi ảnh X-quang được tải lên, module tiền xử lý và mô hình phân lớp sẽ tự động chạy ngầm. Nếu kết quả là âm tính, ứng dụng hiển thị thông báo "Bình thường" và kết thúc luồng kiểm tra.
    \item Nếu kết quả là dương tính (có u/tổn thương), ứng dụng bật chế độ tương tác và chờ bác sĩ vẽ câu nhắc.
    \item Tùy thuộc vào độ chính xác thao tác tay của bác sĩ, hệ thống sẽ rẽ nhánh xử lý. Đáng chú ý nhất, ở các kịch bản vẽ sai lệch hoàn toàn, giao diện ứng dụng lập tức từ chối trả về mặt nạ phân đoạn, đồng thời rọi bản đồ nhiệt GradCAM và kích hoạt IPR để sinh ra "Mặt nạ gợi ý" hiển thị song song. Hệ thống sẽ ở trạng thái chờ bác sĩ bấm xác nhận "Đồng ý" hoặc chọn "Vẽ lại", tôn trọng tuyệt đối quyền quyết định cuối cùng của con người.
\end{itemize}

\textbf{3. Đánh giá thời gian phản hồi (Real-time Performance):}
Khả năng triển khai trong môi trường lâm sàng thực tế phụ thuộc rất lớn vào độ trễ (latency) của phần mềm. Các thử nghiệm đánh giá thời gian phản hồi trên thiết bị máy tính thông thường cho kết quả rất khả quan:
\begin{itemize}
    \item Quá trình tiền xử lý và phân lớp nhanh chóng, mất khoảng 50 ms.
    \item Thời gian thực thi một suy luận phân đoạn (PGA-UNet forward pass) từ lúc nhận câu nhắc đến khi ra mặt nạ chỉ mất khoảng 45 ms.
    \item Ngay cả trong kịch bản cứu hộ phức tạp nhất (Kịch bản C) yêu cầu chạy GradCAM và lặp 3 vòng IPR, tổng thời gian phản hồi từ lúc bác sĩ nhả chuột đến khi hiển thị "Mặt nạ gợi ý" đạt khoảng ${\sim}180$ ms, vẫn nằm trong mốc giới hạn $200$ ms.
\end{itemize}
Với thời gian đáp ứng này, hệ thống hoàn toàn đạt tiêu chuẩn tương tác thời gian thực (real-time interaction), đảm bảo quy trình làm việc của bác sĩ diễn ra liền mạch, không gặp tình trạng giật lag hay phải chờ đợi kết quả xử lý.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/app_interface.png}
    \caption{Giao diện ứng dụng hỗ trợ chẩn đoán (\texttt{app.py}) minh họa luồng xử lý tương tác giữa bác sĩ và hệ thống AI}
    \label{fig:app_interface}
\end{figure}

Tóm lại, ứng dụng phần mềm không chỉ là một bản demo kỹ thuật đơn thuần, mà là một minh chứng trọn vẹn cho triết lý thiết kế "Human-in-the-loop": Trí tuệ nhân tạo không nhằm mục đích thay thế chuyên gia, mà đóng vai trò như một người trợ lý thông minh, sẵn sàng hỗ trợ, nhắc nhở và làm việc một cách minh bạch trong toàn bộ quá trình chẩn đoán y khoa.