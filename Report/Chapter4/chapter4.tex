% ==========================================
% CHƯƠNG 4: THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ
% ==========================================
% Biến số liệu, cập nhật sau khi có kết quả mới
\newcommand{\Ntest}{232}           % N test samples (per-polygon)
\newcommand{\Ntestimg}{187}        % N test images  (merged mask, UNet)
\newcommand{\Ntrain}{1\,493}       % N train samples
\newcommand{\Nval}{187}            % N val samples

% -- Bộ dữ liệu FracAtlas (đánh giá tổng quát hóa liên miền) --
\newcommand{\NtestFA}{92}          % N test samples FracAtlas (per-polygon)
\newcommand{\NtestimgFA}{72}       % N test images FracAtlas (image-level)
\newcommand{\NtrainFA}{730}        % N train samples FracAtlas (per-polygon)
\newcommand{\NvalFA}{95}           % N val samples FracAtlas (per-polygon)

% ==========================================
\chapter{THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ}
\label{Chapter4}

Chương này tổ chức kết quả thực nghiệm thành hai phần. \textbf{Phần I} tập trung đánh giá kiến trúc PGA-UNet trên tập kiểm thử \Ntestimg~ảnh (image-level): so sánh với các mô hình cơ sở và SAM-Med2D (đánh giá image-level, N=\Ntestimg~ảnh), ablation study (PSG/CAD), đánh giá tính bền bỉ (Robustness) và kiểm định xác thực chéo. \textbf{Phần II} trình bày đánh giá module Gatekeeper hỗ trợ (EfficientNet\_B3) và pipeline end-to-end trên dữ liệu hỗn hợp thực tế.

% ============================================================
% PHẦN I, ĐÓNG GÓP 1: Nghiên cứu kiến trúc PGA-UNet
% ============================================================

% ------------------------------------------------------------
\section{Thiết lập môi trường thực nghiệm}
\label{sec:thiet_lap}
% ------------------------------------------------------------

Để đảm bảo tính khách quan và khả năng tái lập (reproducibility) của các kết quả nghiên cứu, mục này trình bày chi tiết về quy mô dữ liệu, phương pháp phân chia các tập đánh giá, cấu hình phần cứng cũng như các siêu tham số (hyperparameters) được thiết lập trong suốt quá trình huấn luyện mô hình.

\subsection{Tập dữ liệu ảnh X-quang (Dataset)}
\label{subsec:dataset}

Dữ liệu thực nghiệm được sử dụng trong khóa luận là bộ \textbf{BTXRD} (\textit{Bone Tumor X-Ray Dataset})~\cite{btxrd2024}, một tập ảnh X-quang xương công khai có đầy đủ ba cấp nhãn: phân lớp, định vị và phân đoạn pixel-level.

\textbf{Lý do chọn BTXRD:} Không giống các bộ dữ liệu X-quang công khai chỉ cung cấp nhãn phân lớp mà thiếu mặt nạ phân đoạn, BTXRD cung cấp đồng thời cả ba cấp nhãn (phân lớp, định vị, phân đoạn pixel-level), đáp ứng đầy đủ yêu cầu huấn luyện có giám sát. Ảnh X-quang thô chứa nhiều nhiễu kỹ thuật (ký tự R/L, metadata) và đã trải qua quy trình tiền xử lý bằng YOLOv11s như mô tả ở Mục~\ref{sec:tien_xu_ly}.

\textbf{Hai phân vùng dữ liệu song song:} Kiến trúc hệ thống sử dụng hai tập dữ liệu với mục đích khác nhau:
\begin{itemize}
    \item \textbf{Dữ liệu phân đoạn} (cho PGA-UNet và baseline): Chỉ chứa các ảnh \textit{có bệnh lý} kèm mặt nạ phân đoạn pixel-level. Đây là giới hạn tự nhiên của học có giám sát, chỉ ảnh có nhãn mới dùng được.
    \item \textbf{Dữ liệu phân lớp} (cho Gatekeeper): Chứa cả ảnh bệnh lý lẫn ảnh bình thường, phân chia theo tỷ lệ 80/10/10 (train/val/test), batch size = 16.
\end{itemize}

\textbf{Thống kê dữ liệu:} Bảng~\ref{tab:dataset_stats} tóm tắt phân bố dữ liệu sau khi phân chia.

\begin{table}[htbp]
    \centering
    \caption{Thống kê phân bố bộ dữ liệu thực nghiệm}
    \label{tab:dataset_stats}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|p{2.6cm}|c|p{3.5cm}|p{5.5cm}|}
        \hline
        \textbf{Tập dữ liệu} & \textbf{Số ảnh} & \textbf{Số mẫu (per-polygon)} & \textbf{Ghi chú} \\
        \hline
        Huấn luyện (Train) & \Ntrain & 1848 & Tất cả ca bệnh lý \\
        \hline
        Xác thực (Validation) & \Nval & 238 & \\
        \hline
        Kiểm thử (Test) & \Ntestimg & \Ntest & Nhiều ảnh có $\geq$2 polygon $\rightarrow$ \Ntest~mẫu per-polygon \\
        \hline
    \end{tabular}
\end{table}

\textbf{Thống kê bộ dữ liệu phân lớp (Gatekeeper):} Bộ BTXRD gồm 3.746 ảnh X-quang (cả lành lặn lẫn bệnh lý) được dùng toàn bộ để huấn luyện mô hình gác cổng, phân chia train/val/test theo tỷ lệ 80/10/10 ($\approx$2.996/375/375~ảnh). Tập phân lớp và tập phân đoạn dùng chung ảnh bệnh lý nhưng độc lập về nhãn: nhãn nhị phân (lành/bệnh) cho Gatekeeper, nhãn mặt nạ pixel-level cho PGA-UNet.

% ------------------------------------------------------------
\subsection{Cấu hình huấn luyện (Hyperparameters, Loss functions)}
\label{subsec:hyperparameters}
% ------------------------------------------------------------

Toàn bộ quá trình huấn luyện và suy luận thực nghiệm được triển khai trên môi trường Kaggle/Colab với GPU T4 nhằm tăng tốc độ tính toán. Các mô hình được cài đặt dựa trên thư viện PyTorch.

\textbf{Hàm mất mát (Loss Function):}
Để xử lý mất cân bằng giữa foreground (vùng tổn thương) và background, hệ thống sử dụng hàm mất mát kết hợp:
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{BCE} + \mathcal{L}_{Dice}
\end{equation}
Thành phần $\mathcal{L}_{BCE}$ tối ưu phân loại từng pixel; $\mathcal{L}_{Dice}$ tối ưu độ chồng lấp diện tích tổng thể.

\textbf{Siêu tham số huấn luyện:}
Tất cả mô hình (U-Net, PGA-UNet) được huấn luyện dưới cùng thiết lập:
\begin{itemize}
    \item \textbf{Batch size:} 4 (phù hợp giới hạn VRAM khi dùng ảnh 512$\times$512).
    \item \textbf{Epochs tối đa:} 100, kết hợp Early Stopping (patience=15).
    \item \textbf{Optimizer:} AdamW với weight decay $10^{-4}$.
    \item \textbf{Learning Rate:} Khởi điểm $10^{-4}$, bộ lập lịch \texttt{ReduceLROnPlateau} (giảm 0.5× sau 5 epoch không cải thiện Dice xác thực).
\end{itemize}

\textbf{Siêu tham số kiến trúc PGA-UNet (chi tiết triển khai):}
Các giá trị dưới đây là tham số cấu hình được xác định qua thực nghiệm trên tập xác thực; chúng là chi tiết triển khai, không phải đóng góp kỹ thuật cần kiểm chứng độc lập.
\begin{itemize}
    \item \textbf{Hệ số $w_l$ theo tầng giải mã (CAD)}: $\{1.0,\ 0.7,\ 0.4,\ 0.2\}$ từ tầng 1 đến 4; lịch trình giảm dần này là lựa chọn thử nghiệm thực nghiệm, chưa có kiểm chứng ablation riêng về lịch trình tối ưu.
    \item \textbf{Hệ số kết nối dư (skip residual)} $\lambda = 0.3$ trong khối Conditioned Attention.
    \item \textbf{Kernel Gaussian Heatmap:} $k = 31$, padding 5~px mỗi cạnh hộp giới hạn.
    \item \textbf{Khởi tạo $\alpha_{gate}$:} $0.1$ (Prompt Spatial Gate), giúp ổn định huấn luyện giai đoạn đầu.
\end{itemize}

% ------------------------------------------------------------
\subsection{Phương pháp đánh giá image-level}
\label{subsec:image_level_eval}
% ------------------------------------------------------------

\textbf{Đặc điểm annotation của tập BTXRD.}
Trong tập kiểm thử, nhiều ảnh X-quang chứa \textit{nhiều hơn một vùng tổn thương}: bác sĩ vẽ từng polygon riêng biệt cho mỗi tổn thương, dẫn đến tổng số \Ntest~mẫu polygon trên \Ntestimg~ảnh (trung bình $\sim$1.24 polygon/ảnh). Nếu tính Dice riêng cho từng polygon, một ảnh có hai tổn thương sẽ đóng góp hai mẫu vào tập đánh giá, làm méo phân phối kết quả và tính nhiều lần trên cùng một bệnh nhân.

\textbf{Cách tính image-level (đánh giá theo ảnh).}
Để khắc phục vấn đề trên và phản ánh trung thực hơn hiệu năng lâm sàng (một điểm số trên mỗi ảnh bệnh nhân), luận văn áp dụng phương pháp \textit{image-level evaluation} sau cho PGA-UNet:

\begin{enumerate}
    \item \textbf{Hợp nhất ground-truth (Union GT):} Với mỗi ảnh chứa $K \geq 1$ polygon, tất cả mask nhị phân GT $\{G_1, G_2, \ldots, G_K\}$ được \textit{hợp nhất} thành một mask GT duy nhất theo phép hợp tập:
    \begin{equation}
        M_{\text{GT}} = G_1 \cup G_2 \cup \cdots \cup G_K
    \end{equation}

    \item \textbf{Kết hợp dự đoán (Max-Merge Prediction):} Mô hình được chạy suy luận riêng cho từng bbox $\mathbf{B}_k$ tương ứng, sinh ra $K$ mask xác suất $\{\hat{P}_1, \hat{P}_2, \ldots, \hat{P}_K\}$ (giá trị $[0,1]$ mỗi pixel). Các mask xác suất được kết hợp bằng phép lấy \textit{giá trị lớn nhất theo pixel} (pixel-wise maximum):
    \begin{equation}
        \hat{M}_{\text{max}}(x,y) = \max_{k=1}^{K}\,\hat{P}_k(x,y)
    \end{equation}
    Sau đó ngưỡng hóa tại $0.5$ để thu mặt nạ nhị phân cuối cùng $\hat{M}_{\text{pred}} = \mathbf{1}[\hat{M}_{\text{max}} \geq 0.5]$.

    \item \textbf{Tính Dice image-level:} Chỉ số Dice được tính một lần duy nhất giữa $\hat{M}_{\text{pred}}$ và $M_{\text{GT}}$:
    \begin{equation}
        \text{Dice}_{\text{img}} = \frac{2\,|M_{\text{GT}} \cap \hat{M}_{\text{pred}}|}{|M_{\text{GT}}| + |\hat{M}_{\text{pred}}|}
        \label{eq:dice_img}
    \end{equation}
\end{enumerate}

Metric cuối cùng được tính trung bình trên toàn bộ \Ntestimg~ảnh kiểm thử. Phương pháp này đảm bảo mỗi ảnh bệnh nhân chỉ đóng góp \textbf{một điểm số}, tránh hiện tượng ảnh có nhiều tổn thương bị đếm nhiều lần và phản ánh trung thực hơn trải nghiệm đánh giá lâm sàng thực tế.

% \textbf{Ghi chú so sánh với SAM-Med2D.}
% Do giới hạn kỹ thuật của cách triển khai SAM-Med2D (mỗi lần chạy nhận một bbox duy nhất), mô hình này được đánh giá theo cơ chế \textit{per-polygon} (N=\Ntest~mẫu), tức là tính Dice riêng biệt cho từng polygon. Sự khác biệt về đơn vị đánh giá (image-level vs.\ per-polygon) cần được lưu ý khi so sánh hai mô hình.

% ============================================================
\section{Đánh giá hiệu năng kiến trúc PGA-UNet}
\label{sec:danh_gia_hieu_nang}
% ============================================================

Để đo lường khách quan năng lực của kiến trúc PGA-UNet, quá trình thực nghiệm được tiến hành qua ba bước: (1)~so sánh với mô hình cơ sở không có prompt (U-Net); (2)~đánh giá tính bền bỉ trước sai số câu nhắc; (3)~so sánh với SAM-Med2D (SOTA prompt-based). PGA-UNet và SAM-Med2D được đánh giá ở cấp độ ảnh (image-level, N=\Ntestimg~ảnh).

% ------------------------------------------------------------
\subsection{Đánh giá cơ sở (Baseline Comparison): U-Net vs.\ PGA-UNet}
% ------------------------------------------------------------

Trong kịch bản đánh giá đầu tiên, PGA-UNet (kịch bản câu nhắc Zoom-out lý tưởng) được so sánh với U-Net hoạt động hoàn toàn tự động (phân đoạn không có prompt).

\textbf{Điều kiện so sánh:}
Về mặt kiến trúc, PGA-UNet được xây dựng trên khung encoder-decoder của U-Net, kế thừa cơ chế Attention Gate từ Attention U-Net~\cite{oktay2018attentionunet} rồi mở rộng thành PSG+CAD có điều kiện hóa bởi prompt (chi tiết tại Chương~\ref{Chapter3}). U-Net thuần túy (không có attention, không có prompt) được chọn làm baseline định lượng để đo mức đóng góp tổng thể của toàn bộ chuỗi cải tiến. U-Net hoạt động theo cơ chế \textit{tự động hoàn toàn} (chỉ nhận ảnh thô), trong khi PGA-UNet hoạt động \textit{tương tác} (nhận thêm bounding box prompt). Sự chênh lệch hiệu năng phản ánh giá trị của việc tích hợp cơ chế chú ý có điều kiện và tri thức định hướng từ bác sĩ vào kiến trúc CNN. Đây là so sánh có chủ đích: minh chứng lợi ích của hướng tiếp cận prompt-guided so với segmentation tự động trên ảnh X-quang xương.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng phân đoạn: U-Net (tự động, N=\Ntestimg~ảnh) so với PGA-UNet kịch bản Zoom-out lý tưởng (prompt-guided, N=\Ntestimg~ảnh, image-level)}
    \label{tab:baseline_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        U-Net & 0.4790 & 0.3815 & 0.6245 & 0.5184 & 146.06 & 0.5906 \\
        \hline
        \textbf{PGA-UNet (Zoom-out)} & \textbf{0.8584} & \textbf{0.7617} & \textbf{0.8526} & \textbf{0.8817} & \textbf{14.80} & \textbf{0.9500} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích kết quả so sánh baseline:}
\begin{itemize}
    \item \textbf{Giới hạn của phân đoạn tự động:} U-Net gốc đạt Dice = 0.4790, phản ánh giới hạn cốt lõi khi phải tự tìm vùng tổn thương trên ảnh X-quang thô mà không có bất kỳ thông tin định hướng nào. Kết quả này là minh chứng cho thách thức của phân đoạn hoàn toàn tự động trên ảnh X-quang xương có độ tương phản thấp và cấu trúc giải phẫu chồng lấp.
    \item \textbf{Giá trị của hướng tiếp cận tương tác:} PGA-UNet (kịch bản Zoom-out lý tưởng) đạt Dice = 0.8584, vượt trội hoàn toàn so với U-Net tự động ($+$0.3794). Kết quả này cho thấy cơ chế PSG và CAD chỉ phát huy hiệu quả khi được ``neo'' bởi tín hiệu định hướng rõ ràng từ bên ngoài (prompt), giải quyết đúng hạn chế cốt lõi của phân đoạn tự động. Đáng lưu ý, cả hai mô hình đều hoạt động trên ảnh đã resize về $512\times512$ (từ ảnh gốc BTXRD 2.000--3.000~px, tương đương còn $\frac{1}{4}$--$\frac{1}{6}$ diện tích gốc). Kết quả so sánh cho thấy: ngay cả khi thông tin không gian đầu vào bị thu nhỏ đáng kể, tín hiệu câu nhắc bounding box vẫn đủ để PGA-UNet phục hồi hiệu năng phân đoạn, minh chứng trực tiếp cho giá trị của cơ chế câu nhắc trực quan trong điều kiện độ phân giải bị giới hạn.
\end{itemize}

\subsection{Đánh giá tính bền bỉ (Robustness): Kịch bản Zoom-out, Shift và Mixed}

Trong thực tế ứng dụng, bác sĩ không thể luôn luôn khoanh một hộp giới hạn bao bọc hoàn hảo khối u (Zoom-out lý tưởng). Sẽ có những lúc mỏi mắt hoặc do góc nhìn chủ quan khiến khung khoanh bị lệch đi một phần so với vị trí trung tâm thực sự. Để kiểm chứng xem PGA-UNet có bị phụ thuộc quá mức vào vị trí tâm câu nhắc hay không, hệ thống được thử nghiệm trên 3 kịch bản:
\begin{enumerate}
    \item \textbf{100\% Zoom-out:} Câu nhắc lý tưởng, bao trọn vẹn tổn thương, cận trên hiệu năng của mô hình.
    \item \textbf{100\% Shift (Lệch tâm):} Câu nhắc bị cố tình dịch chuyển lệch khỏi tâm tổn thương một lượng ngẫu nhiên, điều kiện xấu nhất thực tế, mô phỏng bác sĩ mất tập trung.
    \item \textbf{Mixed (70\% Zoom-out + 30\% Shift):} Pha trộn theo tỷ lệ ước lượng trong lâm sàng thực tế, phần lớn câu nhắc đúng, một phần nhỏ bị lệch.
\end{enumerate}

\textbf{Lý do chọn 3 kịch bản này và không dùng nhiễu mạnh hơn:} Ba mức độ bao phủ toàn bộ phổ sai số câu nhắc hiện thực trong lâm sàng từ hoàn hảo đến lệch tâm. Kịch bản Shift được thiết kế đảm bảo ít nhất 30\% diện tích hộp giới hạn giao với vùng tổn thương thực (\texttt{overlap >= 30\%}), phản ánh sai số thực tế của bác sĩ: bác sĩ luôn có thông tin sơ bộ về vùng nghi ngờ, sai số lâm sàng thường là lệch tâm, không phải sai hoàn toàn vị trí. Kịch bản Shift 100\% đã đại diện cho mức nhiễu cực đoan nhất khả thi trong thực tế và cho thấy PGA-UNet vẫn duy trì Dice = 0.8454, đủ để kết luận về tính bền bỉ.

\textbf{Trường hợp câu nhắc sai hoàn toàn vị trí:} Nếu bác sĩ khoanh hộp hoàn toàn không liên quan đến vùng tổn thương thực, PGA-UNet (như mọi hệ thống phân đoạn tương tác) sẽ phân đoạn tại vùng được chỉ định thay vì vùng tổn thương thực. Đây là hành vi đúng về mặt thiết kế: mô hình thực hiện đúng chỉ dẫn của bác sĩ; nếu mô hình có thể tự sửa hướng dẫn sai của bác sĩ thì cơ chế tương tác mất đi ý nghĩa và bác sĩ trở thành thừa. Trách nhiệm định hướng đúng thuộc về bác sĩ; mô hình đảm nhiệm việc phân đoạn chính xác trong vùng được chỉ định. Vì lý do này, kịch bản bbox sai hoàn toàn không được đưa vào đánh giá định lượng.


\begin{table}[htbp]
    \centering
    \caption{Đánh giá tính bền bỉ của PGA-UNet trên 3 kịch bản prompt (N=\Ntestimg~ảnh, image-level)}
    \label{tab:robustness_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản prompt} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        Zoom-out (100\%, lý tưởng) & 0.8584 & 0.7617 & 0.8526 & 0.8817 & 14.80 & 0.9500 \\
        \hline
        Shift (100\%, lệch tâm) & 0.8454 & 0.7432 & 0.8462 & 0.8630 & 13.89 & 0.9435 \\
        \hline
        Mixed (70\% Zoom-out + 30\% Shift) & 0.8554 & 0.7571 & 0.8531 & 0.8762 & 15.41 & 0.9474 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích tính bền bỉ:}
Số liệu thực nghiệm (Bảng~\ref{tab:robustness_comparison}) khẳng định đặc tính "hướng dẫn mềm" (soft guidance) của kiến trúc PGA-UNet. Khi đối mặt với kịch bản câu nhắc lệch tâm hoàn toàn (100\% Shift), hiệu năng phân đoạn chỉ giảm từ Dice = 0.8584 (Zoom-out) xuống Dice = 0.8454 (Shift), chênh lệch 0.0130, không đáng kể. Kịch bản Mixed (70\% Zoom-out + 30\% Shift) đạt Dice = 0.8554, xác nhận mô hình duy trì hiệu năng ổn định trong môi trường thực tế.

Kết quả cho thấy mô hình \textbf{không} ghi nhớ cứng nhắc vị trí tâm bản đồ nhiệt câu nhắc. Thay vào đó, PGA-UNet sử dụng câu nhắc để thu hẹp vùng tìm kiếm, nhưng ở các lớp giải mã cuối, mạng dựa vào đặc trưng gradient thực tế của ảnh (bờ xương, đường viền u) để xác định ranh giới. Tính bền bỉ này là một yếu tố quan trọng khi xét đến khả năng triển khai hệ thống trong thực tế lâm sàng.

\subsection{So sánh với SAM-Med2D (SOTA Prompt-based)}
\label{subsec:sam_comparison}

Để đánh giá đầy đủ hơn, mô hình PGA-UNet được so sánh trực tiếp với SAM-Med2D~\cite{2023-SAMMed2D-Cheng}, mô hình SOTA prompt-based dựa trên kiến trúc ViT-B ($\sim$91M tham số~\cite{2023-SAMMed2D-Cheng}, fine-tuned từ 4.6M ảnh y tế 2D). PGA-UNet và SAM-Med2D được đánh giá ở cấp độ image-level (N=\Ntestimg~ảnh); cùng 3 kịch bản prompt.

\textbf{Lưu ý điều kiện so sánh về độ phân giải:} SAM-Med2D bắt buộc sử dụng độ phân giải $256\times256$ do position embedding của ViT-B được cố định theo kích thước này bởi tác giả gốc; muốn chạy ở độ phân giải lớn hơn đòi hỏi huấn luyện lại từ đầu với chi phí rất lớn. Ngược lại, PGA-UNet là kiến trúc CNN thuần túy, hoàn toàn linh hoạt về kích thước đầu vào và được chọn chạy ở $512\times512$ để tận dụng tối đa thông tin không gian của ảnh X-quang. Sự chênh lệch độ phân giải này là \textit{hạn chế cố hữu của SAM-Med2D trong môi trường ảnh y tế chi tiết cao}, không phải điều kiện thực nghiệm do nhóm áp đặt; do đó các kết luận về hiệu năng nên được đọc trong ngữ cảnh này thay vì so sánh trực tiếp như kiểm định công bằng tuyệt đối.

\begin{table}[htbp]
    \centering
    \caption{So sánh toàn diện PGA-UNet với SAM-Med2D fine-tuned và SAM-Med2D zero-shot trên 3 kịch bản prompt. PGA-UNet: image-level (N=\Ntestimg~ảnh); SAM-Med2D: image-level (N=\Ntestimg~ảnh). HD95* chuẩn hóa theo kích thước ảnh: PGA÷512, SAM÷256.}
    \label{tab:sam_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Pre↑} & \textbf{Rec↑} & \textbf{HD95*(norm)↓} & \textbf{CBL↑} \\
        \hline
        PGA-UNet & Zoom-out & \textbf{0.8584} & \textbf{0.7617} & \textbf{0.8526} & \textbf{0.8817} & \textbf{0.029} & \textbf{0.9500} \\
        PGA-UNet & Shift & \textbf{0.8454} & \textbf{0.7432} & \textbf{0.8462} & \textbf{0.8630} & \textbf{0.027} & \textbf{0.9435} \\
        PGA-UNet & Mixed & \textbf{0.8554} & \textbf{0.7571} & \textbf{0.8531} & \textbf{0.8762} & \textbf{0.030} & \textbf{0.9474} \\
        \hline
        SAM-Med2D (FT) & Zoom-out & 0.7432 & 0.6238 & 0.7634 & 0.7570 & 0.035 & 0.8919 \\
        SAM-Med2D (FT) & Shift & 0.7159 & 0.5901 & 0.7480 & 0.7187 & 0.037 & 0.8759 \\
        SAM-Med2D (FT) & Mixed & 0.7355 & 0.6136 & 0.7605 & 0.7462 & 0.036 & 0.8884 \\
        \hline
        SAM-Med2D (ZS) & Zoom-out & 0.5438 & 0.4023 & 0.4743 & 0.6776 & 0.068 & 0.8428 \\
        SAM-Med2D (ZS) & Shift & 0.5262 & 0.3842 & 0.4569 & 0.6531 & 0.083 & 0.8006 \\
        SAM-Med2D (ZS) & Mixed & 0.5408 & 0.3983 & 0.4705 & 0.6726 & 0.073 & 0.8303 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích kết quả so sánh PGA vs SAM-Med2D:}
\begin{itemize}
    \item \textbf{PGA vượt trội nhất quán:} Trên kịch bản Zoom-out lý tưởng, PGA-UNet đạt Dice = 0.8584, vượt SAM-Med2D fine-tuned (Dice = 0.7432) và SAM zero-shot (Dice = 0.5438). Sự chênh lệch lớn giữa SAM fine-tuned và zero-shot cho thấy fine-tuning trên dữ liệu miền đặc thù là cần thiết, và PGA vượt qua cả SAM đã được fine-tuned dù chỉ với $\sim$3M tham số. Điều này đặc biệt có ý nghĩa vì PGA-UNet được huấn luyện \textbf{hoàn toàn từ đầu} trên BTXRD (toàn bộ $\sim$3M tham số tham gia backpropagation, không đóng băng bất kỳ thành phần nào), trong khi SAM-Med2D kế thừa trọng số ViT-B đã học từ 4.6M ảnh y tế 2D, xem Bảng~\ref{tab:training_strategy_compare} tại Chương~\ref{Chapter3} để biết chi tiết.
    \item \textbf{Lợi thế HD95:} Sau chuẩn hóa theo kích thước ảnh, PGA có HD95/imgsize thấp hơn SAM đáng kể, cho thấy PGA bám biên sát hơn.
    \item \textbf{Tham số hiệu quả:} PGA ($\sim$3M, toàn bộ trainable) vượt SAM-Med2D ($\sim$91M tổng, chỉ $\sim$15M trainable gồm Adapter + Decoder), cho thấy kiến trúc nhẹ được thiết kế đặc thù cho bài toán có thể hiệu quả hơn foundation model đa mục đích trên miền dữ liệu hẹp này.
    \item \textbf{SAM hội tụ rất nhanh:} SAM-Med2D hội tụ sớm nhờ lợi thế pretrained từ 4.6M ảnh y tế 2D, nhưng kiến trúc ViT-B với resolution 256 là giới hạn vốn có trên ảnh X-quang chi tiết.
    \item \textbf{PGA bền bỉ hơn SAM trước sai số câu nhắc:} Nhìn vào mức sụt giảm Dice khi chuyển từ kịch bản Zoom-out sang Shift, PGA-UNet chỉ giảm $0.8584 \to 0.8454$ ($-0.0130$, tương đương $-1.5\%$), trong khi SAM-Med2D fine-tuned giảm $0.7432 \to 0.7159$ ($-0.0273$, tương đương $-3.6\%$). Đây là kết quả đáng chú ý: mô hình được thiết kế đặc thù cho bài toán với cơ chế ``hướng dẫn mềm'' (Gaussian heatmap + PSG + CAD) không chỉ vượt trội về điểm tuyệt đối mà còn \textbf{bền bỉ hơn chính mô hình nền tảng prompt-based} (SAM-Med2D) vốn được huấn luyện trên hàng triệu ảnh y tế. Kết quả này gợi ý rằng trên miền bài toán hẹp, thiết kế kiến trúc đặc thù có thể đóng vai trò quan trọng không kém quy mô tham số khi xét đến tính ổn định với sai số câu nhắc.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{images/chart_baseline_sam_comparison.png}
    \caption{So sánh Dice Score giữa U-Net (không prompt), PGA-UNet và SAM-Med2D (fine-tuned, zero-shot) trên ba kịch bản prompt (Zoom-out, Shift, Mixed), tổng hợp trực quan từ Bảng~\ref{tab:baseline_comparison}, \ref{tab:robustness_comparison} và \ref{tab:sam_comparison}.}
    \label{fig:chart_baseline_sam}
\end{figure}

% ------------------------------------------------------------
\subsection{Kiểm chứng công bằng tại cùng độ phân giải 256$\times$256}
\label{subsec:fair256}
% ------------------------------------------------------------

Để loại bỏ yếu tố độ phân giải khỏi so sánh, PGA-UNet được huấn luyện lại từ đầu trên cùng bộ dữ liệu BTXRD nhưng với kích thước đầu vào $256\times256$ (thay vì $512\times512$), tạo ra điều kiện so sánh hoàn toàn công bằng với SAM-Med2D vốn bị giới hạn ở độ phân giải này. Trọng số SAM-Med2D fine-tuned được giữ nguyên; chỉ PGA-UNet được huấn luyện lại.

\begin{table}[htbp]
    \centering
    \caption{So sánh công bằng PGA-UNet (256×256) vs SAM-Med2D (256×256) trên 3 kịch bản prompt. PGA-UNet (256): image-level (N=\Ntestimg~ảnh); SAM-Med2D: image-level (N=\Ntestimg~ảnh).}
    \label{tab:sam_comparison_256}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Pre↑} & \textbf{Rec↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        PGA-UNet (256) & Zoom-out & \textbf{0.8420} & \textbf{0.7348} & \textbf{0.8062} & \textbf{0.8966} & \textbf{6.14} & \textbf{0.9479} \\
        PGA-UNet (256) & Shift & \textbf{0.8048} & \textbf{0.6833} & \textbf{0.7798} & \textbf{0.8476} & \textbf{7.77} & \textbf{0.9202} \\
        PGA-UNet (256) & Mixed & \textbf{0.8317} & \textbf{0.7209} & \textbf{0.7998} & \textbf{0.8817} & \textbf{6.57} & \textbf{0.9400} \\
        \hline
        SAM-Med2D (FT, 256) & Zoom-out & 0.7432 & 0.6238 & 0.7634 & 0.7570 & 9.17 & 0.8919 \\
        SAM-Med2D (FT, 256) & Shift & 0.7159 & 0.5901 & 0.7480 & 0.7187 & 9.70 & 0.8759 \\
        SAM-Med2D (FT, 256) & Mixed & 0.7355 & 0.6136 & 0.7605 & 0.7462 & 9.27 & 0.8884 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích:} Kết quả Bảng~\ref{tab:sam_comparison_256} xác nhận rằng ưu thế của PGA-UNet không đến từ lợi thế độ phân giải. Ở cùng $256\times256$, PGA-UNet vẫn vượt SAM-Med2D fine-tuned trên cả 3 kịch bản (Zoom-out: $+0.1070$, Shift: $+0.0951$, Mixed: $+0.1034$ Dice). Đáng lưu ý, mức sụt giảm Dice khi chuyển từ Zoom-out sang Shift của PGA ở $256\times256$ là $-0.0372$ (0.8420$\to$0.8048), lớn hơn so với PGA ở $512\times512$ ($-0.0130$), phù hợp với nhận định rằng độ phân giải cao hơn giúp mô hình bền bỉ hơn trước sai số câu nhắc. Điều này cũng đồng thời khẳng định: lựa chọn $512\times512$ là tốt nhất cho PGA-UNet, và việc so sánh PGA@512 với SAM@256 trong Bảng~\ref{tab:sam_comparison} là hoàn toàn hợp lý vì đó là cấu hình tối ưu của mỗi mô hình.

% ------------------------------------------------------------
\subsection{So sánh hiệu quả tính toán}
\label{subsec:efficiency}
% ------------------------------------------------------------

Ngoài hiệu năng phân đoạn, tính khả thi triển khai trong môi trường lâm sàng đòi hỏi mô hình phải đủ nhẹ để chạy trên phần cứng y tế tuyến cơ sở. Bảng~\ref{tab:efficiency} so sánh toàn diện giữa PGA-UNet và SAM-Med2D về các chỉ số tính toán.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu quả tính toán: PGA-UNet vs SAM-Med2D}
    \label{tab:efficiency}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Chỉ số} & \textbf{PGA-UNet (đề xuất)} & \textbf{SAM-Med2D} \\
        \hline
        Tổng tham số & $\sim$3M & $\sim$91M (ViT-B) + Adapter \\
        \hline
        Tham số huấn luyện được & $\sim$3M (100\%) & $\sim$15M (Adapter + Decoder) \\
        \hline
        Độ phân giải ảnh & 512$\times$512 & 256$\times$256 (cố định) \\
        \hline
        VRAM huấn luyện (batch=4) & $\sim$3 GB & $\sim$6 GB \\
        \hline
        VRAM suy luận (batch=1) & $\sim$1 GB & $\sim$2 GB \\
        \hline
        Có thể chạy trên GPU 4GB & \checkmark & Hạn chế \\
        \hline
    \end{tabular}%
    }
\end{table}

% ============================================================
\section{Phân tích kiến trúc và khả năng tổng quát hóa}
\label{sec:ablation_study}
% ============================================================

Mục này trình bày hai nhóm thực nghiệm bổ sung: (1)~ablation study so sánh các cấu hình kiến trúc PSG/CAD; (2)~kiểm định độ ổn định bằng đánh giá chéo 4-fold.

% ------------------------------------------------------------
\subsection{Ablation kiến trúc: Đóng góp của PSG và CAD}
\label{subsec:ablation_arch}
% ------------------------------------------------------------

Năm biến thể kiến trúc được huấn luyện lại hoàn toàn từ đầu trên cùng tập BTXRD, chỉ thay đổi cấu hình PSG/CAD và loại câu nhắc:
\begin{itemize}
    \item \textbf{V1:} No PSG, No CAD (chỉ concatenate heatmap).
    \item \textbf{V2:} PSG only (không có CAD).
    \item \textbf{V3:} CAD only (không có PSG).
    \item \textbf{V4:} PSG + CAD với Binary bbox làm câu nhắc.
    \item \textbf{V5 (đề xuất):} PSG + CAD với Gaussian Heatmap làm câu nhắc.
\end{itemize}
Thiết kế năm biến thể này cho phép cô lập đóng góp riêng lẻ của PSG (V2 so V1), CAD (V3 so V1), tổ hợp cả hai (V4 so V2/V3) và ảnh hưởng của loại câu nhắc Gaussian vs Binary (V5 so V4). Kết quả được trình bày trong Bảng~\ref{tab:ablation_arch}.

\begin{table}[htbp]
    \centering
    \caption{Ablation study: Dice Score trên 3 kịch bản prompt của 5 biến thể kiến trúc PGA-UNet (N=\Ntestimg~ảnh, image-level). V5 là mô hình đề xuất.}
    \label{tab:ablation_arch}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Biến thể} & \textbf{PSG} & \textbf{CAD} & \textbf{Prompt} & \textbf{Zoom-out↑} & \textbf{Shift↑} & \textbf{Mixed↑} \\
        \hline
        V1: Baseline (concat heatmap) & \texttimes & \texttimes & Gaussian & 0.8739 & 0.7268 & 0.8169 \\
        \hline
        V2: PSG only & \checkmark & \texttimes & Gaussian & 0.8635 & 0.7348 & 0.8137 \\
        \hline
        V3: CAD only & \texttimes & \checkmark & Gaussian & 0.8862 & 0.7410 & 0.8289 \\
        \hline
        V4: Full PGA + Binary prompt & \checkmark & \checkmark & Binary & 0.8834 & 0.7433 & 0.8307 \\
        \hline
        \textbf{V5: Full PGA + Gaussian (đề xuất)} & \checkmark & \checkmark & Gaussian & 0.8584 & \textbf{0.8454} & \textbf{0.8554} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích ablation:} Hai quan sát chính từ Bảng~\ref{tab:ablation_arch}:

\textbf{(1) Đóng góp của PSG và CAD:} V3 (CAD only, Zoom=0.8862) cao hơn V2 (PSG only, Zoom=0.8635) cho thấy CAD đóng góp nhiều hơn vào hiệu năng Zoom-out; song cả hai đều thua V5 trong kịch bản Shift (V2: 0.7348, V3: 0.7410 so với V5: 0.8454), xác nhận kiến trúc đầy đủ PSG+CAD là cần thiết.

\textbf{(2) Trade-off Gaussian vs Binary prompt (V5 vs V4):} Đây là phát hiện đáng chú ý. Khi chuyển từ Binary bbox sang Gaussian heatmap, Dice Shift tăng mạnh +0.1021 (0.7433 → 0.8454), Dice Mixed tăng +0.0247 (0.8307 → 0.8554), trong khi Dice Zoom-out giảm nhẹ -0.0250 (0.8834 → 0.8584). \textbf{Trade-off này là có chủ đích:} Gaussian heatmap "làm mờ" đường biên tín hiệu câu nhắc, buộc mô hình không thể ghi nhớ cứng nhắc vị trí tâm bounding box. Hệ quả là robustness cải thiện vượt trội khi câu nhắc bị lệch, trong khi hiệu năng với câu nhắc lý tưởng chỉ suy giảm không đáng kể. Trong môi trường lâm sàng thực tế, khả năng chịu đựng sai số câu nhắc quan trọng hơn điểm Dice tuyệt đối với câu nhắc hoàn hảo, V5 tối ưu cho đúng mục tiêu này.

\textbf{(3) Đóng góp riêng lẻ của kiến trúc PSG+CAD (V5 vs V1):} Để tách biệt đóng góp của kiến trúc khỏi đóng góp của cách mã hóa câu nhắc, cả bốn biến thể V1--V3 và V5 đều dùng Gaussian heatmap, chỉ thay đổi cấu hình PSG/CAD. So sánh trực tiếp: PSG đơn độc (V2 vs V1): Shift chỉ tăng +0.0080; CAD đơn độc (V3 vs V1): +0.0142; nhưng PSG+CAD kết hợp với Gaussian (V5 vs V1): \textbf{+0.1186} (0.7268 → 0.8454), gấp hơn 5 lần tổng hai đóng góp riêng lẻ (0.0080 + 0.0142 = 0.0222). Hiệu ứng tương hỗ này cho thấy PSG và CAD phối hợp với nhau để tận dụng tối đa tín hiệu gradient liên tục của Gaussian heatmap: PSG định hướng encoder tập trung vào vùng câu nhắc, CAD duy trì tín hiệu đó xuyên suốt decoder; khi thiếu một trong hai, tín hiệu bị suy giảm trước khi đến đầu ra.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/chart_ablation.png}
    \caption{Ablation study: Dice Score của 5 biến thể kiến trúc PGA-UNet trên ba kịch bản prompt (Bảng~\ref{tab:ablation_arch}). V5 (đề xuất, xanh dương) thấp hơn nhẹ ở Zoom-out nhưng vượt trội rõ rệt ở Shift so với bốn biến thể còn lại (xám).}
    \label{fig:chart_ablation}
\end{figure}

% ------------------------------------------------------------
\subsection{Kiểm định độ ổn định bằng đánh giá chéo (Cross-validation)}
\label{subsec:cross_validation}
% ------------------------------------------------------------

Để xác minh tính ổn định và khả năng tổng quát hóa của PGA-UNet không phụ thuộc vào một lần phân chia dữ liệu duy nhất, mô hình được đánh giá chéo trên 4 phân vùng dữ liệu khác nhau. Kết quả trình bày tại Bảng~\ref{tab:cross_validation}.

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá chéo 4-fold PGA-UNet trên 3 kịch bản prompt (trung bình 4 fold)}
    \label{tab:cross_validation}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓} & \textbf{CBL↑} \\
        \hline
        Zoom-out & 0.8809 & 0.7936 & 0.8578 & 0.9149 & 11.05 & 0.9620 \\
        \hline
        Shift & 0.8570 & 0.7589 & 0.8453 & 0.8824 & 13.08 & 0.9459 \\
        \hline
        Mixed (70\% Zoom-out + 30\% Shift) & 0.8747 & 0.7845 & 0.8561 & 0.9049 & 11.39 & 0.9576 \\
        \hline
    \end{tabular}%
    }
\end{table}

\begin{table}[htbp]
    \centering
    \caption{Dice Score từng fold trong đánh giá chéo 4-fold PGA-UNet (kịch bản Zoom-out / Shift / Mixed)}
    \label{tab:cross_validation_per_fold}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|c|c|c|c|}
        \hline
        \textbf{Fold} & \textbf{Zoom-out↑} & \textbf{Shift↑} & \textbf{Mixed↑} \\
        \hline
        Fold 1 & 0.8778 & 0.8461 & 0.8693 \\
        \hline
        Fold 2 & 0.8832 & 0.8679 & 0.8788 \\
        \hline
        Fold 3 & 0.8801 & 0.8518 & 0.8728 \\
        \hline
        Fold 4 & 0.8825 & 0.8620 & 0.8779 \\
        \hline
        \textbf{Trung bình ± Std} & $\mathbf{0.8809 \pm 0.0021}$ & $\mathbf{0.8570 \pm 0.0085}$ & $\mathbf{0.8747 \pm 0.0039}$ \\
        \hline
    \end{tabular}
\end{table}

Bảng~\ref{tab:cross_validation_per_fold} cho thấy Dice Score dao động rất nhỏ giữa các fold: độ lệch chuẩn thấp nhất ở kịch bản Zoom-out ($\pm0.0021$) và lớn nhất ở Shift ($\pm0.0085$), biên độ hoàn toàn chấp nhận được trong nghiên cứu y tế. Kết quả này xác nhận mô hình hội tụ ổn định trên các phân vùng dữ liệu khác nhau, không bị phụ thuộc vào một cách chia cụ thể. Các chỉ số cross-validation nhỉnh hơn tập test chính một chút (Dice Zoom-out: 0.8809 vs 0.8584) phản ánh sự khác biệt tự nhiên giữa dữ liệu huấn luyện và tập kiểm thử độc lập được giữ lại hoàn toàn từ đầu.

\textbf{Tính ổn định thống kê và độ tin cậy kết quả:} Hai bằng chứng hỗ trợ tính tin cậy của các số liệu đã báo cáo. Thứ nhất, độ lệch chuẩn Dice qua 4 fold rất thấp ($\pm0.0021$ ở Zoom-out, $\pm0.0085$ ở Shift), cho thấy kết quả không phụ thuộc vào một cách phân chia dữ liệu cụ thể. Thứ hai, biên độ chênh lệch giữa các mô hình rất lớn so với độ lệch chuẩn: ví dụ, V5 cải thiện Shift Dice lên $+0.1021$ so với V4 (0.8454 vs 0.7433) trong khi std chỉ $\pm0.0085$, tỷ lệ signal-to-noise $> 12$. Để xác nhận ý nghĩa thống kê một cách chính thức, kiểm định Wilcoxon signed-rank (phi tham số, phù hợp vì phân phối Dice không đảm bảo chuẩn) trên \Ntestimg~cặp Dice image-level là bước kiểm định được khuyến nghị cho cặp so sánh V4~vs~V5 (cùng paradigm image-level); với cặp PGA~vs~SAM-Med2D, do hai mô hình có đơn vị đánh giá khác nhau (image-level vs.\ per-polygon), cần tái tính Dice trên cùng đơn vị trước khi kiểm định.

% ------------------------------------------------------------
\subsection{Đánh giá theo đặc tính tổn thương (Sub-Category)}
\label{subsec:subcategory}
% ------------------------------------------------------------

Để đánh giá sâu hơn khả năng tổng quát hóa của PGA-UNet, ba phân tích sub-category được thực hiện: \textbf{(1)}~so sánh PGA-UNet với U-Net trên nhóm \textit{Dễ} (top-50 U-Net Dice) và \textit{Khó} (bottom-50 U-Net Dice), trên $N=\Ntestimg$~ảnh (50 ảnh mỗi nhóm); \textbf{(2)}~so sánh PGA-UNet với SAM-Med2D trên ba nhóm đặc tính lâm sàng (Tổn thương nhỏ, Biên giới mờ, Tổn thương rõ nét), mỗi nhóm 50 ảnh image-level; \textbf{(3)}~kiểm chứng công bằng cùng độ phân giải $256\times256$ để loại trừ lợi thế độ phân giải khi so sánh PGA-UNet với SAM-Med2D.

\subsubsection*{PGA-UNet so với U-Net theo độ khó tác vụ}

Ở nhóm \textit{Dễ} (tương phản cao, hình dạng rõ), U-Net vẫn hoạt động được; PGA kỳ vọng cải thiện thêm đáng kể. Ở nhóm \textit{Khó} (tương phản thấp, xương chồng lấp, hình dạng bất thường), U-Net thiếu thông tin định hướng nên dự đoán suy giảm mạnh, trong khi PGA-UNet nhờ cơ chế prompt guidance kỳ vọng duy trì Dice ổn định hơn, đây là lý do cốt lõi cho sự tồn tại của hướng tiếp cận tương tác.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng PGA-UNet với U-Net theo độ khó tác vụ (50 ảnh mỗi nhóm, image-level; phân nhóm dễ nhất / khó nhất theo Dice của U-Net)}
    \label{tab:subcat_baseline}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{2}{*}{Dễ (top-50)}
            & U-Net       & 0.8615 & 0.7594 & 0.8596 & 0.8764 & 56.02  & 0.9391 \\
            & PGA-UNet    & \textbf{0.8982} & \textbf{0.8179} & \textbf{0.8730} & \textbf{0.9304} & \textbf{12.57} & \textbf{0.9627} \\
        \hline
        \multirow{2}{*}{Khó (bottom-50)}
            & U-Net       & 0.0062 & 0.0032 & 0.0434 & 0.0393 & 307.14 & 0.0665 \\
            & PGA-UNet    & \textbf{0.8120} & \textbf{0.7045} & \textbf{0.8222} & \textbf{0.8202} & \textbf{18.97} & \textbf{0.9252} \\
        \hline
    \end{tabular}%
    }
\end{table}

Kết quả Bảng~\ref{tab:subcat_baseline} xác nhận rõ ràng giả thuyết lý thuyết. Ở nhóm \textit{Dễ} (top-50), U-Net đạt Dice~=~0.8615 trong khi PGA-UNet đạt 0.8982 ($\Delta=+0.0367$), cải thiện có ý nghĩa nhưng không đột biến vì tổn thương đã rõ. Ở nhóm \textit{Khó} (bottom-50), U-Net gần như sụp đổ hoàn toàn, chỉ còn Dice~=~0.0062 (HD95~=~307.14~px); trong khi đó PGA-UNet \textbf{duy trì ổn định ở 0.8120} ($\Delta=+0.8058$ so với U-Net). Kết quả này cho thấy cơ chế prompt spatial gate có vai trò quan trọng trong khả năng xử lý nhất quán các ca bệnh phức tạp.


\subsubsection*{PGA-UNet so với SAM-Med2D theo đặc tính lâm sàng}

Ba quan sát chính dự kiến: (1)~\textbf{Tổn thương nhỏ}: SAM-Med2D hoạt động ở độ phân giải 256$\times$256 khiến các tổn thương nhỏ chỉ còn vài pixel, SAM bỏ sót phần lớn diện tích, trong khi PGA ở 512$\times$512 với heatmap dẫn đường đạt kết quả tốt hơn đáng kể. (2)~\textbf{Biên giới mờ}: ranh giới tổn thương không rõ ràng trên X-quang xương đặc thù BTXRD, SAM pretrain đa dạng nhưng không chuyên biệt hóa đặc trưng gradient X-quang xương; PGA train from scratch trên BTXRD học được các đặc trưng cụ thể này. (3)~\textbf{Tổn thương rõ nét}: khi tổn thương lớn và ranh giới rõ, cả hai mô hình đều hoạt động tốt; PGA nhỉnh hơn SAM với $\sim$30$\times$ ít tham số hơn.

\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet với SAM-Med2D trên ba nhóm đặc tính lâm sàng (kịch bản Zoom-out, 50 ảnh mỗi nhóm, image-level)}
    \label{tab:subcat_sam}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{2}{*}{Tổn thương nhỏ}
            & SAM-Med2D  & 0.4365 & 0.3235 & 0.6133 & 0.3810 & 62.62 & 0.7218 \\
            & PGA-UNet   & \textbf{0.8124} & \textbf{0.7033} & \textbf{0.7873} & \textbf{0.8571} & \textbf{13.57} & \textbf{0.9251} \\
        \hline
        \multirow{2}{*}{Biên giới mờ}
            & SAM-Med2D  & 0.6567 & 0.5017 & 0.7900 & 0.6112 & 27.99 & 0.9004 \\
            & PGA-UNet   & \textbf{0.8544} & \textbf{0.7503} & \textbf{0.8483} & \textbf{0.8729} & \textbf{15.31} & \textbf{0.9551} \\
        \hline
        \multirow{2}{*}{Tổn thương rõ nét}
            & SAM-Med2D  & 0.8588 & 0.7556 & 0.8936 & 0.8331 & 22.55 & 0.9564 \\
            & PGA-UNet   & \textbf{0.8730} & \textbf{0.7835} & \textbf{0.8576} & \textbf{0.8966} & \textbf{21.68} & \textbf{0.9604} \\
        \hline
    \end{tabular}%
    }
\end{table}

Kết quả Bảng~\ref{tab:subcat_sam} xác nhận cả ba nhận định. \textbf{Tổn thương nhỏ}: PGA đạt Dice~=~0.8124 trong khi SAM chỉ đạt 0.4365 ($\Delta=+0.3759$), khoảng cách lớn nhất trong ba nhóm, phù hợp với giả thuyết về ảnh hưởng của độ phân giải 512$\times$512 so với 256$\times$256 đối với tổn thương nhỏ. \textbf{Biên giới mờ}: PGA~=~0.8544 vs SAM~=~0.6567 ($\Delta=+0.1977$), cho thấy đặc trưng X-quang xương chuyên biệt mà PGA học được trên BTXRD. \textbf{Tổn thương rõ nét}: khoảng cách thu hẹp đáng kể (PGA~=~0.8730, SAM~=~0.8588, $\Delta=+0.0143$), cả hai mô hình hoạt động tốt khi điều kiện thuận lợi; PGA vẫn nhỉnh hơn với kiến trúc nhỏ gọn hơn $\sim$30$\times$.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/chart_subcat_btxrd.png}
    \caption{So sánh PGA-UNet và SAM-Med2D theo ba nhóm đặc tính lâm sàng trên BTXRD (Bảng~\ref{tab:subcat_sam}): khoảng cách lớn nhất ở nhóm Tổn thương nhỏ, thu hẹp dần ở nhóm Tổn thương rõ nét.}
    \label{fig:chart_subcat_btxrd}
\end{figure}

\subsubsection*{Kiểm chứng công bằng Sub-Category tại cùng 256$\times$256}

Để loại trừ hoàn toàn lợi thế độ phân giải, phân tích sub-category được lặp lại với PGA-UNet$_{256}$ (huấn luyện và suy luận tại $256\times256$) đối chiếu trực tiếp với SAM-Med2D ($256\times256$). Cả hai mô hình cùng nhóm mẫu, cùng bounding box, chỉ khác checkpoint PGA.

\begin{table}[htbp]
    \centering
    \caption{So sánh công bằng PGA-UNet$_{256}$ vs SAM-Med2D theo đặc tính lâm sàng (kịch bản Zoom-out, 50 ảnh image-level mỗi nhóm, cùng 256×256)}
    \label{tab:subcat_sam_256}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{2}{*}{Tổn thương nhỏ}
            & SAM-Med2D  & 0.4365 & 0.3235 & \textbf{0.6133} & 0.3810 & 62.62 & 0.7218 \\
            & PGA-UNet$_{256}$ & \textbf{0.6836} & \textbf{0.5377} & 0.5825 & \textbf{0.9051} & \textbf{5.94} & \textbf{0.9087} \\
        \hline
        \multirow{2}{*}{Biên giới mờ}
            & SAM-Med2D  & 0.6567 & 0.5017 & \textbf{0.7900} & 0.6112 & 27.99 & 0.9004 \\
            & PGA-UNet$_{256}$ & \textbf{0.8000} & \textbf{0.6731} & 0.7362 & \textbf{0.8992} & \textbf{17.84} & \textbf{0.9410} \\
        \hline
        \multirow{2}{*}{Tổn thương rõ nét}
            & SAM-Med2D  & 0.8588 & 0.7556 & \textbf{0.8936} & 0.8331 & 22.55 & 0.9564 \\
            & PGA-UNet$_{256}$ & \textbf{0.8689} & \textbf{0.7731} & 0.8347 & \textbf{0.9133} & 22.84 & \textbf{0.9525} \\
        \hline
    \end{tabular}%
    }
\end{table}

Bảng~\ref{tab:subcat_sam_256} xác nhận kết luận chính vẫn đúng khi kiểm soát yếu tố độ phân giải: PGA-UNet$_{256}$ vượt SAM-Med2D ở cả ba nhóm về Dice ($+0.2471$ ở tổn thương nhỏ, $+0.1433$ ở biên giới mờ, $+0.0101$ ở tổn thương rõ nét). Đáng chú ý, ở nhóm \textbf{Tổn thương nhỏ}, dù PGA có Precision thấp hơn SAM (0.5825 vs 0.6133) nhưng Recall vượt trội (0.9051 vs 0.3810), nghĩa là SAM bỏ sót phần lớn tổn thương nhỏ trong khi PGA phát hiện được hầu hết; HD95 của PGA$_{256}$ (5.94~px) cũng thấp hơn SAM (62.62~px) rất đáng kể, cho thấy khi PGA phân đoạn được tổn thương nhỏ thì đường biên bám sát hơn nhiều. So sánh Bảng~\ref{tab:subcat_sam} (PGA@512 vs SAM@256) với Bảng~\ref{tab:subcat_sam_256} (cùng 256) cho thấy nhóm Tổn thương nhỏ được hưởng lợi nhiều nhất từ độ phân giải cao (PGA@512: 0.8124 vs PGA@256: 0.6836, $\Delta=+0.1288$), trong khi nhóm Tổn thương rõ nét ít nhạy hơn ($\Delta=+0.0041$).

% ------------------------------------------------------------
\section{Đánh giá khả năng tổng quát hóa liên miền dữ liệu (Cross-dataset: FracAtlas)}
\label{sec:fracatlas}
% ------------------------------------------------------------

Toàn bộ kết quả trình bày từ Mục~\ref{sec:danh_gia_hieu_nang} đến Mục~\ref{subsec:subcategory} đều được huấn luyện và đánh giá trên BTXRD. Để kiểm chứng kiến trúc PGA-UNet và quy trình huấn luyện không chỉ ``phù hợp'' với một bộ dữ liệu cụ thể, mục này lặp lại các thực nghiệm cốt lõi (so sánh PGA vs SAM-Med2D, đánh giá theo 3 kịch bản prompt, phân tích sub-category) trên một bộ dữ liệu X-quang xương độc lập thứ hai: \textbf{FracAtlas}. Đây là bộ dữ liệu có nguồn gốc, quy trình thu thập và đặc điểm tổn thương (gãy xương, dạng đường nứt mảnh, tuyến tính) khác biệt với BTXRD (u xương, dạng khối/mảng), phù hợp để kiểm tra khả năng tổng quát hóa domain.

\subsection{Giới thiệu bộ dữ liệu FracAtlas}

\textbf{FracAtlas}~\cite{fracatlas2023} là bộ dữ liệu X-quang công khai tập trung vào gãy xương (fracture), thu thập từ 3 bệnh viện tại Bangladesh với tổng cộng 4.083 ảnh X-quang vùng tay, vai, hông và chân, trong đó 717 ảnh có bệnh lý (922 vùng gãy xương được khoanh vùng). Nhãn được xây dựng qua quy trình hai giai đoạn: hai bác sĩ X-quang (radiologist) độc lập xác định vị trí gãy, sau đó một bác sĩ chỉnh hình (orthopedic surgeon) đối chiếu và giải quyết bất đồng; mặt nạ đa giác (polygon mask) được vẽ thủ công bằng công cụ mã nguồn mở \texttt{makesense.ai} theo định dạng COCO JSON. Cấu trúc nhãn ba cấp (phân lớp, bounding box định vị, mặt nạ phân đoạn pixel-level) tương đồng với BTXRD nên có thể tái sử dụng trực tiếp toàn bộ pipeline huấn luyện/suy luận (PGA-UNet, SAM-Med2D) mà không cần thay đổi kiến trúc hay hàm mất mát. Điểm khác biệt cốt lõi so với BTXRD là đặc tính hình thái tổn thương: đường gãy xương thường mảnh, tuyến tính và có diện tích nhỏ hơn nhiều so với khối u xương, tạo ra một phép thử tổng quát hóa có ý nghĩa cho cơ chế Prompt Spatial Gate và Conditioned Attention.

\begin{table}[htbp]
    \centering
    \caption{Thống kê phân bố bộ dữ liệu FracAtlas dùng trong thực nghiệm (đơn vị: mẫu per-polygon)}
    \label{tab:fracatlas_dataset_stats}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|p{2.6cm}|c|p{3.5cm}|p{5.5cm}|}
        \hline
        \textbf{Tập dữ liệu} & \textbf{Số ảnh} & \textbf{Số mẫu (per-polygon)} & \textbf{Ghi chú} \\
        \hline
        Huấn luyện (Train) & 573 & \NtrainFA & \\
        \hline
        Xác thực (Validation) & 72 & \NvalFA & \\
        \hline
        Kiểm thử (Test) & \NtestimgFA & \NtestFA & Trung bình $\sim$1.28 polygon/ảnh \\
        \hline
    \end{tabular}
\end{table}

\textbf{Kiểm tra chéo số liệu:} Tổng số mẫu per-polygon dùng trong ba tập của khóa luận ($\NtrainFA + \NvalFA + \NtestFA = 917$) khớp gần đúng với con số 922 vùng gãy xương (fracture instances) mà bài báo gốc công bố trên 717 ảnh bệnh lý~\cite{fracatlas2023}; chênh lệch nhỏ (5 mẫu, $<$1\%) hợp lý do một số polygon bị loại trong bước tiền xử lý (ví dụ polygon quá nhỏ hoặc lỗi định dạng), củng cố độ tin cậy của quy trình chuẩn bị dữ liệu.

Quy mô huấn luyện của FracAtlas (\NtrainFA~mẫu) nhỏ hơn đáng kể so với BTXRD (\Ntrain~mẫu, chỉ bằng $\approx$49\%), đây là điều kiện thực nghiệm khắt khe hơn để đánh giá khả năng học đặc trưng của PGA-UNet với ít dữ liệu huấn luyện hơn.

\subsection{PGA-UNet toàn diện trên FracAtlas (256×256 và 512×512, ba kịch bản prompt)}
\label{subsec:fracatlas_pga_full}

Tương tự Mục~\ref{subsec:sam_comparison} và~\ref{subsec:fair256}, PGA-UNet được huấn luyện lại từ đầu trên FracAtlas ở cả hai độ phân giải $256\times256$ và $512\times512$, đánh giá trên cả ba kịch bản prompt (Zoom-out, Shift, Mixed 70/30), image-level (N=\NtestimgFA~ảnh).

\begin{table}[htbp]
    \centering
    \caption{PGA-UNet trên FracAtlas: kết quả toàn diện tại 256×256 và 512×512, ba kịch bản prompt (N=\NtestimgFA~ảnh, image-level)}
    \label{tab:fracatlas_pga_full}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Độ phân giải} & \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Pre↑} & \textbf{Rec↑} & \textbf{HD95↓ (px)} & \textbf{CBL↑} \\
        \hline
        PGA-UNet (256) & Zoom-out   & 0.8110 & 0.6886 & 0.7790 & 0.8628 & 3.57  & 0.9456 \\
        PGA-UNet (256) & Shift      & 0.7842 & 0.6531 & 0.7566 & 0.8333 & 4.02  & 0.9260 \\
        PGA-UNet (256) & Mixed      & 0.8088 & 0.6864 & 0.7801 & 0.8581 & 3.57  & 0.9431 \\
        \hline
        PGA-UNet (512) & Zoom-out   & \textbf{0.8169} & \textbf{0.6953} & 0.7645 & \textbf{0.8975} & 7.45\textsuperscript{\P} & \textbf{0.9546} \\
        PGA-UNet (512) & Shift      & \textbf{0.7850} & \textbf{0.6537} & 0.7349 & \textbf{0.8618} & 8.88\textsuperscript{\P} & \textbf{0.9308} \\
        PGA-UNet (512) & Mixed      & \textbf{0.8129} & \textbf{0.6904} & 0.7614 & \textbf{0.8913} & 7.80\textsuperscript{\P} & \textbf{0.9499} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textsuperscript{\P}\textit{Lưu ý HD95 khác đơn vị độ phân giải:} Giá trị HD95 thô (px) của PGA-256 và PGA-512 \textbf{không thể so sánh trực tiếp} vì đo trên hai không gian ảnh khác kích thước (1~px lỗi tại 256 tương đương $\sim$2~px tại 512); do đó HD95 không được in đậm ở hàng 512 dù giá trị tuyệt đối cao hơn -- đây không phải PGA-512 kém bám biên hơn, chỉ là khác đơn vị đo (tương tự cách xử lý HD95* chuẩn hóa ở Bảng~\ref{tab:sam_comparison}).

\textbf{Nhận xét:} Ngay cả với chỉ \NtrainFA~mẫu huấn luyện (bằng $\approx$49\% BTXRD), PGA-UNet vẫn đạt Dice $>$ 0.78 ở cả ba kịch bản tại cả hai độ phân giải. Tăng độ phân giải lên $512\times512$ cải thiện hầu hết chỉ số ở cả ba kịch bản (Zoom-out Dice: 0.8110 $\to$ 0.8169; Shift: 0.7842 $\to$ 0.7850; Mixed: 0.8088 $\to$ 0.8129; IoU, Recall, CBL đều tăng tương ứng), nhất quán với quan sát trên BTXRD (Mục~\ref{subsec:fair256}) rằng độ phân giải cao hơn có lợi cho PGA-UNet. Riêng Precision giảm nhẹ ở cả ba kịch bản khi tăng độ phân giải (256$\to$512: Zoom-out $-0.0145$, Shift $-0.0217$, Mixed $-0.0187$), khác với xu hướng chung trên BTXRD -- nhất quán theo cả ba kịch bản nên khó là ngẫu nhiên, có thể phản ánh đặc thù của đường gãy mảnh: ở độ phân giải cao hơn mô hình phát hiện được nhiều vùng nghi ngờ hơn (Recall tăng) nhưng đồng thời bắt luôn một số vùng biên mờ không phải gãy xương thực sự (Precision giảm nhẹ), một đánh đổi hợp lý về mặt lâm sàng (thà nghi ngờ thêm còn hơn bỏ sót). Mức sụt Dice Zoom-out~$\to$~Shift cũng nhỏ ở cả hai độ phân giải (256: $-0.0268$; 512: $-0.0319$), xác nhận tính bền bỉ của cơ chế Gaussian heatmap không phụ thuộc độ phân giải. Nhìn chung, kết quả cho thấy kiến trúc PSG+CAD không chỉ hiệu quả với đặc tính tổn thương dạng khối (u xương, BTXRD) mà còn khái quát tốt sang đặc tính tổn thương dạng đường nứt mảnh (gãy xương, FracAtlas), dù tập huấn luyện nhỏ hơn đáng kể.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{images/chart_fracatlas_pga_full.png}
    \caption{PGA-UNet trên FracAtlas: Dice Score tại 256×256 và 512×512, ba kịch bản prompt (Bảng~\ref{tab:fracatlas_pga_full}).}
    \label{fig:chart_fracatlas_pga_full}
\end{figure}

\subsection{So sánh cơ sở (Baseline): PGA-UNet vs U-Net trên FracAtlas}
\label{subsec:fracatlas_baseline}

Tương tự Mục~\ref{sec:danh_gia_hieu_nang} trên BTXRD, U-Net (không prompt) được huấn luyện lại từ đầu trên FracAtlas ở $512\times512$ để làm baseline cơ sở, đối chiếu với PGA-UNet (512) kịch bản Zoom-out, cùng N=\NtestimgFA~ảnh image-level.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng phân đoạn: U-Net (tự động) so với PGA-UNet (512, Zoom-out) trên FracAtlas (N=\NtestimgFA~ảnh, image-level, cùng độ phân giải 512×512)}
    \label{tab:fracatlas_baseline}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Precision↑} & \textbf{Recall↑} & \textbf{HD95↓ (px)} & \textbf{CBL↑} \\
        \hline
        U-Net & 0.3831 & 0.2836 & 0.4387 & 0.4642 & 136.82 & 0.4628 \\
        \hline
        \textbf{PGA-UNet (512, Zoom-out)} & \textbf{0.8169} & \textbf{0.6953} & \textbf{0.7645} & \textbf{0.8975} & \textbf{7.45} & \textbf{0.9546} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét:} Khoảng cách giữa U-Net và PGA-UNet trên FracAtlas ($\Delta$Dice~$=+0.4338$) còn lớn hơn trên BTXRD ($\Delta$Dice~$=+0.3794$, Bảng~\ref{tab:baseline_comparison}), củng cố quan sát đã có ở Mục~\ref{subsec:fracatlas_sam}: đặc tính đường gãy mảnh của FracAtlas càng bất lợi hơn cho mô hình không có định hướng không gian. HD95 của U-Net trên FracAtlas (136.82~px) cũng cao hơn nhiều so với PGA-UNet (7.45~px, chênh lệch $\sim$18 lần), cho thấy khi không có câu nhắc, U-Net không chỉ bỏ sót tổn thương mà còn định vị hoàn toàn sai vị trí ở nhiều ca.

\subsection{So sánh với SAM-Med2D (Fine-tuned và Zero-shot) trên FracAtlas}
\label{subsec:fracatlas_sam}

Khác với thực nghiệm trên BTXRD (nơi PGA-UNet đánh giá image-level còn SAM-Med2D chỉ đánh giá per-polygon do giới hạn triển khai ban đầu), ở FracAtlas cả ba mô hình (PGA-UNet, SAM-Med2D fine-tuned, SAM-Med2D zero-shot) đều được quy về \textbf{cùng đơn vị đánh giá image-level} (N=\NtestimgFA~ảnh, GT union + max-merge dự đoán), cho phép so sánh trực tiếp không cần quy đổi.

\begin{table}[htbp]
    \centering
    \caption{So sánh toàn diện PGA-UNet (256) với SAM-Med2D fine-tuned và zero-shot trên FracAtlas, ba kịch bản prompt (tất cả cùng image-level, N=\NtestimgFA~ảnh, cùng độ phân giải 256×256)}
    \label{tab:fracatlas_sam_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice↑} & \textbf{IoU↑} & \textbf{Pre↑} & \textbf{Rec↑} & \textbf{HD95↓ (px)} & \textbf{CBL↑} \\
        \hline
        PGA-UNet (256) & Zoom-out & \textbf{0.8110} & \textbf{0.6886} & \textbf{0.7790} & \textbf{0.8628} & \textbf{3.57} & \textbf{0.9456} \\
        PGA-UNet (256) & Shift    & \textbf{0.7842} & \textbf{0.6531} & \textbf{0.7566} & \textbf{0.8333} & \textbf{4.02} & \textbf{0.9260} \\
        PGA-UNet (256) & Mixed    & \textbf{0.8088} & \textbf{0.6864} & \textbf{0.7801} & \textbf{0.8581} & \textbf{3.57} & \textbf{0.9431} \\
        \hline
        SAM-Med2D (FT) & Zoom-out & 0.7339 & 0.5897 & 0.7358 & 0.7508 & 4.33  & 0.9040 \\
        SAM-Med2D (FT) & Shift    & 0.6743 & 0.5227 & 0.6715 & 0.6925 & 5.19  & 0.8606 \\
        SAM-Med2D (FT) & Mixed    & 0.7209 & 0.5745 & 0.7185 & 0.7396 & 4.57  & 0.8916 \\
        \hline
        SAM-Med2D (ZS) & Zoom-out & 0.5278 & 0.3720 & 0.4854 & 0.6519 & 8.53  & 0.8514 \\
        SAM-Med2D (ZS) & Shift    & 0.4976 & 0.3458 & 0.4303\textsuperscript{\S} & 0.5987\textsuperscript{\S} & 9.71  & 0.8129 \\
        SAM-Med2D (ZS) & Mixed    & 0.5077 & 0.3537 & 0.4481\textsuperscript{\S} & 0.6138\textsuperscript{\S} & 9.31  & 0.8305 \\
        \hline
    \end{tabular}%
    }
\end{table}

\textit{Ghi chú về checkpoint fine-tuned:} Lần huấn luyện đầu tiên của SAM-Med2D trên FracAtlas hội tụ quá sớm (checkpoint tốt nhất lưu ngay tại epoch~1, do quá khớp trên tập huấn luyện nhỏ khiến early stopping kích hoạt sau đó), khiến dự đoán gần như trùng khớp với zero-shot. Sau khi huấn luyện lại, checkpoint fine-tuned hiện tại (\texttt{best\_sam.pth}, train Dice tăng liên tục $0.6266 \to 0.8981$ qua 11 epoch) hội tụ đầy đủ và cải thiện rõ rệt so với zero-shot ở cả ba kịch bản -- số liệu trình bày trong bảng trên là kết quả sau khi huấn luyện lại.

\textsuperscript{\S}\textit{Ghi chú đơn vị:} Precision/Recall image-level của SAM-Med2D zero-shot ở kịch bản Shift và Mixed \textbf{không được ghi nhận} ở bước tổng hợp image-level trong lần chạy notebook hiện tại (chỉ Dice/IoU/HD95/CBL được tính tại bước này). Hai giá trị hiển thị ở trên là Precision/Recall \textbf{per-polygon thô} (N=\NtestFA~mẫu, đơn vị khác với các cột Dice/IoU/HD95/CBL còn lại trong bảng vốn tính ở cấp độ ảnh) — dùng làm tham chiếu gần đúng, không đối chiếu trực tiếp 1-1 với các số image-level khác. Cần chạy lại bước tổng hợp image-level để có số liệu chính xác cùng đơn vị.

\textbf{Phân tích:} Sau khi fine-tuning hội tụ đầy đủ, SAM-Med2D (FT) cải thiện rõ rệt so với zero-shot ở cả ba kịch bản (Dice: Zoom-out $+0.2061$, Shift $+0.1767$, Mixed $+0.2132$), xác nhận fine-tuning trên FracAtlas thực sự mang lại lợi ích khi mô hình được huấn luyện đủ epoch. Dù vậy, PGA-UNet vẫn vượt trội SAM-Med2D (FT) trên toàn bộ ba kịch bản, với biên độ đã thu hẹp đáng kể so với biên độ đối với zero-shot ($+0.08$ đến $+0.11$ Dice so với FT, so với $+0.28$ đến $+0.29$ Dice so với ZS). Biên độ còn lại phù hợp với đặc tính hình thái của FracAtlas: tổn thương gãy xương dạng đường nứt mảnh có diện tích nhỏ hơn nhiều so với khối u BTXRD, khiến giới hạn độ phân giải $256\times256$ của SAM-Med2D vẫn bất lợi hơn dù đã fine-tuned đúng cách (tương tự phát hiện ở nhóm ``Tổn thương nhỏ'' Bảng~\ref{tab:subcat_sam}). Đồng thời, PGA-UNet vẫn giữ được tính bền bỉ tốt hơn: mức sụt Dice từ Zoom-out sang Shift chỉ $-0.0268$ (256), nhỏ hơn nhiều so với PGA-UNet@256 trên BTXRD ($-0.0372$, Bảng~\ref{tab:sam_comparison_256}) và nhỏ hơn cả SAM-Med2D (FT) trên FracAtlas ($-0.0596$), cho thấy cơ chế hướng dẫn mềm (Gaussian heatmap) ổn định hơn trước sai lệch câu nhắc ngay cả khi so sánh với một mô hình nền tảng đã fine-tuned đúng cách.

\subsection{Đánh giá theo đặc tính tổn thương (Sub-Category) trên FracAtlas}
\label{subsec:fracatlas_subcat}

\subsubsection*{PGA-UNet so với U-Net theo độ khó tác vụ}

Tương tự Mục~\ref{subsec:subcategory} trên BTXRD, tập test FracAtlas được chia thành hai nhóm \textbf{hậu nghiệm} theo chính Dice của U-Net (baseline không prompt): TOP-DICE (nhóm ảnh U-Net dự đoán tốt nhất) và BOTTOM-DICE (nhóm ảnh U-Net dự đoán kém nhất). Do FracAtlas chỉ có \NtestimgFA~ảnh test (nhỏ hơn nhiều so với 187 ảnh của BTXRD), quy mô mỗi nhóm được điều chỉnh còn N=36 ảnh/nhóm (thay vì top/bottom-50 như BTXRD) để hai nhóm không chồng lấp lên nhau ($2 \times 36 = 72 = \NtestimgFA$).

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng PGA-UNet với U-Net theo độ khó tác vụ trên FracAtlas (36 ảnh mỗi nhóm, image-level; phân nhóm dễ nhất / khó nhất theo Dice của U-Net, kịch bản Zoom-out)}
    \label{tab:fracatlas_subcat_baseline}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{2}{*}{TOP-DICE (N=36)}
            & U-Net       & 0.6532 & 0.5018 & 0.6266 & 0.7500 & 82.10 & 0.7595 \\
            & PGA-UNet    & \textbf{0.8324} & \textbf{0.7179} & \textbf{0.7920} & \textbf{0.8975} & \textbf{8.04} & \textbf{0.9600} \\
        \hline
        \multirow{2}{*}{BOTTOM-DICE (N=36)}
            & U-Net       & 0.1130 & 0.0654 & 0.1397 & 0.1784 & 191.54 & 0.1662 \\
            & PGA-UNet    & \textbf{0.8015} & \textbf{0.6726} & \textbf{0.7370} & \textbf{0.8974} & \textbf{6.85} & \textbf{0.9493} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét:} Xu hướng từ BTXRD (Bảng~\ref{tab:subcat_baseline}) được tái khẳng định rõ ràng trên FracAtlas. Ở nhóm TOP-DICE, U-Net vẫn đạt được Dice khá (0.6532) vì đây là các ca dễ nhất theo chính đánh giá của nó; PGA-UNet vẫn vượt trội hơn ($\Delta=+0.1791$). Ở nhóm BOTTOM-DICE, U-Net gần như sụp đổ hoàn toàn (Dice~=~0.1130, HD95~=~191.54~px), trong khi PGA-UNet \textbf{gần như không đổi} so với nhóm TOP-DICE (Dice~=~0.8015 so với 0.8324, $\Delta=+0.6886$ so với U-Net). Đáng chú ý, PGA-UNet trên FracAtlas duy trì ổn định hơn cả trên BTXRD ở nhóm khó nhất (BTXRD: PGA Dice~=~0.8120 ở nhóm Khó bottom-50 so với U-Net chỉ 0.0062, Bảng~\ref{tab:subcat_baseline}) -- ở cả hai bộ dữ liệu, cơ chế câu nhắc giúp mô hình không phụ thuộc vào việc ảnh có "dễ" hay "khó" theo tiêu chuẩn của một mô hình tự động, mà luôn bám theo vùng được bác sĩ chỉ định.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/chart_fracatlas_subcat_baseline.png}
    \caption{So sánh U-Net và PGA-UNet theo độ khó tác vụ trên FracAtlas (Bảng~\ref{tab:fracatlas_subcat_baseline}).}
    \label{fig:chart_fracatlas_subcat_baseline}
\end{figure}

\subsubsection*{PGA-UNet so với SAM-Med2D theo đặc tính lâm sàng}

Tương tự Mục~\ref{subsec:subcategory}, tập test FracAtlas được chia thành ba nhóm bằng nhau theo cùng tiêu chí (diện tích tổn thương nhỏ nhất, Dice SAM-Med2D thấp nhất trong phần còn lại, diện tích lớn nhất trong phần còn lại), 24 ảnh/nhóm ($3 \times 24 = \NtestimgFA$). Theo yêu cầu phạm vi, chỉ kịch bản \textbf{Zoom-out} được trình bày ở phân tích sub-category (kịch bản đại diện, nhất quán với cách trình bày sub-category trên BTXRD).

\begin{table}[htbp]
    \centering
    \caption{So sánh SAM-Med2D (FT, 256), PGA-UNet (256) và PGA-UNet (512) theo đặc tính tổn thương trên FracAtlas (kịch bản Zoom-out, 24 ảnh/nhóm, image-level)}
    \label{tab:fracatlas_subcat}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{llcccccc}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice} & \textbf{IoU} & \textbf{Pre} & \textbf{Rec} & \textbf{HD95 (px)} & \textbf{CBL} \\
        \hline
        \multirow{3}{*}{Tổn thương nhỏ (N=24)}
            & SAM-Med2D (FT)  & 0.4395 & 0.3141 & \textbf{0.7793} & 0.3507 & 30.94 & 0.8210 \\
            & PGA-UNet (256)  & 0.7584 & 0.6178 & 0.6652 & 0.9006 & 6.04 & 0.9125 \\
            & PGA-UNet (512)  & \textbf{0.8028} & \textbf{0.6756} & 0.7269 & \textbf{0.9150} & \textbf{5.05} & \textbf{0.9400} \\
        \hline
        \multirow{3}{*}{Biên giới mờ (N=24)}
            & SAM-Med2D (FT)  & 0.5787 & 0.4132 & \textbf{0.7814} & 0.4953 & 14.80 & 0.9005 \\
            & PGA-UNet (256)  & 0.7926 & 0.6627 & 0.7118 & \textbf{0.9060} & 9.41 & \textbf{0.9566} \\
            & PGA-UNet (512)  & \textbf{0.8039} & \textbf{0.6775} & 0.7439 & 0.8925 & \textbf{9.29} & 0.9552 \\
        \hline
        \multirow{3}{*}{Tổn thương rõ nét (N=24)}
            & SAM-Med2D (FT)  & 0.7677 & 0.6253 & \textbf{0.8451} & 0.7178 & 9.86 & 0.9373 \\
            & PGA-UNet (256)  & 0.8358 & 0.7213 & 0.8105 & \textbf{0.8801} & 8.17 & 0.9563 \\
            & PGA-UNet (512)  & \textbf{0.8404} & \textbf{0.7280} & 0.8203 & 0.8786 & \textbf{8.09} & \textbf{0.9626} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích:} Xu hướng chính từ BTXRD (Bảng~\ref{tab:subcat_sam}) được tái khẳng định trên FracAtlas: PGA-UNet vượt SAM-Med2D (256, đã fine-tuned đúng cách) về Dice ở cả ba nhóm, khoảng cách lớn nhất tại nhóm \textit{Tổn thương nhỏ} ($\Delta=+0.3633$ Dice so với PGA-512, xấp xỉ mức chênh lệch tương ứng trên BTXRD $+0.3759$), thu hẹp dần ở \textit{Biên giới mờ} ($\Delta=+0.2251$) và \textit{Tổn thương rõ nét} ($\Delta=+0.0726$) -- cùng khuôn mẫu quan sát được trên BTXRD. Đáng chú ý, ở cả ba nhóm SAM-Med2D lại đạt Precision cao hơn PGA-UNet (ví dụ nhóm Tổn thương rõ nét: SAM~$=0.8451$ so với PGA-512~$=0.8203$), trong khi Recall và Dice tổng thể vẫn thấp hơn nhiều; điều này cho thấy mô hình nền tảng đã fine-tuned có xu hướng dự đoán bảo thủ hơn (dự đoán vùng nhỏ hơn nhưng chính xác khi đã dự đoán), còn PGA-UNet ưu tiên bao phủ đầy đủ vùng tổn thương (Recall cao hơn) -- một đánh đổi hợp lý về mặt lâm sàng vì bỏ sót tổn thương thường nguy hiểm hơn dự đoán dư. Không còn xuất hiện bất thường về độ phân giải như quan sát trước đây (PGA-UNet 512 nay vượt PGA-UNet 256 ở toàn bộ ba nhóm, nhất quán với các bảng khác trong khóa luận): nguyên nhân là hai nhóm \textit{Biên giới mờ}/\textit{Tổn thương rõ nét} được phân loại hậu nghiệm theo thứ hạng Dice của chính SAM-Med2D, nên khi checkpoint SAM-Med2D được huấn luyện lại đúng cách (thay vì hội tụ sớm như trước), thành phần ảnh trong hai nhóm này cũng thay đổi theo, kéo theo số liệu PGA-UNet trên các nhóm này được tính lại trên tập ảnh mới.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{images/chart_subcat_fracatlas.png}
    \caption{So sánh SAM-Med2D (FT), PGA-UNet (256) và PGA-UNet (512) theo ba nhóm đặc tính tổn thương trên FracAtlas (Bảng~\ref{tab:fracatlas_subcat}).}
    \label{fig:chart_subcat_fracatlas}
\end{figure}

\subsection{Thảo luận: khả năng tổng quát hóa liên miền dữ liệu}

Ba quan sát tổng hợp từ thực nghiệm FracAtlas:

\begin{itemize}
    \item \textbf{Tổng quát hóa kiến trúc, không phải overfitting vào BTXRD:} Dù được huấn luyện lại hoàn toàn từ đầu trên một bộ dữ liệu có nguồn gốc, đặc tính tổn thương và quy mô huấn luyện khác biệt (\NtrainFA~mẫu so với \Ntrain~mẫu, chỉ $\approx$49\%), PGA-UNet vẫn đạt Dice $>0.78$ ở mọi kịch bản và cả hai độ phân giải, vượt trội U-Net (baseline không prompt) với biên độ còn lớn hơn BTXRD ($+0.4338$ so với $+0.3794$) và vượt SAM-Med2D (FT, đã fine-tuned đúng cách) với biên độ $+0.08$ đến $+0.11$ Dice (biên độ so với zero-shot lớn hơn, $+0.28$ đến $+0.29$ Dice). Phân tích theo độ khó tác vụ (Bảng~\ref{tab:fracatlas_subcat_baseline}) cho thấy PGA-UNet gần như không đổi giữa nhóm ảnh dễ và khó (theo đánh giá hậu nghiệm của U-Net), trong khi U-Net sụp đổ hoàn toàn ở nhóm khó -- cùng một khuôn mẫu quan sát được trên BTXRD (Bảng~\ref{tab:subcat_baseline}). Điều này củng cố luận điểm rằng lợi ích của cơ chế Prompt Spatial Gate và Conditioned Attention không phải là hiện tượng đặc thù (overfit) của một bộ dữ liệu, mà là đặc tính kiến trúc tổng quát của phương pháp prompt-guided segmentation.
    \item \textbf{Tính bền bỉ trước sai số câu nhắc được tái khẳng định ở cả hai độ phân giải:} Mức sụt Dice Zoom-out $\to$ Shift trên FracAtlas nhỏ ở cả 256 ($-0.0268$) và 512 ($-0.0319$), thấp hơn BTXRD ở cùng độ phân giải ($-0.0372$ tại 256) và thấp hơn cả SAM-Med2D (FT) trên FracAtlas ($-0.0596$), cho thấy cơ chế hướng dẫn mềm (Gaussian heatmap) không chỉ hoạt động tốt mà còn ổn định qua các miền dữ liệu khác nhau.
    \item \textbf{SAM-Med2D fine-tuned cải thiện rõ rệt so với zero-shot, nhưng PGA-UNet vẫn duy trì ưu thế:} Sau khi fine-tuning hội tụ đầy đủ trên FracAtlas (train Dice $0.6266 \to 0.8981$ qua 11 epoch), SAM-Med2D (FT) vượt zero-shot $+0.18$ đến $+0.21$ Dice ở cả ba kịch bản (Bảng~\ref{tab:fracatlas_sam_comparison}), xác nhận chất lượng fine-tuning ảnh hưởng lớn đến hiệu năng của mô hình nền tảng trên miền dữ liệu mới. Dù vậy, PGA-UNet vẫn vượt trội SAM-Med2D (FT) ở toàn bộ ba kịch bản ($+0.08$ đến $+0.11$ Dice), cho thấy lợi thế của kiến trúc nhẹ huấn luyện trực tiếp trên miền đích không chỉ đến từ việc so sánh với một checkpoint chưa hội tụ, mà vẫn được giữ vững khi so sánh với một SAM-Med2D đã fine-tuned đúng cách.
\end{itemize}

% ------------------------------------------------------------
\section{Trực quan hóa kết quả (Qualitative Results)}
\label{sec:truc_quan_hoa}
% ------------------------------------------------------------

Bên cạnh các đánh giá định lượng bằng số liệu thống kê, việc quan sát trực tiếp kết quả phân đoạn mang ý nghĩa đặc biệt quan trọng trong lĩnh vực y khoa lâm sàng. Mục này trình bày các hình ảnh trực quan hóa nhằm minh họa rõ nét cách mô hình PGA-UNet xử lý các trường hợp phức tạp trong thực tế.

\FloatBarrier
\subsection{Trực quan hóa các kịch bản phân đoạn tiêu biểu}

Hình~\ref{fig:vis_pga_sample} minh họa một ca kiểm thử tiêu biểu của PGA-UNet (kịch bản Zoom-out): mặt nạ phân đoạn bám sát đường biên tổn thương nhờ cơ chế Prompt Spatial Gate và Conditioned Attention dẫn đường từ Prompt Heatmap.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_01.png}
    \caption{Trực quan hóa kết quả phân đoạn của PGA-UNet kịch bản Zoom-out: ảnh gốc (trái), mặt nạ Ground Truth (giữa), mặt nạ dự đoán (phải). Đường biên bám sát GT nhờ cơ chế Prompt Spatial Gate và Conditioned Attention.}
    \label{fig:vis_pga_sample}
\end{figure}


% ------------------------------------------------------------
\section{Giới hạn kiến trúc và phân tích trường hợp thất bại}
\label{sec:failure_cases}
% ------------------------------------------------------------

Bên cạnh các kết quả tích cực đã trình bày, việc phân tích khách quan các trường hợp kiến trúc PGA-UNet hoạt động kém là cần thiết để đánh giá đúng giới hạn và định hướng cải tiến.

\FloatBarrier
\subsection{Trường hợp PGA-UNet dự đoán kém}

Qua quá trình phân tích trên tập kiểm thử, ba nhóm trường hợp mà PGA-UNet cho kết quả phân đoạn dưới mức trung bình (Dice $< 0.70$) được nhận diện như sau:

\begin{itemize}
    \item \textbf{Tổn thương có hình dáng bất thường:} Các khối u dạng kéo dài, phân nhánh hoặc có đường biên không liên tục thường vượt quá khả năng biểu diễn của bản đồ nhiệt Gaussian (vốn có dạng hình elip đối xứng). Hậu quả là mô hình có xu hướng ``cắt tròn'' đường biên thay vì bám sát hình dạng thực tế.
    \item \textbf{Vùng chồng lấp đa lớp xương dày đặc:} Tại các vị trí như khớp cổ chân nơi nhiều cấu trúc xương chồng lên nhau, gradient cạnh bị nhiễu nghiêm trọng khiến bộ giải mã gặp khó khăn trong việc xác định ranh giới chính xác, ngay cả khi có prompt hướng dẫn.
    \item \textbf{Ảnh chất lượng thấp:} Các ảnh X-quang bị mờ, thiếu sáng hoặc có nhiễu kỹ thuật còn sót lại sau tiền xử lý đều làm suy giảm chất lượng đặc trưng mà bộ mã hóa trích xuất được.
\end{itemize}

% ============================================================
% PHẦN II, Module Gatekeeper và Pipeline End-to-End
% ============================================================

\section{Đánh giá module Gatekeeper và pipeline end-to-end}
\label{sec:product_overview}

Phần này trình bày đánh giá module Gatekeeper (EfficientNet\_B3), thành phần hỗ trợ được tích hợp để lấp đầy khoảng trống giữa điều kiện huấn luyện và triển khai thực tế, cùng với đánh giá pipeline end-to-end trên dữ liệu hỗn hợp. Phân tích sai số tích lũy của pipeline được trình bày tại Mục~\ref{subsec:cascading_error}.

% ------------------------------------------------------------
\subsection{Đánh giá mô hình phân lớp gác cổng (EfficientNet\_B3)}
\label{sec:danh_gia_phan_lop}
% ------------------------------------------------------------

Mô hình phân lớp đóng vai trò là "người gác cổng" (gatekeeper) trong luồng xử lý đầu cuối của phần mềm. Nhiệm vụ của mô hình là sàng lọc tự động ảnh X-quang đầu vào thành hai nhóm: Bình thường (Âm tính) và Có bệnh lý bất thường (Dương tính). Việc đánh giá chính xác hiệu năng của module này mang ý nghĩa quyết định, bởi một dự đoán sai sót ở bước này có thể khiến bệnh nhân bị lọt lưới sàng lọc mà không bao giờ đi tới được bước phân đoạn chi tiết phía sau.

\textbf{Kiến trúc: EfficientNet\_B3 tinh chỉnh hai giai đoạn.}
Mô hình \textbf{EfficientNet\_B3}~\cite{tan2019efficientnet} được tiền huấn luyện trên ImageNet-1K sử dụng chiến lược \textit{compound scaling}, đồng thời mở rộng chiều rộng, chiều sâu và độ phân giải theo một hệ số hợp lý. Backbone gồm các khối MBConv kết hợp với cơ chế Squeeze-and-Excitation (SE). Với $\sim$10.7M tham số và đầu vào $300\times300$, EfficientNet\_B3 đạt cân bằng tốt giữa hiệu năng phân lớp và tốc độ suy luận. Tầng phân lớp gốc được thay thế bởi $\text{Linear}(1536, 1)$ + Sigmoid, xuất xác suất $p \in (0,1)$; ngưỡng $p \geq 0.5$ → dương tính.

\textit{Giai đoạn 1 (25 epoch):} Đóng băng backbone, chỉ cập nhật head với $\text{LR}=10^{-4}$, giúp head thích nghi không gian đặc trưng X-quang mà không phá hủy biểu diễn ImageNet.

\textit{Giai đoạn 2 (tối đa 100 epoch, dừng sớm tại epoch 68):} Mở khóa toàn bộ, $\text{LR}=10^{-5}$, CosineAnnealingLR. Hàm mất mát: BCEWithLogitsLoss; tối ưu: AdamW; dừng sớm: patience = 15 (theo Accuracy); Gradient Clipping: max\_norm = 1.0. Lưu song song hai checkpoint: theo Loss tốt nhất và theo Accuracy tốt nhất.

\textbf{Các độ đo đánh giá (Evaluation Metrics):}
Để có cái nhìn toàn diện, mô hình được đánh giá qua các độ đo tiêu chuẩn của bài toán phân loại nhị phân: Độ chính xác tổng thể (Accuracy), Độ chính xác (Precision), Độ bao phủ/Độ nhạy (Recall/Sensitivity), Độ đặc hiệu (Specificity) và Điểm F1 (F1-Score). 

Đặc biệt trong bối cảnh y tế dự phòng và sàng lọc, \textbf{Độ nhạy (Recall)} là chỉ số được ưu tiên hàng đầu. Một mô hình sàng lọc tốt thà báo động nhầm (False Positive) để bác sĩ tốn thêm chút thời gian kiểm tra lại ở bước phân đoạn, còn hơn là bỏ sót bệnh nhân thực sự có khối u (False Negative).

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá hiệu năng của mô hình phân lớp sàng lọc trên tập kiểm thử}
    \label{tab:classification_results}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Độ đo đánh giá (Metrics)} & \textbf{Kết quả} \\
        \hline
        Độ chính xác tổng thể (Accuracy) & \textbf{88.00\%} \\
        \hline
        Độ chính xác (Precision / PPV) & \textbf{87.37\%} \\
        \hline
        Độ nhạy (Recall / Sensitivity) & \textbf{88.77\%} \\
        \hline
        Độ đặc hiệu (Specificity) & \textbf{87.23\%} \\
        \hline
        Giá trị dự báo Âm tính (NPV) & \textbf{88.65\%} \\
        \hline
        Điểm F1 (F1-Score) & \textbf{88.06\%} \\
        \hline
        Diện tích dưới đường cong (AUC-ROC) & \textbf{0.9405} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Phân tích kết quả:}
Bảng~\ref{tab:classification_results} tổng hợp hiệu năng của mô hình phân lớp gác cổng sau quá trình tinh chỉnh hai giai đoạn, đánh giá thống nhất trên tập kiểm thử \textbf{dataset\_online} (375 ảnh: 187 bệnh lý + 188 bình thường). Kết quả cho thấy module Gatekeeper đạt mức cân bằng tốt giữa Sensitivity và Specificity:
\begin{itemize}
    \item \textbf{AUC-ROC 0.9405:} Khả năng phân biệt bệnh/không bệnh rất tốt, bất kể ngưỡng phân loại.
    \item \textbf{Recall/Sensitivity 88.77\%:} Phát hiện đúng $\approx$89\% ca bệnh thực sự (bỏ sót 21/187 ca), đảm bảo an toàn sàng lọc.
    \item \textbf{Specificity 87.23\%:} Loại đúng $\approx$87\% ca bình thường (24/188 ảnh lành bị phân loại nhầm thành dương tính).
    \item \textbf{Precision 87.37\%:} Khi báo dương tính, xác suất đúng là $\approx$87\%.
    \item \textbf{NPV 88.65\%:} Khi kết luận "bình thường", xác suất thực sự không bệnh là $\approx$89\%, đủ tin cậy làm bộ lọc sơ cấp.
    \item \textbf{F1-Score 88.06\%:} Cân bằng tốt giữa Precision và Recall, phù hợp với kịch bản dữ liệu cân bằng (187 vs 188).
\end{itemize}

\subsection{Phân tích sai số tích lũy (Cascading Error)}
\label{subsec:cascading_error}

Khi đánh giá \textbf{hệ thống end-to-end} trên tập \textbf{dataset\_online} (375 ảnh hỗn hợp, bao gồm 187 ảnh bệnh lý và 188 ảnh bình thường), sai số từ bước phân lớp lan truyền sang bước phân đoạn qua hai kênh:

\begin{enumerate}
    \item \textbf{Sai số Recall (FN):} Các ca bệnh bị gác cổng bỏ sót → không chuyển xuống PGA-UNet (21 ảnh FN, tương ứng Sensitivity~=~88.77\%).
    \item \textbf{Sai số Specificity (FP):} Các ảnh bình thường bị phân loại nhầm thành dương tính → PGA-UNet chạy nhưng không có GT mask (24 ảnh FP, tương ứng Specificity~=~87.23\%), đóng góp Dice~=~0 vào mẫu số.
\end{enumerate}

Cận trên ước tính của Pipeline Dice (bỏ qua hiệu ứng FP lên mẫu số):
\begin{equation}
    \begin{aligned}
    \text{Pipeline Dice}_{\text{ước tính}} &\approx
        \text{Sensitivity} \times \overline{\text{Dice}}_{\text{TP}} \\
    &= 88.77\% \times 86.35\% \approx \mathbf{76.6\%}
    \end{aligned}
\end{equation}

Kết quả thực nghiệm đo được Pipeline Dice = \textbf{0.7544}, thấp hơn ước tính 76.6\% do ảnh hưởng của FP lên mẫu số: mẫu số thực tế là 190 đơn vị image-level (166 TP + 24 FP), không phải 166 TP đơn thuần. Hai nguồn suy giảm: \textbf{24 ảnh FP} (Specificity~=~87.23\%) gây 24 đơn vị Dice~=~0 trong mẫu số 190, và \textbf{21 ảnh FN} (Sensitivity~=~88.77\%) làm mất 21 ảnh TP khỏi tử số. Cải thiện đồng thời cả Sensitivity lẫn Specificity là hướng ưu tiên tiếp theo. Chi tiết xem Mục~\ref{sec:pipeline_eval} và định hướng cải thiện tại Chương~\ref{Chapter5}.


% ------------------------------------------------------------
\subsection{Đánh giá pipeline end-to-end trên dữ liệu hỗn hợp}
\label{sec:pipeline_eval}
% ------------------------------------------------------------

Để đánh giá pipeline trong điều kiện thực tế, nơi không phải ảnh nào cũng có bệnh lý, hệ thống được chạy trên tập \textbf{dataset\_online} (375 ảnh: 187 ảnh bệnh lý + 188 ảnh bình thường). Tập này được ghép từ \textbf{tập kiểm thử của PGA-UNet} (187 ảnh bệnh lý, có chú thích đa giác) và \textbf{tập kiểm thử của EfficientNet\_B3} (188 ảnh bình thường), cả hai đều thuộc phân vùng kiểm thử BTXRD (10\%), đảm bảo không mô hình nào đã thấy các ảnh này trong quá trình huấn luyện, tránh rò rỉ dữ liệu (data leakage).

\textbf{Thiết kế đánh giá:} Mỗi ảnh đi qua toàn bộ pipeline (EfficientNet\_B3 → PGA-UNet). Kết quả chia thành 4 trường hợp: TP (có JSON, dự đoán dương, PGA chạy thực tế), FP (không có JSON, dự đoán dương, Dice = 0 vì không có GT mask), FN (có JSON, dự đoán âm, không qua PGA, không tính), TN (không có JSON, dự đoán âm).

Dice pipeline được tính trên \textit{toàn bộ đơn vị image-level classifier gửi cho PGA} (mẫu số = ảnh TP + ảnh FP), phản ánh hiệu năng tổng thể khi tích hợp cả trường hợp phân loại sai. Mỗi ảnh TP được tính một Dice image-level (max-merge predictions, union GT), mỗi ảnh FP đóng góp 1 đơn vị với Dice~=~0:
\begin{equation}
    \text{Pipeline Dice} = \frac{\displaystyle\sum_{i \in \text{ảnh TP}} \text{Dice}_{\text{img}}(i)}{N_{\text{ảnh TP}} + N_{\text{ảnh FP}}}
\end{equation}

\begin{table}[htbp]
    \centering
    \caption{Kết quả pipeline end-to-end trên tập dataset\_online (hỗn hợp bình thường + có bệnh)}
    \label{tab:pipeline_eval}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|p{7.5cm}|}
        \hline
        \textbf{Thống kê} & \textbf{Giá trị} \\
        \hline
        Tổng ảnh đầu vào & 375 (187 có bệnh, 188 bình thường) \\
        \hline
        TP / FP / FN / TN (ảnh) & 166 / 24 / 21 / 164 \\
        \hline
        \multicolumn{2}{|c|}{\textit{Phân loại (EfficientNet\_B3 trên dataset\_online)}} \\
        \hline
        Sensitivity (Recall) & \textbf{88.77\%} \\
        \hline
        Specificity & \textbf{87.23\%} \\
        \hline
        Accuracy & \textbf{88.00\%} \\
        \hline
        AUC-ROC & \textbf{0.9405} \\
        \hline
        \multicolumn{2}{|c|}{\textit{Phân đoạn pipeline (PGA-UNet, đánh giá image-level)}} \\
        \hline
        Ảnh TP đưa vào PGA & 166 ảnh (209 polygon, tb. 1.26 polygon/ảnh) \\
        \hline
        Ảnh FP (Dice = 0) & 24 ảnh \\
        \hline
        Mẫu số (ảnh TP + ảnh FP, image-level) & 166 + 24 = \textbf{190 đơn vị} \\
        \hline
        Pipeline Dice & \textbf{0.7544} \\
        \hline
        Pipeline IoU & \textbf{0.6693} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Nhận xét:} Pipeline Dice~=~0.7544 (mẫu số 190 đơn vị image-level: 166 ảnh TP và 24 ảnh FP bị gán Dice~=~0) phản ánh hiệu năng tổng thể khi pipeline xử lý luồng ảnh hỗn hợp theo đúng thiết kế image-level. Con số này thấp hơn PGA-UNet độc lập (Dice~=~0.8584) do hai nguồn suy giảm: (1) 24 ảnh bình thường bị phân loại sai (FP, Specificity~=~87.23\%) kéo mẫu số lên mà không đóng góp vào tử số; (2) 21 ảnh bệnh bị bỏ sót (FN, Sensitivity~=~88.77\%) loại bỏ 21 ảnh TP khỏi tử số. Đây là \textbf{kết quả thực nghiệm đầy đủ} trên dữ liệu hỗn hợp thực tế, minh chứng cho sai số tích lũy tất yếu của kiến trúc hai tầng.

Mô hình phân lớp gác cổng đạt AUC-ROC~=~0.9405 trên dataset\_online là bản lề để toàn bộ hệ thống vận hành ổn định. Cải thiện đồng thời Sensitivity và Specificity (hiện tương đương nhau ở mức $\sim$88\%) nhằm giảm cả FN lẫn FP là hướng ưu tiên phát triển tiếp theo (xem Chương~\ref{Chapter5}).

