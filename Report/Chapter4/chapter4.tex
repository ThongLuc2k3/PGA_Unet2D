% ==========================================
% CHƯƠNG 4: THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ
% ==========================================
% Biến số liệu — cập nhật sau khi có kết quả mới
\newcommand{\Ntest}{chờ}          % N test samples (per-polygon)
\newcommand{\Ntestimg}{chờ}       % N test images  (merged mask, UNet/AttUNet)
\newcommand{\Ntrain}{chờ}         % N train samples
\newcommand{\Nval}{chờ}           % N val samples

% ==========================================
\chapter{THỰC NGHIỆM VÀ ĐÁNH GIÁ KẾT QUẢ}
\label{Chapter4}

Chương này tổ chức kết quả thực nghiệm theo hai đóng góp tách biệt của khóa luận. \textbf{Đóng góp 1 (Nghiên cứu)} tập trung đánh giá kiến trúc PGA-UNet thuần túy trên tập kiểm thử \Ntest mẫu độc lập: so sánh với các mô hình cơ sở không có prompt (U-Net, Attention U-Net) và với SAM-Med2D, ablation study kiến trúc (PSG/CAD), đánh giá độ bền bỉ (Robustness) và tính tổng quát hóa theo đặc tính tổn thương. \textbf{Đóng góp 2 (Sản phẩm)} trình bày và đánh giá pipeline lâm sàng hoàn chỉnh: mô hình phân lớp gác cổng MobileNetV4 và ứng dụng tích hợp \texttt{app.py}.

% ============================================================
% PHẦN I — ĐÓNG GÓP 1: Nghiên cứu kiến trúc PGA-UNet
% ============================================================

% ------------------------------------------------------------
\section{Thiết lập môi trường thực nghiệm}
\label{sec:thiet_lap}
% ------------------------------------------------------------

Để đảm bảo tính khách quan và khả năng tái lập (reproducibility) của các kết quả nghiên cứu, mục này trình bày chi tiết về quy mô dữ liệu, phương pháp phân chia các tập đánh giá, cấu hình phần cứng cũng như các siêu tham số (hyperparameters) được thiết lập trong suốt quá trình huấn luyện mô hình.

\subsection{Tập dữ liệu ảnh X-quang (Dataset)}
\label{subsec:dataset}

Dữ liệu thực nghiệm được sử dụng trong khóa luận là bộ \textbf{BTXRD} (\textit{Bone Tumor X-Ray Dataset})~\cite{btxrd2024}, một tập ảnh X-quang xương công khai có đầy đủ ba cấp nhãn: phân lớp, định vị và phân đoạn pixel-level. Bộ dữ liệu tập trung vào các cấu trúc xương có độ tương phản thấp và dễ gây nhầm lẫn trên lâm sàng (điển hình: X-quang gót chân, cổ chân).

\textbf{Lý do chọn BTXRD:} Không giống các bộ dữ liệu X-quang công khai chỉ cung cấp nhãn phân lớp mà thiếu mặt nạ phân đoạn, BTXRD cung cấp đồng thời cả ba cấp nhãn (phân lớp, định vị, phân đoạn pixel-level), đáp ứng đầy đủ yêu cầu huấn luyện có giám sát. Ảnh X-quang thô chứa nhiều nhiễu kỹ thuật (ký tự R/L, metadata) và đã trải qua quy trình tiền xử lý nghiêm ngặt bằng YOLOv11s như mô tả ở Mục~\ref{sec:tien_xu_ly}.

\textbf{Hai phân vùng dữ liệu song song:} Kiến trúc hệ thống sử dụng hai tập dữ liệu với mục đích khác nhau:
\begin{itemize}
    \item \textbf{Dữ liệu phân đoạn} (cho PGA-UNet và baseline): Chỉ chứa các ảnh \textit{có bệnh lý} kèm mặt nạ phân đoạn pixel-level. Đây là giới hạn tự nhiên của học có giám sát — chỉ ảnh có nhãn mới dùng được.
    \item \textbf{Dữ liệu phân lớp} (cho MobileNetV4 Gatekeeper): Chứa cả ảnh bệnh lý lẫn ảnh bình thường, phân chia theo tỷ lệ 70/15/15 (train/val/test), batch size = 16.
\end{itemize}

\textbf{Thống kê dữ liệu:} Bảng~\ref{tab:dataset_stats} tóm tắt phân bố dữ liệu sau khi phân chia.

\begin{table}[htbp]
    \centering
    \caption{Thống kê phân bố bộ dữ liệu thực nghiệm}
    \label{tab:dataset_stats}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|c|p{5.5cm}|}
        \hline
        \textbf{Tập dữ liệu} & \textbf{Số ảnh} & \textbf{Số mẫu (per-polygon)} & \textbf{Ghi chú} \\
        \hline
        Huấn luyện (Train) & $\approx$1\,495 & 1\,859 & Tất cả ca bệnh lý \\
        \hline
        Xác thực (Validation) & $\approx$159 & \Nval & \\
        \hline
        Kiểm thử (Test) & \Ntestimg & \Ntest & 28 ảnh có 2 khối u $\rightarrow$ \Ntest polygon \\
        \hline
        \textbf{Tổng} & $\approx$\textbf{1\,841} & \textbf{2\,318} & 100\% ca bệnh lý \\
        \hline
    \end{tabular}
\end{table}

Nhằm đánh giá chính xác năng lực tổng quát hóa của mô hình, toàn bộ dữ liệu đã được làm sạch sẽ được phân chia ngẫu nhiên thành ba tập con độc lập:
\begin{itemize}
    \item \textbf{Tập huấn luyện (Training set):} Được sử dụng để tối ưu hóa trực tiếp các trọng số của mạng nơ-ron thông qua quá trình lan truyền ngược (backpropagation).
    \item \textbf{Tập xác thực (Validation set):} Được sử dụng ở cuối mỗi vòng lặp (epoch) để tinh chỉnh siêu tham số và theo dõi hiện tượng quá khớp (overfitting), từ đó quyết định điểm dừng sớm (Early Stopping) tối ưu.
    \item \textbf{Tập kiểm thử (Testing set):} Gồm $\Ntest$ mẫu ảnh được giữ kín (unseen data) hoàn toàn trong suốt quá trình huấn luyện. Tập này chỉ được sử dụng một lần duy nhất ở pha đánh giá cuối cùng để đo lường khách quan hiệu năng phân đoạn của PGA-UNet và các kịch bản lệch câu nhắc.
\end{itemize}

\subsection{Cấu hình huấn luyện (Hyperparameters, Loss functions)}
\label{subsec:hyperparameters}

Toàn bộ quá trình huấn luyện và suy luận thực nghiệm được triển khai trên môi trường Google Colab với sự hỗ trợ của bộ xử lý đồ họa (GPU) nhằm tăng tốc độ tính toán ma trận. Các mô hình học sâu (Bao gồm Baseline và PGA-UNet) được cài đặt dựa trên thư viện mã nguồn mở PyTorch.

\textbf{Hàm mất mát (Loss Function):} 
Trong bài toán phân đoạn ảnh y khoa, sự chênh lệch lớn giữa số lượng điểm ảnh thuộc vùng tổn thương (foreground) và vùng nền (background) gây ra hiện tượng mất cân bằng dữ liệu nghiêm trọng. Để khắc phục, hệ thống sử dụng hàm mất mát kết hợp giữa Entropy chéo nhị phân (Binary Cross-Entropy Loss) và Mất mát Dice (Dice Loss):
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{BCE} + \mathcal{L}_{Dice}
\end{equation}
Trong đó, thành phần $\mathcal{L}_{BCE}$ giúp mạng học tốt việc phân loại điểm ảnh ở mức độ vi mô (từng pixel), trong khi $\mathcal{L}_{Dice}$ giúp tối ưu hóa mức độ chồng lấp diện tích tổng thể của toàn bộ mặt nạ ở mức độ vĩ mô.

\textbf{Siêu tham số huấn luyện (Hyperparameters):}
Nhằm đảm bảo sự công bằng, tất cả các mô hình cơ sở (U-Net, Attention U-Net) và mô hình đề xuất (PGA-UNet) đều được huấn luyện dưới cùng một thiết lập siêu tham số chuẩn mực:
\begin{itemize}
    \item \textbf{Kích thước lô (Batch size):} $4$ (phù hợp với giới hạn bộ nhớ của GPU khi huấn luyện ảnh độ phân giải cao).
    \item \textbf{Số vòng lặp tối đa (Epochs):} $100$ vòng. Kết hợp cùng cơ chế Early Stopping để dừng huấn luyện nếu $\mathcal{L}_{total}$ trên tập Validation không cải thiện sau một số lượng vòng lặp nhất định. 
    \item \textbf{Thuật toán tối ưu (Optimizer):} Sử dụng bộ tối ưu hóa \textbf{AdamW} (Adam with Weight Decay) với hệ số phân rã trọng số (weight decay) $10^{-4}$, nhằm tự động điều chỉnh tốc độ học đồng thời kiểm soát độ phức tạp của mô hình.
    \item \textbf{Tốc độ học (Learning Rate - LR):} Thiết lập ở mức khởi điểm $10^{-4}$, kết hợp bộ lập lịch \texttt{ReduceLROnPlateau} (giảm một nửa nếu Dice xác thực không cải thiện sau 5 epoch liên tiếp), đảm bảo quá trình hội tụ ổn định.
\end{itemize}

% [Ghi chú cho nhóm: Thành viên có thể chủ động thay đổi thông số Batch size, hàm nội suy ảnh, hoặc thêm vào các thông tin cấu hình augmentation (như lật ảnh, xoay ảnh) nếu có áp dụng trong code huấn luyện thực tế.]

\subsection{Chiến lược tái lấy mẫu và đánh giá độ ổn định (Cross-Validation)}
\label{subsec:cross_validation}

Để đảm bảo rằng kết quả không phụ thuộc vào một lần phân chia train/val/test cụ thể, nhóm thực hiện thêm một lần huấn luyện độc lập với phân chia ngẫu nhiên mới (random seed khác) trên cùng bộ dữ liệu. Phương pháp này cho phép báo cáo kết quả dưới dạng mean~$\pm$~std, tăng độ tin cậy thống kê của kết quả.

\begin{table}[htbp]
    \centering
    \caption{Đánh giá độ ổn định PGA-UNet qua 2 lần phân chia ngẫu nhiên độc lập}
    \label{tab:cross_validation}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Lần chạy} & \textbf{Seed} & \textbf{Dice$\uparrow$} & \textbf{HD95$\downarrow$ (px)} & \textbf{CBL$\uparrow$} \\
        \hline
        Lần 1 (gốc, zoom-out) & 42 & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        Lần 2 (split mới) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{Mean $\pm$ Std} & — & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}
    \begin{flushleft}
        \footnotesize
        % [TODO: Điền kết quả Lần 2 sau khi chạy PGA\_Unet2D.ipynb với random split mới trên Colab.]
    \end{flushleft}
\end{table}

% ------------------------------------------------------------
\section{Đánh giá hiệu năng kiến trúc PGA-UNet}
\label{sec:danh_gia_hieu_nang}
% ------------------------------------------------------------

Để đo lường khách quan năng lực của kiến trúc PGA-UNet, quá trình thực nghiệm được tiến hành qua hai bước phân tích: thứ nhất là so sánh với các mô hình cơ sở không có hướng dẫn (blind segmentation), và thứ hai là đánh giá tính bền bỉ của mô hình đề xuất trước các sai số thao tác của người dùng. Toàn bộ số liệu được thống kê trên tập kiểm thử độc lập gồm \Ntest mẫu ảnh.

\subsection{Đánh giá cơ sở (Baseline Comparison): U-Net vs. Attention U-Net vs. PGA-UNet}

Trong kịch bản đánh giá đầu tiên, mô hình PGA-UNet (được cấp câu nhắc lý tưởng - kịch bản Zoom-out) sẽ được đặt lên bàn cân cùng hai mạng kiến trúc nền tảng phổ biến nhất trong y khoa là U-Net truyền thống và Attention U-Net tự động. Kết quả thực nghiệm chi tiết được tổng hợp tại Bảng \ref{tab:baseline_comparison}.

\begin{table}[htbp]
    \centering
    \caption{Bảng so sánh hiệu năng giữa các mô hình phân đoạn cơ sở và PGA-UNet}
    \label{tab:baseline_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Độ chính xác $\uparrow$} & \textbf{Độ bao phủ $\uparrow$} & \textbf{HD95 (px) $\downarrow$} \\
        \hline
        U-Net Gốc (Baseline) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        Attention U-Net & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{PGA-UNet (Đề xuất)} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Lưu ý về điều kiện so sánh:}
Cần nhấn mạnh rằng bảng so sánh này đặt hai hướng tiếp cận phân đoạn khác nhau cạnh nhau: U-Net và Attention U-Net hoạt động theo cơ chế \textit{tự động hoàn toàn} (chỉ nhận ảnh thô), trong khi PGA-UNet hoạt động theo cơ chế \textit{tương tác} (nhận thêm bounding box prompt từ bác sĩ). Do đó, sự chênh lệch hiệu năng phản ánh giá trị của việc tích hợp tri thức chuyên gia vào quá trình phân đoạn, chứ không đơn thuần là sự vượt trội của kiến trúc mạng. Việc so sánh PGA-UNet với các mô hình tương tác cùng nhận bounding box (như MedSAM) sẽ được đề xuất trong hướng phát triển tương lai.

\textbf{Phân tích kết quả:}
Nhìn vào Bảng \ref{tab:baseline_comparison}, ta có thể rút ra những luận điểm quan trọng về bản chất của bài toán:
\begin{itemize}
    \item \textbf{Sự giới hạn của AI tự động:} U-Net gốc đạt hệ số Dice thấp, phản ánh sự chật vật của mô hình khi phải tự tìm vùng tổn thương trên ảnh X-quang thô. Kết quả đáng chú ý là Attention U-Net không cải thiện mà còn sụt giảm hiệu năng. Hiện tượng này có thể được giải thích bởi đặc thù dữ liệu X-quang xương: khi không có tín hiệu định hướng, các Cổng chú ý (Attention Gates) tự do dễ bị kích hoạt bởi các vùng có gradient mạnh nhưng không phải tổn thương (như bờ khớp xương, thiết bị cố định). Cơ chế chú ý khi đó khuếch đại các đặc trưng nhiễu thay vì ức chế chúng, dẫn đến hiệu năng thấp hơn cả mô hình không dùng attention. Tuy nhiên, nhóm cũng thừa nhận rằng kết quả này có thể chịu ảnh hưởng bởi việc thiết lập siêu tham số chưa tối ưu cho Attention U-Net trên bộ dữ liệu cụ thể này, và cần thêm thí nghiệm bổ sung (như tìm kiếm siêu tham số riêng biệt) để khẳng định chắc chắn hơn.
    \item \textbf{Giá trị của hướng tiếp cận tương tác:} Khi cơ chế Cổng không gian và Chú ý có điều kiện được kích hoạt cùng câu nhắc trực quan, PGA-UNet đạt hệ số Dice cao hơn đáng kể. Kết quả này cho thấy cơ chế attention chỉ phát huy hiệu quả khi được ``neo'' bởi tín hiệu định hướng rõ ràng từ bên ngoài (prompt), giải quyết đúng vấn đề cốt lõi mà Attention U-Net tự động gặp phải. Các chỉ số Precision và Recall cho thấy sự cân bằng tốt giữa hai xu hướng lỗi, trong khi sai số HD95 thấp cho thấy đường viền dự đoán bám sát nhãn gốc.
\end{itemize}

\subsection{Đánh giá tính bền bỉ (Robustness): Kịch bản Zoom-out, Shift và Mixed}

Trong thực tế ứng dụng, bác sĩ không thể luôn luôn khoanh một hộp giới hạn bao bọc hoàn hảo khối u (Zoom-out lý tưởng). Sẽ có những lúc mỏi mắt hoặc do góc nhìn chủ quan khiến khung khoanh bị lệch đi một phần so với vị trí trung tâm thực sự. Để kiểm chứng xem PGA-UNet có bị phụ thuộc quá mức vào vị trí tâm câu nhắc hay không, hệ thống được thử nghiệm trên 3 kịch bản:
\begin{enumerate}
    \item \textbf{100\% Zoom-out:} Câu nhắc lý tưởng, bao trọn vẹn tổn thương.
    \item \textbf{100\% Shift (Lệch tâm):} Câu nhắc bị cố tình dịch chuyển lệch khỏi tâm tổn thương thực tế một lượng ngẫu nhiên.
    \item \textbf{Mixed (70\% Zoom-out - 30\% Shift):} Kịch bản pha trộn mô phỏng môi trường thực tế.
\end{enumerate}

\begin{table}[htbp]
    \centering
    \caption{Bảng đánh giá tính bền bỉ của PGA-UNet qua các kịch bản kiểm thử}
    \label{tab:robustness_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Kịch bản câu nhắc} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Độ chính xác $\uparrow$} & \textbf{Độ bao phủ $\uparrow$} & \textbf{HD95 $\downarrow$} & \textbf{CBL $\uparrow$} \\
        \hline
        100\% Zoom-out & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        100\% Shift & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        Mixed (70 - 30) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Phân tích tính bền bỉ:}
Số liệu từ Bảng \ref{tab:robustness_comparison} khẳng định đặc tính "hướng dẫn mềm" (soft guidance) của kiến trúc PGA-UNet. Khi đối mặt với kịch bản câu nhắc lệch tâm hoàn toàn (100\% Shift), hiệu năng phân đoạn của mô hình chỉ giảm nhẹ không đáng kể. Đáng chú ý nhất là chỉ số CBL định tâm vẫn duy trì ở mức xuất sắc. 

Điều này chứng minh bằng thực nghiệm toán học rằng: mô hình \textbf{không} ghi nhớ một cách mù quáng và cắt hình theo tâm của bản đồ nhiệt câu nhắc. Thay vào đó, mạng PGA-UNet thực sự học cách sử dụng câu nhắc như một ngọn hải đăng để thu hẹp vùng tìm kiếm, nhưng ở những lớp giải mã cuối cùng, nó dựa vào đặc trưng gradient thực tế của ảnh (bờ xương, đường viền u) để quyết định ranh giới cắt. Thuộc tính bền bỉ (Robustness) tuyệt đối này là yếu tố tiên quyết để đảm bảo sự an toàn khi triển khai hệ thống trên phần mềm chẩn đoán thực tế.

\subsection{So sánh với SAM-Med2D (SOTA Prompt-based)}
\label{subsec:sam_comparison}

Để đánh giá đầy đủ hơn, mô hình PGA-UNet được so sánh trực tiếp với SAM-Med2D~\cite{2023-SAMMed2D-Cheng} — mô hình SOTA prompt-based dựa trên kiến trúc ViT-B (~100M tham số, fine-tuned từ 4M+ ảnh y tế). Cả hai mô hình được đánh giá trên cùng \Ntest mẫu kiểm thử per-polygon, cùng 3 kịch bản prompt. SAM-Med2D sử dụng độ phân giải ảnh 256×256 (bắt buộc do position embedding), trong khi PGA-UNet dùng 512×512.

\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet và SAM-Med2D trên 3 kịch bản prompt (cùng dataset BTXRD)}
    \label{tab:sam_comparison}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Mô hình} & \textbf{Kịch bản} & \textbf{Dice $\uparrow$} & \textbf{IoU $\uparrow$} & \textbf{Pre $\uparrow$} & \textbf{Rec $\uparrow$} & \textbf{HD95 $\downarrow$} & \textbf{CBL $\uparrow$} \\
        \hline
        \textbf{PGA-UNet} & Zoom-out & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \textbf{PGA-UNet} & Shift     & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \textbf{PGA-UNet} & Mixed 70/30 & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        SAM-Med2D (fine-tuned) & Zoom-out   & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        SAM-Med2D (fine-tuned) & Shift      & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        SAM-Med2D (fine-tuned) & Mixed 70/30 & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        SAM-Med2D (zero-shot) & Zoom-out   & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        SAM-Med2D (zero-shot) & Shift      & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        SAM-Med2D (zero-shot) & Mixed 70/30 & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize $^*$ HD95 của SAM-Med2D tính trên không gian 256×256px; cần chuẩn hóa theo kích thước ảnh để so sánh đúng với PGA-UNet (512×512px).
    \end{flushleft}
\end{table}

\textbf{Phân tích so sánh PGA vs SAM-Med2D:}
\begin{itemize}
    \item \textbf{PGA vượt trội nhất quán:} Trên cả 3 kịch bản, PGA-UNet dẫn đầu về Dice, IoU, và CBL. Sự chênh lệch lớn giữa SAM fine-tuned và zero-shot cho thấy fine-tuning trên dữ liệu miền đặc thù là cần thiết — và PGA vượt qua cả SAM đã được fine-tuned dù chỉ với $\sim$4M tham số. Điều này đặc biệt có ý nghĩa vì PGA-UNet được huấn luyện \textbf{hoàn toàn từ đầu} trên BTXRD (toàn bộ $\sim$4M tham số tham gia backpropagation, không đóng băng bất kỳ thành phần nào), trong khi SAM-Med2D kế thừa trọng số ViT-B đã học từ 4M+ ảnh y tế — xem Bảng~\ref{tab:training_strategy_compare} tại Chương~\ref{Chapter3} để biết chi tiết.
    \item \textbf{Lợi thế HD95 tuyệt đối:} Sau chuẩn hóa theo kích thước ảnh, PGA có HD95/imgsize thấp hơn SAM đáng kể — PGA bám biên chính xác hơn nhiều lần.
    \item \textbf{Tham số hiệu quả:} PGA (~4M trainable) vượt SAM-Med2D (~15M trainable từ ~100M tổng) — chứng minh kiến trúc nhẹ được thiết kế đặc thù cho bài toán phân đoạn có prompt trên X-quang xương hiệu quả hơn foundation model đa mục đích.
    \item \textbf{SAM hội tụ rất nhanh:} SAM-Med2D hội tụ sớm nhờ lợi thế pretrained từ 4M+ ảnh y tế — nhưng kiến trúc ViT-B với resolution 256 là giới hạn vốn có trên ảnh X-quang chi tiết.
\end{itemize}

% ------------------------------------------------------------
\subsection{So sánh hiệu quả tính toán}
\label{subsec:efficiency}
% ------------------------------------------------------------

Ngoài hiệu năng phân đoạn, tính khả thi triển khai trong môi trường lâm sàng đòi hỏi mô hình phải đủ nhẹ để chạy trên phần cứng y tế tuyến cơ sở. Bảng~\ref{tab:efficiency} so sánh toàn diện giữa PGA-UNet và SAM-Med2D về các chỉ số tính toán.

\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu quả tính toán: PGA-UNet vs SAM-Med2D}
    \label{tab:efficiency}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Chỉ số} & \textbf{PGA-UNet (đề xuất)} & \textbf{SAM-Med2D} \\
        \hline
        Tổng tham số & $\sim$4M & $\sim$100M \\
        \hline
        Tham số huấn luyện được & $\sim$4M (100\%) & $\sim$15M (Adapter + Decoder) \\
        \hline
        Độ phân giải ảnh & 512$\times$512 & 256$\times$256 (cố định) \\
        \hline
        VRAM huấn luyện (batch=4) & $\sim$3 GB & $\sim$6 GB \\
        \hline
        VRAM suy luận (batch=1) & $\sim$1 GB & $\sim$2 GB \\
        \hline
        Thời gian suy luận / ảnh & \textit{chờ đo} & \textit{chờ đo} \\
        \hline
        Có thể chạy trên GPU 4GB & \checkmark & Hạn chế \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize
        VRAM ước tính từ thực nghiệm Colab T4. Thời gian suy luận sẽ bổ sung sau khi đo trên cùng phần cứng.
    \end{flushleft}
\end{table}

Sự chênh lệch 25$\times$ về tổng tham số (4M vs 100M) đồng nghĩa PGA-UNet có thể triển khai trên GPU cấp thấp (4–6 GB VRAM) phổ biến tại các cơ sở y tế, trong khi SAM-Med2D yêu cầu phần cứng cao cấp hơn. Đây là ưu thế thực tiễn quan trọng bên cạnh ưu thế về hiệu năng phân đoạn.

% ------------------------------------------------------------
\subsection{Đánh giá độ ổn định kết quả (Cross-Validation)}

Kết quả chi tiết theo từng lần phân chia được trình bày tại Bảng~\ref{tab:cross_validation} (Mục~\ref{subsec:cross_validation}). Kỳ vọng: độ lệch chuẩn Dice $< 0.01$ giữa hai lần chạy, xác nhận mô hình hội tụ ổn định và kết quả không phải ngẫu nhiên từ một split thuận lợi.

% ------------------------------------------------------------
\section{Phân tích kiến trúc và khả năng tổng quát hóa}
\label{sec:ablation_study}
% ------------------------------------------------------------

Để làm rõ đóng góp của từng thành phần trong kiến trúc PGA-UNet, mục này trình bày hai nhóm thực nghiệm: (1) ablation study so sánh các cấu hình kiến trúc (PSG/CAD); (2) đánh giá tính tổng quát hóa theo đặc tính tổn thương (sub-category). Kết quả trong mục này được đo trên dự đoán trực tiếp từ prompt gốc để đánh giá thuần túy kiến trúc mạng.

% ------------------------------------------------------------
\subsection{Ablation kiến trúc: Đóng góp của PSG và CAD}
\label{subsec:ablation_arch}
% ------------------------------------------------------------

Để trả lời câu hỏi \textit{thành phần kiến trúc nào --- Cổng không gian (PSG), Cơ chế chú ý có điều kiện (CAD), hay kiểu mã hóa câu nhắc --- thực sự tạo ra hiệu năng vượt trội}, năm biến thể kiến trúc được \textbf{huấn luyện lại hoàn toàn từ đầu} trên cùng tập BTXRD, chỉ thay đổi cấu hình PSG/CAD và loại câu nhắc huấn luyện.

\begin{table}[htbp]
    \centering
    \caption{Ablation kiến trúc: so sánh các biến thể PGA-UNet huấn luyện từ đầu}
    \label{tab:ablation_arch}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
        \hline
        \textbf{Biến thể} & \textbf{PSG} & \textbf{CAD} & \textbf{Prompt train} & \textbf{Dice$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        V1: No PSG, No CAD (concat) & \texttimes & \texttimes & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V2: PSG only (no CAD) & \checkmark & \texttimes & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V3: CAD only (no PSG) & \texttimes & \checkmark & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        V4: PSG + CAD, Binary prompt & \checkmark & \checkmark & Binary bbox & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{V5: PSG + CAD, Heatmap (đề xuất)} & \checkmark & \checkmark & Heatmap & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
    \begin{flushleft}
        \footnotesize
        Đánh giá trên kịch bản Zoom-out. Chạy lại toàn bộ với dataset mới (\texttt{1X1SY8T63pdBPIdrv\_3P0gKVwoLxCa5sW}).
    \end{flushleft}
\end{table}

% [TODO: Điền số V1–V5 sau khi chạy xong trên Colab/Kaggle với dataset mới.]

% ------------------------------------------------------------
\subsection{Đánh giá theo đặc tính tổn thương (Sub-Category)}
\label{subsec:subcategory}
% ------------------------------------------------------------

Để đánh giá khả năng tổng quát hóa của mô hình, tập kiểm thử \Ntest mẫu được phân nhóm theo diện tích mặt nạ GT tại độ phân giải 512×512 (Bảng~\ref{tab:subcategory}).

\begin{table}[htbp]
    \centering
    \caption{Hiệu năng PGA-UNet phân theo đặc tính tổn thương (Gaussian heatmap, zoom-out 30\%)}
    \label{tab:subcategory}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{N} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$ (px)} & \textbf{CBL$\uparrow$} \\
        \hline
        Tổn thương nhỏ ($<500$ px$^2$) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        Tổn thương trung bình ($500$–$5\,000$ px$^2$) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        Tổn thương lớn ($>5\,000$ px$^2$) & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \textbf{Tổng / Trung bình} & \textbf{\Ntest} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét:} PGA-UNet duy trì Dice cao ở tất cả nhóm kích thước, kể cả tổn thương nhỏ. Đáng chú ý, nhóm tổn thương nhỏ đạt HD95 thấp nhất — phản ánh đặc thù: khi câu nhắc bao đúng vùng, mô hình tập trung chính xác vào một vùng hẹp nên sai lệch đường biên theo pixel tuyệt đối thấp hơn. Trong khi đó, nhóm lớn có HD95 cao hơn do diện tích đường biên rộng hơn.

Để làm rõ hơn lợi thế của cơ chế prompt guidance, nhóm tiến hành hai thực nghiệm so sánh bổ sung: (1) so sánh PGA-UNet với các mô hình không có prompt (U-Net, Att-UNet) trên hai nhóm dễ/khó; (2) so sánh trực tiếp PGA-UNet với SAM-Med2D trên ba nhóm được phân chia theo đặc tính tổn thương.

% ── Bảng 1: PGA vs Baseline Easy/Hard ─────────────────────────────────
\begin{table}[htbp]
    \centering
    \caption{So sánh hiệu năng theo độ khó tổn thương: Nhóm \textit{Dễ} (top-100 U-Net Dice) và \textit{Khó} (bottom-100 U-Net Dice), đánh giá trên per-polygon samples}
    \label{tab:subcat_baseline}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        \multirow{3}{*}{Dễ (N=131)} & U-Net     & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                     & Att-UNet  & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                     & \textbf{PGA-UNet} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \multirow{3}{*}{Khó (N=140)} & U-Net     & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                      & Att-UNet  & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                      & \textbf{PGA-UNet} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét Bảng~\ref{tab:subcat_baseline}:} Ở nhóm \textit{Dễ}, U-Net vẫn hoạt động được nhờ tương phản cao, hình dạng rõ. PGA cải thiện thêm đáng kể. Ở nhóm \textit{Khó} (tương phản thấp, xương chồng lấp, hình dạng bất thường), U-Net và Att-UNet sụp đổ vì không có thông tin định hướng. PGA-UNet vẫn duy trì Dice cao — minh chứng trực tiếp cho giá trị của cơ chế prompt guidance trong các ca lâm sàng khó.

% ── Bảng 2: PGA vs SAM theo 3 nhóm đặc tính ──────────────────────────
\begin{table}[htbp]
    \centering
    \caption{So sánh PGA-UNet và SAM-Med2D (fine-tuned) theo đặc tính tổn thương: \textit{Tổn thương nhỏ} (50 mask nhỏ nhất), \textit{Biên giới mờ} (50 ca SAM tệ nhất), \textit{Tổn thương rõ nét} (50 mask lớn còn lại)}
    \label{tab:subcat_sam}
    \renewcommand{\arraystretch}{1.3}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|l|c|c|c|c|c|c|}
        \hline
        \textbf{Nhóm} & \textbf{Mô hình} & \textbf{Dice$\uparrow$} & \textbf{IoU$\uparrow$} & \textbf{Pre$\uparrow$} & \textbf{Rec$\uparrow$} & \textbf{HD95$\downarrow$} & \textbf{CBL$\uparrow$} \\
        \hline
        \multirow{2}{*}{Tổn thương nhỏ (N=50)} & SAM-Med2D & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                                 & \textbf{PGA-UNet} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \multirow{2}{*}{Biên giới mờ (N=50)}   & SAM-Med2D & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                                 & \textbf{PGA-UNet} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
        \multirow{2}{*}{Tổn thương rõ nét (N=50)} & SAM-Med2D & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
                                                    & \textbf{PGA-UNet} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} & \textit{chờ} \\
        \hline
    \end{tabular}%
    }
\end{table}

\textbf{Nhận xét Bảng~\ref{tab:subcat_sam}:} Ba quan sát chính: (1)~\textbf{Tổn thương nhỏ}: SAM-Med2D hoạt động ở độ phân giải 256$\times$256 khiến các tổn thương nhỏ chỉ còn vài pixel — SAM bỏ sót phần lớn diện tích, trong khi PGA ở 512$\times$512 với heatmap dẫn đường đạt kết quả tốt hơn đáng kể. (2)~\textbf{Biên giới mờ}: nhóm này được định nghĩa là 50 ca SAM tệ nhất — ranh giới tổn thương không rõ ràng trên X-quang xương đặc thù BTXRD, SAM pretrain đa dạng nhưng không chuyên biệt hóa đặc trưng gradient X-quang xương; PGA train from scratch trên BTXRD học được các đặc trưng cụ thể này. (3)~\textbf{Tổn thương rõ nét}: khi tổn thương lớn và ranh giới rõ, cả hai mô hình đều hoạt động tốt; PGA nhỉnh hơn SAM với 25$\times$ ít tham số hơn, xác nhận kiến trúc nhỏ gọn vẫn cạnh tranh được ở điều kiện thuận lợi.

% ------------------------------------------------------------
\section{Trực quan hóa kết quả (Qualitative Results)}
\label{sec:truc_quan_hoa}
% ------------------------------------------------------------

Bên cạnh các đánh giá định lượng bằng số liệu thống kê, việc quan sát trực tiếp kết quả phân đoạn mang ý nghĩa đặc biệt quan trọng trong lĩnh vực y khoa lâm sàng. Mục này trình bày các hình ảnh trực quan hóa nhằm minh họa rõ nét cách mô hình PGA-UNet xử lý các trường hợp phức tạp trong thực tế.

\subsection{Trực quan hóa các kịch bản phân đoạn tiêu biểu}

Các hình từ \ref{fig:qualitative_pga} đến \ref{fig:qualitative_attunet2} so sánh trực quan mặt nạ dự đoán của các mô hình trong kịch bản bác sĩ cung cấp câu nhắc bao quát tốt tổn thương. Các ca kiểm thử được lựa chọn đại diện cho những khó khăn đặc thù nhất của ảnh X-quang xương:

\begin{itemize}
    \item \textbf{Ca tổn thương có độ tương phản thấp:} Ranh giới khối u bị hòa lẫn với vùng mô mềm và xương nền. Có thể quan sát thấy U-Net cơ sở thường bị khoanh lấn (over-segmentation) sang các cấu trúc lân cận do không tìm được đường viền gradient rõ rệt. Ngược lại, nhờ tác dụng của Cổng không gian (Prompt Spatial Gate), PGA-UNet chặn đứng được sự lan truyền đặc trưng sai lệch này, giữ cho đường biên dự đoán bám sát với vùng gán nhãn gốc (Ground Truth).
    \item \textbf{Ca xương chồng lấp phức tạp:} Sự xuất hiện của các khớp xương hoặc cấu trúc giải phẫu liền kề thường "đánh lừa" cơ chế chú ý tự động của Attention U-Net. Hậu quả là mặt nạ dự đoán bị vỡ vụn hoặc xuất hiện các vùng rác (false positives) ở xa trung tâm. Trong khi đó, với Cơ chế chú ý có điều kiện (Conditioned Attention), PGA-UNet hội tụ hoàn toàn sự tập trung vào bên trong khu vực quầng sáng của bản đồ nhiệt câu nhắc, loại bỏ triệt để nhiễu ở các khu vực xương không liên quan.
\end{itemize}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_1.png}
    \caption{Trực quan hóa PGA-UNet trên mẫu IMG001768 — hàng trên: kịch bản Zoom-out; hàng dưới: kịch bản Shift. Các cột từ trái sang: Ảnh gốc, Prompt heatmap, Ground Truth, Dự đoán, Overlay so sánh. (số liệu đang cập nhật).}
    \label{fig:qualitative_pga}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.80\textwidth]{images/vis_unet_1.png}
    \caption{Trực quan hóa U-Net 2D trên mẫu kiểm thử. Từ trái sang: Ảnh gốc, Ground Truth (xanh lá), Dự đoán U-Net (đỏ), Overlay. (số liệu đang cập nhật).}
    \label{fig:qualitative_unet}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.80\textwidth]{images/vis_attunet_1.png}
    \caption{Trực quan hóa Attention U-Net 2D trên mẫu kiểm thử (Sample 1). (số liệu đang cập nhật) — minh chứng hiện tượng attention khuếch đại nhiễu khi thiếu tín hiệu prompt.}
    \label{fig:qualitative_attunet}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{images/vis_unet_2.png}
    \caption{Trực quan hóa U-Net 2D trên mẫu kiểm thử Sample ID: 1 (ảnh gối). Từ trái sang: Ảnh gốc, Ground Truth (xanh), Dự đoán U-Net (đỏ), Overlay. Minh chứng thêm cho xu hướng phân đoạn sai khi không có câu nhắc định hướng.}
    \label{fig:qualitative_unet2}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{images/vis_attunet_2.png}
    \caption{Trực quan hóa Attention U-Net 2D trên mẫu kiểm thử Sample ID: 5 (ảnh bàn chân). (số liệu đang cập nhật) — mặt nạ dự đoán lệch hoàn toàn khỏi tổn thương thực tế, minh họa rõ hiện tượng attention bị kích hoạt sai vùng nhiễu thiết bị.}
    \label{fig:qualitative_attunet2}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_pga_3.png}
    \caption{Trực quan hóa PGA-UNet trên mẫu IMG001100 — hàng trên: kịch bản Zoom-out; hàng dưới: kịch bản Shift. Minh họa khả năng bám sát khối u lớn ở vùng đùi xương dài với độ chính xác cao trong cả hai kịch bản.}
    \label{fig:qualitative_pga3}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_sam_1.png}
    \caption{Trực quan hóa SAM-Med2D trên mẫu IMG001768 — 3 hàng tương ứng 3 kịch bản prompt: Zoom-out, Shift, Mixed 70/30. Các cột: Bbox prompt, Ground Truth, SAM-Med2D prediction, GT/Pred overlay. (số liệu đang cập nhật).}
    \label{fig:qualitative_sam}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/vis_sam_2.png}
    \caption{Trực quan hóa SAM-Med2D trên mẫu IMG001538 — 3 kịch bản Zoom-out (xanh), Shift (đỏ), Mixed (vàng). Các cột: Bbox prompt, Ground Truth, SAM-Med2D prediction, GT/Pred overlay. Kết quả bổ sung minh chứng sự hạn chế về độ phân giải 256×256 của SAM-Med2D trên cấu trúc gót chân nhỏ.}
    \label{fig:qualitative_sam2}
\end{figure}

\subsection{Trực quan hóa theo đặc tính tổn thương (Sub-Category)}
\label{subsec:vis_subcat}

Số liệu định lượng tại Bảng~\ref{tab:subcat_baseline} và Bảng~\ref{tab:subcat_sam} được bổ sung bằng ảnh minh chứng đại diện. Với mỗi nhóm, chọn \textbf{1 mẫu điển hình nhất} để minh họa sự khác biệt rõ nhất giữa PGA-UNet và SAM-Med2D, tránh dày đặc hình ảnh. Bố cục mỗi hình: Ảnh gốc + Prompt | Ground Truth | SAM prediction | PGA prediction.

% [TODO: Chọn và thêm 3 ảnh (1 per nhóm) từ Kaggle output — 1 ảnh mỗi nhóm: Tổn thương nhỏ, Biên giới mờ, Tổn thương rõ nét]
% Cách thêm khi có ảnh:
%   1. Copy file PNG vào Report/images/ (đổi tên ngắn gọn)
%   2. Uncomment đoạn figure bên dưới

% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_fuzzy.png}
%     \caption{Nhóm \textit{Biên giới mờ} — ví dụ điển hình: SAM-Med2D (Dice=0.46) bỏ sót phần lớn tổn thương do biên không rõ ở độ phân giải 256px; PGA-UNet (Dice=0.82) bám sát biên nhờ heatmap dẫn đường tại 512px.}
%     \label{fig:vis_subcat_fuzzy}
% \end{figure}
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_small.png}
%     \caption{Nhóm \textit{Tổn thương nhỏ} — SAM (Recall=0.18, HD95=181px) bỏ sót; PGA (Recall=0.91, HD95=3px) phân đoạn chính xác.}
%     \label{fig:vis_subcat_small}
% \end{figure}
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.98\textwidth]{images/vis_subcat_clear.png}
%     \caption{Nhóm \textit{Tổn thương rõ nét} — cả hai mô hình đều tốt; PGA nhỉnh hơn (Dice=0.89 vs 0.85).}
%     \label{fig:vis_subcat_clear}
% \end{figure}

% ------------------------------------------------------------
\section{Giới hạn kiến trúc và phân tích trường hợp thất bại}
\label{sec:failure_cases}
% ------------------------------------------------------------

Bên cạnh các kết quả tích cực đã trình bày, việc phân tích khách quan các trường hợp kiến trúc PGA-UNet hoạt động kém là cần thiết để đánh giá đúng giới hạn và định hướng cải tiến.

\subsection{Trường hợp PGA-UNet dự đoán kém}

Qua quá trình kiểm tra trên tập test, nhóm nhận diện được ba nhóm trường hợp mà PGA-UNet cho kết quả phân đoạn dưới mức trung bình (Dice $< 0.70$):

\begin{itemize}
    \item \textbf{Tổn thương có hình dáng bất thường:} Các khối u dạng kéo dài, phân nhánh hoặc có đường biên không liên tục thường vượt quá khả năng biểu diễn của bản đồ nhiệt Gaussian (vốn có dạng hình elip đối xứng). Hậu quả là mô hình có xu hướng ``cắt tròn'' đường biên thay vì bám sát hình dạng thực tế.
    \item \textbf{Vùng chồng lấp đa lớp xương dày đặc:} Tại các vị trí như khớp cổ chân nơi nhiều cấu trúc xương chồng lên nhau, gradient cạnh bị nhiễu nghiêm trọng khiến bộ giải mã gặp khó khăn trong việc xác định ranh giới chính xác, ngay cả khi có prompt hướng dẫn.
    \item \textbf{Ảnh chất lượng thấp:} Các ảnh X-quang bị mờ, thiếu sáng hoặc có nhiễu kỹ thuật còn sót lại sau tiền xử lý đều làm suy giảm chất lượng đặc trưng mà bộ mã hóa trích xuất được.
\end{itemize}

% ============================================================
% PHẦN II — ĐÓNG GÓP 2: Pipeline Lâm Sàng End-to-End
% ============================================================

\section{Tổng quan Đóng góp 2: Sản phẩm phần mềm lâm sàng}
\label{sec:product_overview}

Trong khi Đóng góp 1 tập trung chứng minh chất lượng phân đoạn của kiến trúc PGA-UNet trên dữ liệu thuần túy, \textbf{Đóng góp 2} hướng đến việc xây dựng một \textbf{hệ thống phần mềm hỗ trợ chẩn đoán lâm sàng hoàn chỉnh, triển khai được}. Pipeline end-to-end này bao gồm ba thành phần liên kết:

\begin{enumerate}
    \item \textbf{Gác cổng sàng lọc (MobileNetV4):} Tự động phân loại ảnh X-quang đầu vào thành "có bệnh lý" / "bình thường", giúp bác sĩ tập trung vào ca nghi ngờ mà không phải xem thủ công từng ảnh.
    \item \textbf{Ứng dụng tích hợp (\texttt{app.py}):} Đóng gói toàn bộ pipeline vào giao diện tương tác, sẵn sàng triển khai lâm sàng.
\end{enumerate}

Các mục tiếp theo đánh giá định lượng từng thành phần. Hiệu năng tổng thể end-to-end (cascading error thực nghiệm) sẽ được bổ sung sau khi chạy test trên tập dữ liệu hỗn hợp (xem Mục~\ref{subsec:cascading_error}).

% ------------------------------------------------------------
\section{Đánh giá mô hình phân lớp gác cổng (MobileNetV4)}
\label{sec:danh_gia_phan_lop}
% ------------------------------------------------------------
% ------------------------------------------------------------

Mô hình phân lớp đóng vai trò là "người gác cổng" (gatekeeper) trong luồng xử lý đầu cuối của phần mềm. Nhiệm vụ của mô hình là sàng lọc tự động ảnh X-quang đầu vào thành hai nhóm: Bình thường (Âm tính) và Có bệnh lý bất thường (Dương tính). Việc đánh giá chính xác hiệu năng của module này mang ý nghĩa quyết định, bởi một dự đoán sai sót ở bước này có thể khiến bệnh nhân bị lọt lưới sàng lọc mà không bao giờ đi tới được bước phân đoạn chi tiết phía sau.

\textbf{Các độ đo đánh giá (Evaluation Metrics):}
Để có cái nhìn toàn diện, mô hình được đánh giá qua các độ đo tiêu chuẩn của bài toán phân loại nhị phân: Độ chính xác tổng thể (Accuracy), Độ chính xác (Precision), Độ bao phủ/Độ nhạy (Recall/Sensitivity), Độ đặc hiệu (Specificity) và Điểm F1 (F1-Score). 

Đặc biệt trong bối cảnh y tế dự phòng và sàng lọc, \textbf{Độ nhạy (Recall)} là chỉ số được ưu tiên hàng đầu. Một mô hình sàng lọc tốt thà báo động nhầm (False Positive) để bác sĩ tốn thêm chút thời gian kiểm tra lại ở bước phân đoạn, còn hơn là bỏ sót bệnh nhân thực sự có khối u (False Negative).

\begin{table}[htbp]
    \centering
    \caption{Kết quả đánh giá hiệu năng của mô hình phân lớp sàng lọc trên tập kiểm thử}
    \label{tab:classification_results}
    \renewcommand{\arraystretch}{1.3}
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Độ đo đánh giá (Metrics)} & \textbf{Kết quả} \\
        \hline
        Độ chính xác tổng thể (Accuracy) & \textbf{85.77\%} \\
        \hline
        Độ chính xác (Precision / PPV) & \textbf{83.11\%} \\
        \hline
        Độ nhạy (Recall / Sensitivity) & \textbf{89.64\%} \\
        \hline
        Độ đặc hiệu (Specificity) & \textbf{81.91\%} \\
        \hline
        Giá trị dự báo Âm tính (NPV) & \textbf{88.85\%} \\
        \hline
        Điểm F1 (F1-Score) & \textbf{86.25\%} \\
        \hline
        Diện tích dưới đường cong (AUC-ROC) & \textbf{0.9514} \\
        \hline
    \end{tabular}
\end{table}

\textbf{Phân tích kết quả:}
Bảng~\ref{tab:classification_results} tổng hợp hiệu năng của mô hình MobileNetV4-Hybrid-Medium (37.88 MB, $\approx$9.8 triệu tham số) sau quá trình tinh chỉnh hai giai đoạn trên BTXRD. Các chỉ số chứng minh module Gatekeeper hoạt động ổn định và đáng tin cậy trong bối cảnh y tế:
\begin{itemize}
    \item \textbf{Recall 89.64\%:} Chỉ số ưu tiên số một trong sàng lọc y tế — mô hình bắt được $\approx$90\% ca bệnh thực sự, đảm bảo bệnh nhân không bị bỏ sót trước khi đến bước phân đoạn.
    \item \textbf{AUC-ROC 0.9514:} Diện tích dưới đường cong ROC đạt 0.95 phản ánh khả năng phân biệt bệnh/không bệnh xuất sắc, bất kể ngưỡng phân loại được chọn.
    \item \textbf{Specificity 81.91\%:} Trong 5 ca bình thường, hệ thống loại đúng $\approx$4 ca, hạn chế việc chuyển ảnh lành sang module phân đoạn (tiết kiệm tài nguyên tính toán).
    \item \textbf{NPV 88.85\%:} Khi hệ thống kết luận "bình thường", xác suất thực sự không có bệnh là 88.85\% — đủ tin cậy để sử dụng như bộ lọc sơ cấp.
    \item \textbf{F1-score 86.25\%:} Trị số F1 cao và cân bằng, phản ánh mô hình không bị lệch quá nhiều về bất kỳ loại lỗi nào.
\end{itemize}
Hình~\ref{fig:confusion_matrix} trực quan hóa Ma trận nhầm lẫn và Đường cong ROC của mô hình.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{images/classification_evaluation.png}
    \caption{Đánh giá trực quan mô hình phân lớp MobileNetV4: (a) Ma trận nhầm lẫn (Confusion Matrix), (b) Đường cong ROC (AUC = 0.9514) và (c) Phân phối xác suất dự đoán.}
    \label{fig:confusion_matrix}
\end{figure}

\subsection{Phân tích sai số tích lũy của pipeline (Cascading Error)}
\label{subsec:cascading_error}

Khi đánh giá độ chính xác của \textbf{hệ thống end-to-end}, cần tính đến sai số tích lũy giữa hai module nối tiếp. Ước tính lý thuyết:

\begin{equation}
    \text{Hiệu năng tổng thể (ước tính)} \approx \text{Recall}_{\text{MobileNetV4}} \times \text{Dice}_{\text{PGA-UNet}}
    = 89.64\% \times 85.58\% \approx \mathbf{76.7\%}
\end{equation}

Diễn giải: trong 100 ca bệnh thực sự, MobileNetV4 bỏ sót $\approx$10 ca (Recall = 89.64\%). Trong 90 ca được chuyển xuống PGA-UNet, mô hình phân đoạn đúng $\approx$86\% → hiệu quả tổng $\approx$76.7\%.

\textbf{Xác minh thực nghiệm (đang chuẩn bị):} Để thay thế ước tính lý thuyết bằng con số đo thực tế, nhóm sẽ tạo một tập kiểm thử hỗn hợp gồm \textbf{\Ntest ảnh có bệnh} (từ tập test hiện tại) cộng với \textbf{$N$ ảnh không bệnh} (từ BTXRD hoặc bổ sung thêm), gói chung vào một thư mục, chạy pipeline đầy đủ MobileNetV4 $\rightarrow$ PGA-UNet và ghi nhận:
\begin{itemize}
    \item Tỉ lệ ảnh bệnh được phân lớp đúng và đưa xuống phân đoạn (True Positive Rate)
    \item Tỉ lệ ảnh không bệnh bị lọc đúng (True Negative Rate)
    \item Dice trung bình trên toàn bộ ảnh có bệnh trong tập hỗn hợp
    \item Hiệu năng tổng thực nghiệm = (TP phân lớp) $\times$ (Dice phân đoạn)
\end{itemize}

% [TODO: Chạy test hỗn hợp sau khi có notebook. Xem EVALUATION.md task "Cascading error empirical test"]

\textbf{Tại sao con số này vẫn chấp nhận được trong ngữ cảnh lâm sàng thực tế?}

\begin{itemize}
    \item \textbf{Thiết kế Human-in-the-loop làm giảm nhẹ cascading error:} Hệ thống không hoạt động độc lập — bác sĩ luôn xác nhận kết quả phân lớp trước khi PGA-UNet chạy. Do đó, 10.36\% ca bị MobileNetV4 bỏ sót vẫn có cơ hội được bác sĩ phát hiện thủ công.
    \item \textbf{Mục tiêu là hỗ trợ, không phải thay thế:} Hệ thống đóng vai trò giảm tải công việc cho bác sĩ (lọc $\approx$82\% ca bình thường tự động), không phải thay thế hoàn toàn. Việc thừa nhận sai số tích lũy là minh bạch và phù hợp với chuẩn mực y tế.
    \item \textbf{Bottleneck rõ ràng:} Con số 76.7\% cho thấy bottleneck nằm ở module phân lớp (MobileNetV4), \textbf{không} phải kiến trúc PGA-UNet. PGA-UNet đánh giá độc lập đạt Dice=0.8606 — đây là số sạch, chứng minh kiến trúc phân đoạn tốt. Cải thiện bước phân lớp sẽ trực tiếp nâng độ chính xác tổng thể (xem hướng phát triển tại Chương~\ref{Chapter5}).
\end{itemize}

Việc mô hình phân lớp đạt AUC-ROC = 0.9514 và Recall = 89.64\% chính là bản lề vững chắc để toàn bộ hệ thống tự tin chuyển giao quyền điều khiển sang cho module phân đoạn tương tác PGA-UNet trong luồng thực thi (Online Inference Pipeline).

% ------------------------------------------------------------
\section{Tích hợp và đánh giá ứng dụng lâm sàng (app.py)}
\label{sec:app_integration}
% ------------------------------------------------------------

Thành phần cuối cùng của Đóng góp 2 là \texttt{app.py} — nơi toàn bộ hai module (MobileNetV4 gác cổng, PGA-UNet phân đoạn) được tích hợp vào một ứng dụng phần mềm duy nhất có thể triển khai trực tiếp. Đây là bằng chứng cụ thể nhất cho tính \textbf{end-to-end} của đóng góp sản phẩm: không chỉ là nghiên cứu học thuật, mà là một hệ thống hoạt động thực tế có thể bàn giao cho cơ sở y tế. Ứng dụng cung cấp giao diện đồ họa (GUI) trực quan, cho phép bác sĩ tương tác mà không cần kiến thức kỹ thuật.

\textbf{1. Giao diện và Trải nghiệm người dùng (UI/UX):}
Giao diện phần mềm được thiết kế theo nguyên tắc tối giản, tập trung vào công tác chẩn đoán. Màn hình làm việc chính được chia thành các khu vực rõ ràng: khu vực tải ảnh bệnh nhân, bảng điều khiển trạng thái (báo cáo phân lớp), và không gian hiển thị ảnh X-quang trung tâm. Bác sĩ có thể thao tác phóng to, thu nhỏ và sử dụng chuột để trực tiếp vẽ hộp giới hạn (Bounding Box) lên ảnh với trải nghiệm mượt mà. Kết quả mặt nạ dự đoán được hiển thị dưới dạng các lớp phủ (overlays) có thể tùy chỉnh độ trong suốt, giúp bác sĩ dễ dàng đối chiếu vùng dự đoán với cấu trúc giải phẫu gốc.

\textbf{2. Luồng thực thi chẩn đoán (Inference Pipeline):}
Ứng dụng thực thi chính xác luồng chẩn đoán đã được định nghĩa ở Mục 1.3.2 và Chương 3:
\begin{itemize}
    \item Ngay khi ảnh X-quang được tải lên, module tiền xử lý và mô hình phân lớp sẽ tự động chạy ngầm. Nếu kết quả là âm tính, ứng dụng hiển thị thông báo "Bình thường" và kết thúc luồng kiểm tra.
    \item Nếu kết quả là dương tính (có u/tổn thương), ứng dụng bật chế độ tương tác và chờ bác sĩ vẽ câu nhắc.
    \item Sau khi bác sĩ vẽ câu nhắc, hệ thống xử lý và trả về mặt nạ phân đoạn. Hệ thống sẽ ở trạng thái chờ bác sĩ bấm xác nhận "Đồng ý" hoặc chọn "Vẽ lại", tôn trọng tuyệt đối quyền quyết định cuối cùng của con người.
\end{itemize}

\textbf{3. Khả năng triển khai thực tế:}
Ứng dụng được thiết kế để chạy trên phần cứng thông thường tại cơ sở y tế mà không yêu cầu GPU chuyên dụng ở phía người dùng. Tối ưu hóa hiệu năng thời gian phản hồi là một định hướng phát triển tiếp theo (xem Chương~\ref{Chapter5}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth, height=0.82\textheight, keepaspectratio]{images/app_interface.png}
    \caption{Giao diện ứng dụng hỗ trợ chẩn đoán (\texttt{app.py}) minh họa luồng xử lý tương tác giữa bác sĩ và hệ thống AI}
    \label{fig:app_interface}
\end{figure}

Tóm lại, ứng dụng phần mềm không chỉ là một bản demo kỹ thuật đơn thuần, mà là một minh chứng trọn vẹn cho triết lý thiết kế "Human-in-the-loop": Trí tuệ nhân tạo không nhằm mục đích thay thế chuyên gia, mà đóng vai trò như một người trợ lý thông minh, sẵn sàng hỗ trợ, nhắc nhở và làm việc một cách minh bạch trong toàn bộ quá trình chẩn đoán y khoa.