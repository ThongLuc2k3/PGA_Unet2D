/.claude

# JSON annotation gốc của dataset (tải riêng, không cần commit)
/dataset_json/
/Source/Report
/diagrams

# Executed training/testing notebooks and their exported CSVs: large, and the
# canonical copies live under Source/. Re-run on Kaggle rather than committed.
/Result/

# Trained checkpoints under Results/: large binaries, re-downloadable via the
# Google Drive IDs already saved in the corresponding notebooks
Results/**/*.pth

# LaTeX build artifacts
Paper_IEEE_Access/**/*.aux
Paper_IEEE_Access/**/*.log
Paper_IEEE_Access/**/*.fls
Paper_IEEE_Access/**/*.fdb_latexmk
Paper_IEEE_Access/**/*.synctex.gz
Paper_IEEE_Access/**/*.out