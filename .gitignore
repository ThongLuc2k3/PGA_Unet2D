/.claude

# Kết quả CSV sinh ra từ các notebook ablation (results/per-image) — không cần commit
**/Ablation/**/*.csv

# JSON annotation gốc của dataset (tải riêng, không cần commit)
/dataset_json/
/Source/Report
/diagrams
/ACCESS_latex_template_20260513-1-1